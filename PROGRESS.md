# PROGRESS

## Current Status

- **CI update (2026-09-13, #479):** GitHub Actions runners are back; the release-notes step was fixed in #478 and run 192 (desktop 0.1.192) is green — the first successful build since 0.1.139. The release feed should now carry it. The owner-report line below is kept for history.

- **⚠ OWNER REPORT (2026-09-07, later): the newest installer they can get still opens a dark, empty window.** The release feed tops out at 0.1.139 (built 05:17 UTC); every push since is unbuildable — desktop-release runs 140–165 fail in four seconds with `runner_id: 0` (no runner assigned: GitHub Actions spending limit / billing on the account — owner action). #451 makes the watchdog fire on "no painted text" (the mounted flag was set before React drew anything) and report in-flight requests. The other items in that batch (project CRUD, open from disk, missing PDF, icon, achievements) shipped in 0.1.11x–0.1.139; if they still show, the installed build is older than it looks — Diagnostics says the version.

- **Cadence change (2026-09-12, owner):** one shipped slice per hour — `/loop 1h /cycle` fires `/cycle` at the top of every hour; each firing ships one slice. The owner merged the branch into `main` via PR #1 (branch A1); this branch keeps receiving slices.

- **AUDIT #31 (2026-09-15, #528).** pip-audit clean, npm audit clean, `scripts/audit.sh` green; new surfaces since #518 probed (anon/wrong key 401 on export, reading-now, check-retractions GET/POST, progress GET/POST; export `fmt` junk → 400, ids capped at 500, CSV formula prefixes quoted — now incl. tab and CR; `year_min`/`year_max`/`author`/`retracted`/`sort` junk ignored or whitelisted; `fromUrl` reads known keys only, 200 chars each; `browse_library.url` percent-encoded; check-retractions 51 ids / junk ids / non-JSON → 400, unknown ids → nothing checked, limit clamped to 50, a live Crossref check of four DOIs in 5.6 s, the offline breaker pinned). **Three findings fixed:** (1) `POST references/{id}/progress/` with a page or page count past the 32-bit column was a **500** → `max_value=100_000` on the serializer + `MAX_PAGES` in `record_position`, 400 / `ProgressError`, pinned in test_progress.py; (2) `POST check-retractions/ {stale, days: 10^9}` overflowed the timedelta — **500** → `days` clamped to `MAX_STALE_DAYS` (ten years) in `stale_references`, pinned in test_retractions.py; (3) an id past the 32-bit column reached `pk__in` on seven paths (export/bibliography `ids=`, check-retractions, both bulk bars, three reorders) — harmless on Postgres, an `OverflowError` **500** on the desktop's SQLite (Django's `__in` does not range-check) → `core/ids.py` (`MAX_PK`, `parse_ids`) + a bounded `PkField` + the reorders' guards, pinned in core/tests/test_ids.py. Query counts on the new endpoints 4–18, all grouped; hot endpoints 6–52 ms in-process. Desktop run 238 (#527) green. Report: AUDITS.md › Audit #31. **Next audit due at #538.**

- **Today (return pass) — a Trash: a deleted item waits thirty days, comes back as it was, keeps its chain (2026-09-19, #570, backlog 344, 158 tools).** `TodoItem.deleted_at` (core 0016) + a hiding default manager (`objects`) and `all_objects`; `core/todos.py` `trash` / `restore` / `prune_trash` (`TRASH_DAYS` 30); `spawn_next` restores a trashed successor instead of doubling, `unspawn` removes trashed untouched ones; API: DELETE trashes (`?forever=true` or a second DELETE deletes), `POST restore/`, `?trash=true`, `POST empty-trash/`, 409 on PATCH / snooze of a trashed row, clear-done trashes (`{deleted, trashed}`); serializer `deleted_at`; admin sees the Trash (`EmptyFieldListFilter`); huey `prune_todo_trash_task` 05:40 + the desktop scheduler block; MCP `list_todos(when="trash")` + `trash_todo(restore, forever)`, `get_compile_diagnostics` folded into `get_compile_status` (158 flat); Today page: the delete undo calls restore, a collapsible Trash section (Restore, confirmed Delete forever, confirmed Empty); seed: one row trashed three days ago; README + atlas-daily skill. Evidence: `core/tests/test_trash.py` (12: managers hide the Trash for relations / open_today / achievements, trash + restore keep stamp / rule / position and move the ETag, prune past 30 d, the chain walk both ways, API delete / lists / 409s / restore / forever / second DELETE / clear-done / empty-trash / anon 401 / reorder refuses a trashed id, the sweeps wired, the seed, MCP names + docstrings); `test_repeat` / `test_logbook` / `test_today` pins updated to the new clear-done shape and the successor keeping its chain until the prune; `test_today_undo.py` rewritten to the restore contract; live curls (delete 204 → live list without it, trash list with it, PATCH / snooze 409, restore `deleted_at` null, forever 204 → 404, anon 401); `$S/probe570.py` dark + light (Trash · n header, delete → toast "in the Trash for 30 days" → `z` puts the row back, a done row restored keeps its stamp and lands in Done today, Delete forever and Empty the trash through the confirm dialog, the section disappears when empty); ruff, tsc, spectacular, migrations check, full suite 1968 passed / 1 skipped (three source pins in `test_today_narrow` / `test_today_undo` updated for the fourth row kind and the restore contract, then green). Desktop runs 300 (#569) and 301 (its follow-up) green; run 302 (this push) queued at push time.
- **Prompts — the narrow-width pass + the Prompts verdict (2026-09-19, #569, 158 tools).** Measured first: 640 px (a 336-px column beside the rail) scrolled 193 px sideways, 420 px 157 px — the card header row never wrapped and the toolbar overran. `Prompts.tsx`: the header is title · actions · meta (title `flex-1 basis-0 max-w-max truncate`; Copy / Next / ⋮ one group before the chips in the DOM, `ml-auto`, `lg:order-last`; chips `order-last basis-full` below `lg`, beside the title from it), the toolbar wraps with the search box giving way (`flex-1 basis-40 max-w-72`), the form's columns start at `lg`, a picked chip shrinks inside its label, the picker's hit list hangs from the input's right edge when its left box would overflow (`useLayoutEffect`, `flip`). Viewport breakpoints rather than `@container` because the kebab's `fixed` menu would be re-anchored by containment. **Verdict:** Prompts is best-in-field for a single-user research tool (typed fill-ins from Atlas, use counts + history, "Use with this…" on every object, chains, Claude on the same contract); backlog 366 (body versions), 367 (⌘K copy verb + hotkey), 368 (import / export) logged; pointer → Today's return pass (344 at #570, 345 at #571), then Diagnostics. Evidence: `$S/probe569.py` at 640 / 420 / 1440 dark + 640 light (`scrollWidth === clientWidth` on the gallery, the history panel, the picker open, the use banner, the form; actions on the title line at every width; meta under the title below `lg`, beside it at 1440; picker right edge 508 ≤ 640); `$S/shot569.py` before / after at 1440 — the first title box identical (x 293, w 165); `prompts/tests/test_narrow.py` (source + chunk + stylesheet needles, DOM order of the groups); the 73 prompt / hooks / CRUD-needle tests; tsc; `make js` (`make css` a no-op — every utility already existed); ruff; full suite 1959 passed / 1 skipped. The first full run caught a flake in #568's zip test — the zip format keeps two-second resolution, so an odd `updated_at` second failed half the time — fixed at the root: `documents/bulk.py::zip_time` floors the stamp to an even second before it is written, and the test pins that. Desktop runs 298 (#568) and 299 (its follow-up) green; run 300 (this push) queued at push time.
- **AUDIT #35 (2026-09-19, #568).** `scripts/audit.sh` flagged **anyio 4.13.0** (CVE-2026-63374, CVE-2026-64847) → 4.15.1 via `uv lock --upgrade-package anyio` (+ typing-extensions 4.16.0), pinned ≥ 4.14.2 by test; npm audit clean; every surface since #558 probed (prompts / render / uses / tags / merge anon → 401; reference ids past 32-bit and past int → 404; `values` as a list → 400; `next` self / missing / string → 400; tag merge self 400, cross-project 404, junk colour / long / blank name 400; 11 `?tag=` → 200 capped; 501 bulk ids → 400; latency 13–18 ms). **Three findings, fixed:** a Unicode digit (`"²"`) as a typed fill-in was a 500 (`isdigit()` guarding `int()`) — `isdecimal()` at all ten `isdigit → int` pairs (`prompts/services.py`, `api/views.py` ×5, `literature/library.py` ×2, `literature/matrix.py` ×2, `core/comments.py`); an object / list render value leaked a Python repr into the text — 400 (message never "No …"), not a use; the admin could save a chain loop — `PromptAdminForm.clean()` runs `chain_would_cycle`. #34 carry-over closed: `build_archive` copies members with `shutil.copyfileobj` through an explicit `ZipInfo` (post-ship advisor catch: a bare name to `open()` stamps 1980 and no mode — members now carry the file's last change, deflate and 0600, pinned), `duplicate_document` streams the open `FieldFile`; the admin change form is exercised through `admin_client` (a self-`next` POST re-renders with the loop error, the row stays unlinked). Evidence: `core/tests/test_audit35.py` (9: object / list → 400, `²` `①` `٣` never 500, `?depth=²` 400 / `?year=²` 200 / `?limit=²` 400 / `?run=²` 200, matrix lookups fall through, admin form refuses self + two-cycle and passes a chain / no next / a new prompt, duplicate + zip byte equality past one chunk, anyio ≥ 4.14.2); live curls after restart (dict / list 400, `²` 200 as text, 200k 200, `depth=²` 400); `scripts/audit.sh` fully green after the bump; MCP `get_prompt(9, values={'paper':'22'}, history=True)` → 11 uses, an object value raises `AtlasClientError` 400; ruff, spectacular, migrations check, full suite 1957 passed / 1 skipped. AUDITS.md report; DECISIONS entry; next audit at #578. Desktop runs 296 (#567) and 297 (#567 follow-up) green; run 298 (this push) queued at push time.
- **Prompts — slice 6: chains (2026-09-19, #567, 158 tools).** `Prompt.next` (prompts 0004, self FK, SET_NULL), `chain_would_cycle` (20 hops) behind `PromptSerializer.validate` → 400 "loop", `next` writable + `next_title`, `select_related("next")` keeps the list budget ≤ 8; gallery: "then → Title" chip (`prompt-next`), a "Next: Title →" button after Copy (`prompt-next-step`) that hands values to the next card (`carryOver`: same name, else same kind; a parent `handoff` state merged by a `useEffect` in the card) and flashes it, a "then" select in the form (`prompt-next-select`); MCP docstring line; seed: "Find the gap in {{paper}}" + Summarize → Find the gap, once. Evidence: `prompts/tests/test_chains.py` (10: straight chains pass, self / 2- / 3-cycles and the hop limit caught; API set / read / clear, loop 400 leaves the row untouched, SET_NULL on delete, list under budget; MCP docstring; UI needles + chunk; seed once, the owner's chain survives a re-seed); `$S/probe567.py` in dark and light (then chip; pick a paper + venue → Copy → Next → the gap card flashes with the same paper chip; the form's select excludes the card itself; a loop through the form is refused with the error dialog; after the fix, the Next button survives the 2 s "Copied" flash); ruff, tsc, spectacular, migrations check, full suite 1948 passed / 1 skipped. Desktop run 296 (ef43373) queued at push time; 295 (#566 follow-up) green.
- **Prompts — slice 5: the remaining "Use a prompt with this…" entry points (2026-09-19, #566, 158 tools).** Project overview menu (`use=project:${project.id}`; the TSX overview type gains the payload's `id`), a "Use a prompt" link beside Open in the Library detail pane (`pane-use-prompt`), a Studio palette action that awaits the save, then navigates (`useNavigate` declared in `StudioInner`, above every early return); `seed_demo` adds a `{{project:project}}` prompt. Evidence: `prompts/tests/test_use_with.py` (needles for all three pages incl. the save-then-navigate call, seed per kind now four); `$S/probe566.py` in dark and light (overview kebab → banner "Using Attention and Working Memory — 1 prompt takes a project" with the chip; Library row → pane → link → banner; Studio ⌘⇧P → "use a prompt" → Enter → banner; then the dirty-buffer path: a character typed into main.tex, the palette action, the banner, and the workbench read shows the character saved — the action now awaits `saveAll()` before `navigate`); ruff, tsc, spectacular, migrations check, full suite 1938 passed / 1 skipped. Desktop run 294 (8a00a76) queued at push time; 293 (#565) green.
- **Prompts — slice 4: a history per prompt + Use again (2026-09-19, #565, 158 tools).** `PromptUse` (prompts 0003: prompt FK, `variables` JSON = the render's resolved fill-ins, ids + labels capped at 300 chars, never text), filed by `record_use(prompt, variables)` in one transaction with the counter, last 50 kept; render returns `use`, serializer `last_use` via a sliced `Prefetch` (list budget ≤ 8 queries pinned), `GET /prompts/{id}/uses/`; MCP `get_prompt(…, history=True)`; gallery: "last used … with …" line, History panel behind the "used n×" chip (lazy `useQuery`), Use again refills the card, Copy invalidates the panel; seed: three uses per used demo prompt through the real renderer, once. Evidence: `prompts/tests/test_history.py` (10: row + variables, bare call keeps `updated_at`, keep-50 + all-time count, cap + junk, API render → use + list `last_use` under budget, uses newest-first + 401, failed render files nothing, MCP history flag + signature, UI needles + chunk, seed once); `$S/probe565.py` in dark and light (line, panel 3 rows, Use again → venue ICLR + paper chip, Copy → new top row, clipboard holds the paper, "as written" row without Use again); live API `/uses/` + `last_use`; live MCP `get_prompt(2, {venue}, history=True)`; ruff, tsc, spectacular, migrations check, full suite 1938 passed / 1 skipped (the rules-of-hooks guard caught a helper named `useSummary` in JSX below the early return — renamed `summarizeUse`); the sliced prefetch + `last_use` repeated on SQLite 3.45 under the desktop settings with the same answer. Desktop run 293 (66f758f) queued at push time; 292 (#564) green.
- **Prompts — slice 3: "Use a prompt with this…" (2026-09-19, #564, 158 tools).** `/prompts?use=<kind>:<id>&label=` filters the gallery to the prompts that take that kind, pre-picks the object into every fill-in of the kind (card remounts per address), banner with × Show all, no Recent strip while filtering, empty state with a pre-filled New prompt form; entry points: Reference page menu, Library row menu, manuscript page menu, note editor header (`note-use-prompt`); API `GET /prompts/?kind=` (whitelist → body regex, documented); MCP `list_prompts(query, kind)`; seed: a `{{note:note}}` and a `{{draft:manuscript}}` prompt. Evidence: `prompts/tests/test_use_with.py` (5: kind filter hit / miss / second position / junk / case / composes with q, MCP client params, UI needles across five pages + chunk, seed per kind); `$S/probe564.py` in dark and light (Reference kebab → gallery: 1 card, chip carries the title, Copy → clipboard; × Show all → 5 cards, address clean; note header → note pre-picked; junk address → plain gallery; project kind → empty state); live API `?kind=` per kind + MCP `list_prompts(kind="note")`; the kind filter repeated on SQLite (desktop settings) and a render with the paper id grepped for the paper; ruff, tsc, spectacular, migrations check, full suite 1928 passed / 1 skipped. Desktop run 292 (70487e9) queued at push time; 291 (#563) green.
- **Prompts — slice 2: a use count + a Recent strip (2026-09-18, #563, 158 tools).** `Prompt.use_count` / `last_used_at` (prompts 0002) bumped by `record_use` after a successful render only (`save(update_fields)` — `updated_at` untouched, ETag bumps, `F()` increment); `POST /prompts/{id}/render/` carries both back; serializer exposes them read-only; MCP `get_prompt` always renders (defaults fill) so a fetch to use counts; gallery: "used n×" chip per card, a Recent strip of the last five copied (hidden while searching; click → scroll + flash), the list patched in place after Copy; `seed_demo` seeds a history for two prompts only when never used. Evidence: `prompts/tests/test_usage.py` (7: increment + stamp + `updated_at` unchanged, fresh row, API render counts + carries back, failed render counts nothing, read-only on write, UI needles, seed + re-seed keeps real usage) + MCP tests re-pinned; `$S/probe563.py` in dark and light (strip 3 chips → copy → chip 2×→3×, strip reordered, clipboard holds the text, flash ring, strip hidden while searching); live MCP `get_prompt(2, {venue: ICLR})` → `use_count` 13; ruff, tsc, spectacular, migrations check, full suite 1923 passed / 1 skipped.
- **Prompts — slice 1: typed fill-ins picked from Atlas (2026-09-16, #562, 158 tools).** `{{paper:reference}}` / `{{x:note}}` / `{{x:project}}` / `{{x:manuscript}}` draw a picker in the gallery (global search filtered by kind, ≤ 8 hits, chip with ×); Copy renders on the server (`POST /prompts/{id}/render/` → `{text, variables}`; a reference expands to title / authors / year / venue / doi / abstract, the others to title + body; 50 000-char cap per fill-in; 404 for a missing row, 400 for junk values); `parse_variables` → `{name, kind, default}`; `prompts/services.py` is the one row-reading place; MCP `get_prompt(prompt_id, values)` returns the rendered text (fold, count flat); demo prompt uses `{{venue|NeurIPS}}` + `{{paper:reference}}`; remembered values migrate. Evidence: `prompts/tests/test_typed_variables.py` (8: parse, reference expansion, note/project/manuscript/text, render_with_data, cap, API endpoint, MCP fold + signature, UI needles) + `test_prompts.py` pins; `$S/probe562.py` 12/12 in dark and light (pick → chip → Copy → clipboard holds the paper); live MCP `get_prompt(id, {paper, venue})` rendered; ruff, spectacular, migrations check, full suite 1916 passed / 1 skipped. Built and verified on 16 Sep; the session then sat idle until 18 Sep 21:55 UTC (the 54 hourly firings queued in between were consumed at once, no slice was lost) and the commit landed on the 18th. Desktop run 290 (74a9a09) in progress at push time; 289 (#561) green.
- **Files — the last seam + the area verdict (2026-09-16, #561).** Measured at 420 / 768 px (empty state, tree, selection bar + filter row, pane with tags): no seam. The pane found the gap — a file created without a `kind` (classic upload, seed, import) was never previewed as text and drew a generic icon; `Document.save()` now derives a blank kind from the file name (before the title; a text-only node is `other`; an explicit kind kept), `documents/0006_derive_kinds` backfills (5 of the demo's 9 rows). **Verdict:** Files is best-in-field for a single-user research tool; what a Finder / Drive / Notion / Obsidian user would still miss logged as backlog 357–363 (undo / trash for a deleted file first, in-place text editing, full-text search inside files, Office previews, cut/paste, folder metadata, local-folder sync). Pointer → **Prompts** from #562. Evidence: `documents/tests/test_kind_derivation.py` (3: derivation order + tree `is_text`, the classic bulk upload → text / asset, the migration on rows forced blank), `$S/probe561.py` (dark + light: seams `[]` at 420 and 768 for all four states; the seed protocol previews as text on the phone after the backfill), `$S/shots561/`. ruff, spectacular, migrations check, full suite green. Desktop runs 287 + 288 (#560) green.
- **Files — slice 7: the selection drags, duplicates, and Claude organizes files by path (2026-09-16, #560, backlog 355, 158 tools).** Dragging a checked row moves every checked file (one bulk request; banner "Move n files into this folder"; an unchecked row moves alone); Duplicate from the file menu (⌘D), the bar or the key — a numbered copy next to the original with description, tags and type, no history, the copies become the selection; `untag` joins the bulk verbs; `documents/bulk.py` `available_name` / `duplicate_document` (new storage name — never shared with the original, since deletes unlink), `ACTIONS` + API choices `move | tag | untag | duplicate | delete` with `created` ids; MCP `export_bibtex` folded into `export_references(fmt="bib")`, `organize_files(project, paths, action, folder, tag)` by tree path takes the slot; a Files group on the shortcuts sheet. Evidence: `documents/tests/test_organize_files.py` (9: copy metadata / numbering / no history, no shared storage across a delete, inline text copy, per-folder numbering, service duplicate + untag with a skipped source, API duplicate → created + bad verb 400 + 401, the fold, client path / folder / tag resolution + every error, UI needles + chunk), `test_client.py` export tests moved to `export_references`; live MCP client run (write → duplicate → move by folder path → tag / untag by name → delete, unknown path named in the error); Playwright `$S/probe560.py` → `$S/shots560/{duplicate,moved}-{dark,light}.png`: 10 checks green in both themes (menu Duplicate ⌘D → `-2` copy checked, "1 selected"; ⌘D on two → two copies, "2 selected", bar Duplicate; drag a checked row → both into the folder, one bulk request, no PATCH; an unchecked row alone via PATCH; the sheet lists Files with ⌘D). ruff, tsc, `make js`, `make css`, spectacular, migrations check, full suite green. Desktop runs 285 + 286 (#559) green.
- **Files — slice 6: tags managed where they are used + the AND filter (2026-09-16, #559, backlog 354, 158 tools).** Right-click a chip in the filter row (or its ⋯ once active) → Only this tag / Show every file, Rename…, Colour… (eight hues, current marked), Merge into… (other tags with counts), Delete tag… (count as the hint, confirm "they keep their other tags"); a rename into an existing name any-case offers the merge; several chips = files carrying all of them ("1 of 9 · all 2 tags" + Clear); the active filter follows a rename / merge / delete. Backend `documents/tags.py` (rename / recolour / merge via the M2M manager / delete / `tag_names` + `filter_by_tags`), `TagSerializer.count` + `validate()` (clash names the tag, no re-parenting), `POST /tags/{id}/merge/`, repeated `?tag=` (AND, ≤ 10) on `/documents/` and `/projects/{slug}/tree/`. MCP: `list_documents` folded into `list_project_files(project, tag)` (comma = all), `manage_file_tag(project, tag, rename|color|merge_into|delete)` takes the slot; follow-up: the tag list keeps its name order under the count annotation, and the demo's protocol file also carries key-paper so two chips show a file. Evidence: `documents/tests/test_tag_management.py` (16: service clash / colour / merge idempotent + guards / delete count / names + AND + tree; API counts, clash 400 naming the tag, colour, no re-parent, merge guards + 401, AND on list + tree with a flat query count and no cross-project leak; MCP client params + signatures + `manage_file_tag` verbs; UI needles + chunk), `test_files_tags.py` updated; Playwright `$S/probe559.py` → `$S/shots559/{and,menu,colour,merge,phone}-{dark,light}.png`: 21 checks green in both themes (3 chips → AND "1 of 9 · all 2 tags" with two ⋯ → Clear; right-click menu; Colour… palette with "current"; a green dot; ⋯ → Rename with the current name → chip "methods" still active at 2 of 9, dots kept; rename to "Key-Paper" → merge confirm → two chips, key-paper 3, filter followed; Merge into… lists the others with counts; Delete confirm "It is on 1 file"; phone: tap chip, tap ⋯). ruff, tsc, `make js`, `make css`, spectacular, migrations check, full suite green. Desktop run 284 (#558) green.
- **AUDIT #34 (2026-09-16, #558).** pip-audit clean, npm audit clean, `scripts/audit.sh` green; every surface since #548 probed (versions / raw / diff / restore / replace / `?tag=` / bulk / archive / tree / logbook anon → 401; version numbers and folder / tag ids past 32-bit or past int → 404; 601 ids → 400; `?ids=` junk → 400; a 300-char tag → 200 empty (capped at 60); replace without a file 400, on a manuscript source 403; diff on a manuscript source 404; latency 21–28 ms) — **no finding across the wire**. Finding from the inside: the tests wrote into the working tree's `media/` (32 471 files) → `conftest.py` temp `MEDIA_ROOT` per test, which exposed `test_media_served_with_debug_off` passing only via an orphan (the `/media/` view bound `MEDIA_ROOT` at import; it now reads it per request). Closed: backlog 356 (`documents/signals.py` post_delete unlink on commit for `Document` + `DocumentVersion`; `manage.py prune_media` dry-run / `--apply`, scoped to the two documents prefixes — removed 32 448 orphans, 23 real files remain), 343 (`oa._download` gates the first address through `check_url` like every hop; fixture stubs the resolver), 349 / 350 / 351 (Projects header `flex-wrap`, overview grids `[&>*]:min-w-0`, constellation labels only ≥ 640 px — the three pages measure 420 at 420). 344 / 345 stay queued (Today features). Evidence: `core/tests/test_audit34.py` (6: temp media root, a fileless delete is a no-op, delete with a version + the bulk path remove files via `django_capture_on_commit_callbacks`, prune keeps referenced and out-of-scope files, the three seams + the label gate), `test_oa.py::test_first_address_on_a_private_host_is_refused`, live upload → replace → delete → both files gone, `$S/probe558x.py` before / after (441 / 608 → 420 / 420), AUDITS.md report; ruff, tsc, `make css`, `make js`, spectacular, migrations check, full suite green. Desktop runs 282 + 283 (#557) green. Next audit at #568.
- **Files — slice 5: time in the explorer — sort, stamps, a Recent strip (2026-09-16, #557, 158 tools).** Tree rows carry `created_at` / `updated_at` / `modified_at` (the bytes' last change = the newest version's stamp or the creation, on the existing versions join); the Explorer header gets a Name / Last change / Size sort (folders first by name; remembered); Last change shows a relative stamp per row instead of the size, every row's title carries the absolute dates, the pane says "added … · changed …" with absolute times on hover; a collapsible Recent strip (six last-changed files, ≥ 6-file trees, hidden under a tag filter; click opens the folders above and selects). Found first: inline-text nodes had `file_size` 0 → `Document.save()` sizes content in bytes. Checked: the explorer fits at 420 / 640 / 768 px (no overflow), so the narrow-width pass is retired. MCP unchanged; `list_project_files` docstring names the stamps. Evidence: `documents/tests/test_files_time.py` (5: a tag / description leaves `modified_at` alone while a replace moves it, the version count survives the Max join, inline nodes carry bytes, the docstring, UI needles + chunk), `test_workspace_tree.py` key set; curl (a tag bumped `updated_at` and left `modified_at`); Playwright `$S/probe557.py` → `$S/shots557/{recent,modified}-{dark,light}.png`: 13 checks green in both themes (default Name, Recent lists the fresh file first "just now", no row stamps, clicking opens Methods + selects, pane "added just now · changed just now" with a title, Last change → first in Methods with stamps on every row and absolute dates in titles, Size → the 900-byte note leads, the sort and the collapsed strip survive a reload). ruff, tsc, `make js`, spectacular, migrations check, full suite green. Desktop runs 280 + 281 (#556) green.
- **Files — slice 4: a selection — zip, move, tag, delete many files at once (2026-09-16, #556, 158 tools).** Checkboxes on file rows (hover-revealed, always on a coarse pointer; a click does not change the pane), shift-click ranges over the visible rows, space / ⌘A / Esc on the tree, a bar with Download zip · Move to… · Tag… · Delete… · Clear, a "Download as zip" on every folder menu; the selection pruned to what the tree shows. `documents/bulk.py`: `clean_ids` (distinct, ≤ 500, junk refused), `sync_rel_path` (lifted from the API's `perform_update`; the classic Documents-page bulk move never recomputed `rel_path` — found first, now one service behind the bar, the API and the classic form), `bulk_documents` (manuscript sources skipped + reported, per-instance delete; storage files were never removed on delete — backlog 356), `archive_members` / `build_archive` (spooled temp file, members by `rel_path` — relative to the folder for a folder zip, inline text from `content`, `safe_archive_name` + "(2)" collisions, 512 MB cap → 413). API: `POST /projects/{slug}/documents/bulk/`, `GET /projects/{slug}/archive/?ids=|folder=`. MCP unchanged (backlog 355 logs the parity gap + fold path). Evidence: `documents/tests/test_bulk_files.py` (12: ids cleaning, move recomputes paths + skips sources, tag idempotent + delete, foreign folder / tag / unknown action, folder-relative vs selection members + inline text, unsafe + duplicate names, size cap, the bulk endpoint 200 / 404 / 404 / 400 / 400 / 401, the archive endpoint members + 400 / 400 / 404 / 401, 413, the classic bulk move keeps `rel_path`, UI needles + chunk); curl (folder zip `Pilot/pilot-rt.csv`, a selection zip with project paths and an unknown id ignored, junk 400, tag → skipped [source], move to root → `pilot-rt.csv`); Playwright `$S/probe556.py` → `$S/shots556/{bar,after}-{dark,light}.png`: 16 checks green in both themes (hover-only boxes → visible once one is checked, shift-click 3 selected without changing the pane, the zip download opened with three members under `Methods/`, Tag → three protocol dots with the selection kept, Move to Data → paths updated, Esc clears, ⌘A → "4 selected", the Data folder zip relative with `Pilot/pilot-rt.csv`, Delete 3 with the confirm naming the count, the root file untouched, the bar gone). ruff, tsc, `make js`, spectacular, migrations check, full suite green. Desktop runs 278 + 279 (#555) green.
- **Files — slice 3: compare a version with now (2026-09-16, #555, backlog 352, 158 tools).** `documents/history.py::version_diff` (unified line diff, two lines of context, `same` / `too_large` / `is_text` states) + `table_diff` for `.csv` / `.tsv` (rows aligned by content with a sequence matcher, rows in a replaced block paired by shared cells, columns by header; changes `{row, column, then, now}`, rows / columns added and removed, caps 5000 rows · 500 changes with `truncated`); `GET /documents/{id}/versions/{n}/diff/`; MCP folded `read_project_file(version, diff=True)`. UI: a Compare / Hide toggle per text version row in the History panel opening a block with the summary line ("v13 → v14 (now) · 1 cell changed · 1 row removed"), the cell table (then struck red, now green) and the coloured line diff (shown alone for text, for a table only when rows / columns changed). Evidence: `documents/tests/test_version_diff.py` (9: a changed cell next to a removed middle row, an added column, TSV + caps, text lines + same, CSV table vs binary, too-large, the endpoint 200 / 404 / 404 / 401, MCP client paths, UI needles + chunk); curl (v2 → +1 row, v12 → +2 rows, a write-file markdown +2 −2 with the exact lines); Playwright `$S/probe555.py` → `$S/shots555/{csv,text}-{dark,light}.png`: 9 checks green in both themes (the seeded CSV after a cell edit + row drop: summary, one cell row `2 rt_ms 412 415`, `-2,low,398` in the line diff, Hide closes; a markdown file: "+2 −2 lines" with the edited lines, no cell table). ruff, tsc, `make js`, spectacular, migrations check, full suite green. Desktop runs 276 + 277 (#554) green.
- **Files — slice 2: tags and the description in the explorer (2026-09-16, #554, backlog 353, 158 tools).** Tree rows carry `description` + `tags` (name, colour; one prefetch); the pane shows the description (Edit / + Add a description via the multiline prompt) and tag chips (remove ×, "+ Tag" menu of the project's other tags + "New tag…" which reuses a same-name tag any-case and colours a new one from its name); one dot per tag on rows; a filter row of the tags in use with counts narrows the tree (folders keep matching descendants and open; "2 of 9"); ⌘P matches tag names. API: `tag_names` read-only next to `tags` ids, another project's tags refused (400), `GET /documents/?tag=` documented, `select_related("project")` closes a per-row slug query on the documents list. MCP folded: `list_documents(project, tag="")`. Evidence: `documents/tests/test_files_tags.py` (8: tree rows + flat query count, PATCH ids → names + foreign tags refused on PATCH and POST, `?tag=` filter, list flat query count, MCP client params + signature, UI needles + chunk), `test_workspace_tree.py` key set; curl end to end (tree rows with tags, `?tag=key-paper` → 1, foreign tag → 400 with its name, own tag + description → 200); Playwright `$S/probe554.py` → `$S/shots554/{filter,pane}-{dark,light}.png`: 18 checks green in both themes (chips figure 1 / key-paper 1 / protocol 2, the protocol filter leaves two files in an opened Methods with "2 of 9" and no Figures folder, clearing collapses back to five root folders, the pane's description + chip, + Tag lists figure / protocol / New tag…, add → two dots on the row, a new `probe554-tag` with a colour, removes, a two-line description saved with the tags intact, ⌘P "keypap" finds the key-paper file). ruff, tsc, `make js`, spectacular, migrations check, full suite green. Desktop runs 274 + 275 (#553) green.
- **Files — slice 1: file history (2026-09-16, #553, 158 tools).** Found first: a same-name upload made silent duplicates (three nodes, one path), and every overwrite lost the old bytes. Now `Document.version` + `DocumentVersion` (documents 0004), `documents/history.py` (snapshot before every overwrite, 20 kept, `replace_file` / `replace_content` (unchanged text files nothing) / `restore` (files the current state first) / rows), wired into the editor's save, `write-file` and `upload-file` (`on_conflict=keep|replace`: numbered name or a new version, never a duplicate); `POST /documents/{id}/replace/`, `GET …/versions/`, `GET …/versions/{n}/raw/` (attachment, ETag), `POST …/versions/{n}/restore/`, `GET …/content/?version=n`; tree rows carry `version` / `versions`; manuscript sources 403. MCP folded (158): `write_project_file(note)`, `read_project_file(version)`. UI: version chip on rows, "v3 (2 earlier)" line, Replace… with a note prompt, a History panel with Download / Restore, menu items, and a Replace / Keep both confirm on same-name uploads. Seed: `Data/Pilot/pilot-rt.csv` with two versions and a note. Evidence: `documents/tests/test_history.py` (13: service replace / unchanged no-op / inline text / restore files first / the 20 cap / manuscript refused / cross-file refused; API keep-vs-replace + junk 400, replace → versions → content?version → raw filename + ETag → restore → 404s → read-only version, editor + write-file versions + tree counts, manuscript 403; MCP client params; UI wiring + chunk); curl end to end (v1 → replace → v3 → restore v1 → v4 → Claude's write → v5, four versions listed, `h-2.csv` for the keep case, 403 for a manuscript source); Playwright `$S/probe553.py` → `$S/shots553/{history,replaced}-{dark,light}.png` (chip, pane line, panel with the seeded note, Replace with a note → v3 and the new bytes in the preview, Restore v1 → v4 with the old bytes, a same-name upload asks and Keep both makes `pilot-rt-2.csv`); spectacular, migrations check, tsc, `make js`, full suite green. Desktop runs 271–273 (#552) green.
- **The shell — the rail as a drawer below 640 px (2026-09-16, #552, backlog 312, 158 tools).** Below 640 px the sidebar slides in as a drawer behind a top bar (hamburger, wordmark, ⌘K); closed on navigation / Escape / a tap outside / widening, `inert` while closed; one `matchMedia` hook reads the CSS's `sm` query verbatim in rem (a px query would drift under a larger default font); the rail carries no transform from 640 up (`max-sm:` variants only) and the achievement toast moved out of the aside for that reason; the content column drops the rail margin and gains the bar's height (388 px of column at 420, was 114); the terminal dock follows. Evidence: `core/tests/test_shell_drawer.py` (the hook and query, the `max-sm:` translate pair, the bar / toggle / backdrop wiring, close-on-navigate / widen / Escape, `inert`, focus return, the main column classes, the toast outside the aside, the dock, the viewport meta, the built CSS `not all and (min-width:40rem)` and the chunk), Playwright `$S/probe552.py` → `$S/shots552/{today,drawer,dashboard}-420-{dark,light}.png`: at 420 (mobile emulation) every check green in both themes; at 640 and 1440 no bar, no transform, main at 240; the route sweep at 420 named the Projects index (436–441 px) and a project overview (608 px) as backlog 349 / 350, the hero caption overlap as 351. tsc, `make css`, `make js`, full suite green. Desktop runs 269 + 270 (#551) green. **Pointer → Files from #553.**
- **Today, second pass — slice 5 + verdict (2026-09-16, #551, 158 tools).** The narrow-width pass: the page root is a `@container` and every row's chip group (age · repeat · due · time · project) is one wrap-aware group that sits inline from a 32-rem column and drops under the text below it (indented by the tick), the text keeps a 13-rem floor from that column up (below it the text shrinks so the buttons share its line), the action buttons are one group placed before the chips so a tight desktop row drops the chips and keeps the buttons on the text line (on desktop the chips now sit flush right with the hover controls between text and chips); the snooze menu is bounded by its row; the panel's `overflow-hidden` is gone (rows round their own corners, sections stack `z-[3]/[2]/[1]` over `rise`'s transform) so the last row's menu is no longer clipped; every hover-only control shows on a coarse pointer; section headers wrap. **The Today area verdict** (best-in-field for a single-user tool; the misses checked against the code and parked as backlog 346–348, 344, 345, 312) is in DECISIONS; the cycle pointer goes to **backlog 312 for #552** (the shell's rail, deferred by four verdicts), then Files from #553. Evidence: `core/tests/test_today_narrow.py` (container + group needles per row kind, the actions-before-chips order, the menu bound, eight coarse-pointer controls, the un-clipped panel + z-order, the built CSS carries `@container (min-width:32rem)` and `(pointer:coarse)`), Playwright `$S/probe551.py` → `$S/shots551/today-{640,768,1440}-*.png`: 640 px text 0 → 242 px, overflow list empty, menu inside the row (was 30 px off-screen); 1440 px long-row text 179 → 500 px with the chips under it; the last row's menu visible at every width (was clipped); mobile emulation → snooze button and drag handle at opacity 1; tsc, `make css`, `make js`, full suite green. Desktop runs 267 + 268 (#550) green.
- **Today, second pass — slice 4 (2026-09-16, #550, 158 tools).** The Done section as a logbook: `core/todos.done_today_q` / `logbook_q` (done_at's local day; unstamped done rows count as earlier), `POST /todos/clear-done/` keeps "all" as its default and takes `{scope: "earlier"}` (junk → 400, schema); the Today page shows **Done today · n** and a collapsed **Logbook · n · since …** grouped newest day first (open state remembered per browser), a "n done today" header count, Clear moved to the Logbook header (earlier days only), and a locked tick on repeating Logbook rows (unticking would take back the spawned occurrence); seed one tick today + one yesterday. Evidence: `core/tests/test_logbook.py` (boundaries partition the table, clear all / earlier keeps today / junk 400, UI wiring incl. the hook-before-return and the built chunk), Playwright `$S/shots550/logbook{,-cleared}-{dark,light}.png` — collapsed with count and "since yesterday", expand → Yesterday group, open state survives a reload, untick from the Logbook returns the row to today, an unstamped row lands under "earlier", Clear empties the Logbook and keeps today's ticks; tsc, `make js`, spectacular, migrations check, full suite green. Desktop runs 265 + 266 (#549) green.
- **Today, second pass — slice 3 (2026-09-16, #549, 158 tools).** Undo for the three actions that vanish a row: delete (re-created with every field, done rows as done through POST — no second spawn — and put back at its old index among today's rows), tick (untick; the toast names where the next occurrence went), snooze (the exact previous day restored by PATCH); `z` fires the toast on screen (`undoLast()` in `components/UndoToast.tsx`), harmless with none; the dashboard hero tick gets the same toast; Clear done stays explicit; footer hint. No backend change. Evidence: `core/tests/test_today_undo.py` (toast export, the three `showUndo` sites, the re-create body, the index splice, the exact-day restore, `z` before the empty-list guard, the hero toast, the built chunk), Playwright `$S/shots549/undo-{delete,snooze}-{dark,light}.png` — `x` → toast → Undo → back at index 2; space → `z` → unticked and toast gone; `s` → Later → Undo → back on today with no chip; `z` with no toast is a no-op; tsc, `make js`, spectacular, migrations check, full suite green. Desktop run 264 (#548) green; the docs-only follow-up triggered no build, as intended.
- **AUDIT #33 (2026-09-16, #548).** pip-audit clean, npm audit clean, `scripts/audit.sh` green; every surface since #538 probed (feed mute lists: counts, lengths, types, `(a+)+$` stored as text — `re.escape`; the PDF sweep: id caps, `days` clamped, junk → 400; Today: snooze / `due` / `repeat` / `when` junk → 400 or ignored, `repeat_of` read-only, the tick loop idempotent by construction; frame ancestors: DENY when unset, the parser drops every non-origin; toolset parsing: junk → core) — **no finding in the window's slices**; backlog 340 / 341 / 342 closed: the release workflow fires on `**/*.py` (tests excluded) + `uv.lock`, `oa._download` follows ≤ 5 hops by hand through `check_url` on https with a streamed body cap, `find_pdf` runs each paper under a 45 s wall clock. Evidence: `test_desktop_scaffold.py::test_release_workflow_rebuilds_on_any_python_change`, `test_oa.py::TestHopGuard` (six tests), `$S/audit33_timing.py` (todos 7 ms / 4 q, dashboard 91 ms / 64 q, brief 77 ms / 56 q, feeds 9 ms, sweep status 7 ms), AUDITS.md report; ruff, full suite green. Desktop runs 262 + 263 (#547) green. Next audit at #558.
- **Today, second pass — slice 2 (2026-09-16, #547, 158 tools).** Repeating items: `TodoItem.repeat` + `repeat_of` (core 0015); `core/todos.py` `advance` / `next_due` / `spawn_next` / `unspawn` / `repeat_label`; `mark(done, was=)` spawns the next occurrence on tick (once; one open occurrence per chain) and takes it back on untick while untouched; `clear-done` keeps successors; serializer `repeat`, `repeat_label`, `repeat_of`, `next` on the tick response, weekly/monthly with no day anchored today; `dueTime.ts` reads "every day / weekday / week / month / Monday" + daily / weekly / monthly before the day phrases; Today rows carry a ↻ chip, the menu a Repeat row, ⌘K sends `repeat`; MCP `add_todo(repeat)` + `complete_todo` → `next`; seed the Monday agenda. Evidence: `core/tests/test_repeat.py` (advance rules incl. Fri→Mon and month clamp, next_due from own day past today, timed keeps time, snooze moves the chain, spawn once / untick takes back / edited stays / plain never spawns / clear-done keeps, labels, API create-tick-next-untick-404, weekly anchored today, patch repeat, MCP client, UI wiring), node check +7 cases, descriptions budget (core trimmed to fit); curl: create weekly → tick → `next` → untick → successor gone; Playwright `$S/shots547/repeat{,-menu}-{dark,light}.png` — "Water the plants every day" → ↻ every day, tick → Tomorrow in Later, menu Repeat → Weekly → "every Wednesday"; ruff, tsc, `make css`, `make js`, spectacular, migrations check, full suite green. Desktop runs 260 + 261 (#546) green.
- **Today, second pass — slice 1 (2026-09-16, #546, 158 tools).** The Today list has a **Later**: `TodoItem.all_day` (core 0014), `core/todos.py` (`today_q` / `later_q` / `open_today` / `open_later` — one boundary for the page, the API, the dashboard and the brief; `snooze(item, until)` on the inbox's vocabulary, timed items keep their clock time, `""` wakes), `GET /todos/?when=today|later`, `POST /todos/{id}/snooze/`, a write-only `due` on the serializer; `dueTime.ts` reads bare "tomorrow", "on / by / next Friday", a closing weekday, "next week", "in 3 days" (all-day at local noon) and day + time; the Today page gets a quiet Later section grouped by day with Today / another-day / delete, a snooze menu on today's rows, `s` → tomorrow, `S` → the menu; the nudge skips all-day items; ⌘K `todo:` and the dashboard hero chip are all-day aware; capture → todo writes the same shape; MCP `list_todos(when)`, `add_todo(due)`, `snooze_todo` (inbox toolset); seed two Later items; README + atlas-daily skill. Evidence: `core/tests/test_later.py` (boundary, snooze keep-time / all-day / wake / rejects, `?when=`, the action, create with `due`, dashboard + brief count today only, capture bare date → all-day, MCP client, tool + toolset, UI wiring), `test_due_times.py` node check extended (nine day cases, `isLater`, `dayLabel`, `formatDue` all-day, nudge skip), `test_when.py` updated; curl: `due=monday` → noon all-day, snooze → wake → 400 on junk, dashboard `todos_open` 3 with two waiting; Playwright `$S/shots546/{today,menu,today-after}-{dark,light}.png` — typed "Review Sam's draft on Friday" landed under Friday, `s` moved the first row to Tomorrow, the sun button brought it back; ruff, tsc, `make css`, `make js`, spectacular, migrations check, full suite green.
- **Library return pass — slice 15 + verdict (2026-09-16, #545, 157 tools).** Two small fixes: backlog 332 — `citing.alerts()` orders `published_on` desc with `nulls_last` (+ `-created_at`, `pk`) so undated citing works sort last on Postgres and SQLite alike; backlog 324 — `preprints.parse_journal_ref` (venue / volume / issue / pages / year from an arXiv `journal_ref` line), `lookup_arxiv` keeps the revision (`v6`), `_store` writes `extra.arxiv_version` + `extra.published_ref`, the offline `upgrade` uses the parsed venue, year, volume and pages, `_row` carries `arxiv_version`, the Library banner says "matches arXiv v6"; the `.bib` export now emits `volume` / `number` / `pages` from `extra` (it never had, for Crossref papers either — found in the post-ship review). **The Library area verdict** (best-in-field for a single-user tool; the misses and where each is parked) is in DECISIONS; the cycle pointer moves to the second pass over the earliest areas, **Today** first, from #546. Evidence: `test_citing.py::test_alerts_put_undated_works_last_on_every_database`, `test_preprints.py::TestJournalRef` (six parse cases, version + parts stored, offline upgrade), tsc, `make js`, spectacular, full suite green. Desktop runs 256 + 257 (#544) green.

- **Library return pass — slice 14 (2026-09-15, #544, 157 tools).** The library fills its own PDFs: `Reference.pdf_checked_at` + `pdf_source` (literature 0017); `literature/oa.py` `find_pdf` → {outcome, attached, source, answered} (stamps on answer or attach; offline stamps nothing), `missing_pdfs` / `stale_missing` (never looked at first, then > 30 d; DOI or arXiv id), `find_pdfs` (wall-clock budget + three-in-a-row offline breaker; `stopped` offline / budget), `sweep_missing`, `watch_status` {missing, lookable, unchecked, found_30d, last_checked_at}; huey 05:20 + desktop tick (10 papers / 120 s) + `manage.py find_pdfs`, all gated on `ATLAS_AUTO_FETCH_PDF` (`.env.example` documents it); `GET/POST /references/find-pdfs/` (status; ids ≤ 20; stale ≤ 20; 120 s); serializer rows carry the stamps; MCP `fetch_pdf(reference_id=0, reference_ids, days, limit)` folds the sweep in (count stays 157); Library rail "Find PDFs now" + status line under Files, toast by source; seed_demo stamps the demo PDF as found via arXiv. Evidence: `literature/tests/test_pdf_sweep.py` (12 tests: stamps, selection order + status, sources + offline breaker, budget + skips, command, gate, API, wired rail, env, model); **live through the proxy: the ResNet DOI with no arXiv id → `POST /references/find-pdfs/ {ids}` → attached via arXiv, id learned, row stamped; `{stale, limit 3}` over demo DOIs → not found, stamped, 0 errors**; Playwright `$S/shots544/rail-{dark,light}.png`; spectacular, tsc, `make js`, migrations check, full suite green. Desktop runs 254 + 255 (#543) green.

- **Library return pass — slice 13 (2026-09-15, #543, 157 tools).** Feed mute lists (backlog 327): `Feed.mute` + `muted_total`, `FeedItem.muted_at` + `muted_by` (literature 0016); `literature/feeds.py` `clean_mute` / `mute_matcher` (word on boundaries, phrase substring, `author:Name`; compiled once per fetch) / `apply_mute` (both ways; dismissed and in-library never touched); `_store_entry` stores a matching new or open entry muted; `fetch_feed` / `refresh` report `muted` and bump `muted_total`; `open_items` excludes muted (dashboard watches, brief, MCP follow); `muted_items`, `status().muted`, `feeds_with_counts` `muted_count`, `feed_row` mute / muted / muted_total, `_row` muted_at / muted_by, `items(muted=)`. API: `FeedSerializer.mute` (ListField 2–60 chars, ≤ 50, cleaned) + `muted` + `muted_total`, `FeedViewSet.update` applies the list and answers fresh, `GET /feeds/items/?muted=1`. MCP `update_feed(feed_id, title, project, mute)` (client + server + library toolset + literature skill). Library: Mute row under a feed's header (chips ×, Enter input, hidden-so-far), "muted (n)" toggle, muted rows say the term and carry no Add/Dismiss, `&muted=1` address, refresh toast counts muted. seed_demo: the arXiv feed mutes "benchmark" + `author:Doe` and one entry is hidden. Evidence: `literature/tests/test_feeds.py::TestMute` (cleaner + matcher, fetch-time muting + counts + idempotent refetch, apply_mute both ways + precedence, API round trip + validation), `mcp_server/tests/test_client.py` update_feed, `test_ui_is_wired` needles; Playwright `$S/shots543/feeds-{dark,light}.png` + `feeds-muted-dark.png` (chips, add a term → hidden count rises and the row leaves, muted view lists it with the term, remove → it returns); spectacular, tsc, `make js`, migrations check, full suite green. README 154/156 → 157 (line 12 had been stale since #540). Desktop runs 252 + 253 (#542) green; run 254 builds this.

- **Library return pass — slice 12 (2026-09-15, #542, 156 tools).** Find PDF walks four sources: `literature/oa.py` rewritten as a lazy generator over arXiv → Unpaywall (every location, best first) → Semantic Scholar (its open-access PDF + `externalIds.ArXiv`, stored as the paper's `arxiv_id` when it had none, so the arXiv PDF is tried next — backlog 323 done) → OpenAlex (`pdf_url` of every location + an arXiv id off an arXiv landing page); a later source is asked only when the earlier ones gave nothing that downloaded; at most four downloads; https only; the outcome names the source and `extra` keeps `oa_source` / `oa_tried` / `oa_checked_at`; `POST /references/{id}/fetch-pdf/` returns `source` + `arxiv_id` (schema); MCP `fetch_pdf` docstring; Library detail pane shows "via arXiv" next to Read and the Find PDF tooltip names the four sources; README line. Evidence: `literature/tests/test_oa.py::TestFourSources` (second Unpaywall location + S2 never asked, S2 fills the arXiv id and the arXiv PDF wins with the API key header, S2's own PDF, OpenAlex last resort + landing-page arXiv id, a known arXiv id never overwritten, four-download cap, API `source`/`arxiv_id`), the rewritten non-https test, `test_reading.py::test_fetch_pdf_api`; **live through the proxy: the ResNet DOI `10.1109/CVPR.2016.90` with no arXiv id → Unpaywall nothing → Semantic Scholar `1512.03385` → arXiv PDF attached (800 KB) in 2.5 s, `source: arxiv`**; Playwright `$S/shots542/library-{dark,light}.png` (the chip); spectacular, tsc, `make js`, migrations check, full suite green. Desktop runs 250 (#541) and 251 green. Backlog 341 (download redirect hop) for Audit #33.

- **MCP descriptions Claude can pick from (2026-09-15, #541, 156 tools, 25 loaded by default).** Backlog 339, the follow-on to the owner's "simpler for Claude": the descriptions are what Claude reads on every turn. Twenty-six docstrings rewritten by hand (every core tool + the nine over 700 chars: `browse_library` 1544→715, `get_dashboard` 1397→360, `get_project_overview`, `get_plan`, `check_retractions`, `check_preprints`, `get_new_citations`, `lint_manuscript`, `submit_manuscript`, `preflight_manuscript`, `get_feed_items`, `check_citations`…) with a "Use for / when / before" cue; slice numbers (`#527`…) stripped from every docstring mechanically (`$S/rewrite541.py`); three long headlines split. Core description chars 8 997 → 4 952 (≈3.6k → 2.6k tokens with schemas); all 156: 46 202 → 37 892; none over 800; no `#nnn` left. Evidence: `mcp_server/tests/test_descriptions.py` (headline ≤ 110 chars and capitalised, no slice numbers, description ≤ 800, core ≤ 720 each and ≤ 5 200 total, the nine ambiguous core names carry a use-cue); README sentence; full suite green. Desktop run 249 (#540) green.
- **Owner ask — MCP toolsets (2026-09-15, #540, 156 tools, 25 loaded by default).** Owner: "154 tools is too much — it should be simpler for Claude." `mcp_server/toolsets.py`: nine areas (plan 24, library 46, notes 18, writing 17, studio 15, inbox 15, research 7, files 4, ops 10; every tool in exactly one) + a 25-tool **core** cross-cut; `parse` / `unknown` / `resolve` / `describe` / `from_env`. `server.py`: `FastMCP("atlas", instructions=…)` states the contract at initialize; `list_toolsets` (loaded / total / rows with use-when, tools, enabled, loaded) and `enable_toolset(name, ctx)` (async; public `add_tool` from `_REGISTRY`, then `ctx.session.send_tool_list_changed()`; unknown name → `ok: false` with the list); `apply_toolsets(spec)` prunes with `remove_tool` from `main()` only (unknown names → stderr, never stdout; nothing left → core); `self_check` adds `tools_total` + `toolsets`; `_serve()` runs the stdio loop with `NotificationOptions(tools_changed=True)` because FastMCP's `run()` announces `listChanged: false`. Connect page: a Toolsets block (chips with count, "on by default", use-when + tool list on hover; `connection_info` carries `toolsets` + `tools_total`). README: "156 tools", a Toolsets paragraph under the add line (`--env ATLAS_MCP_TOOLSETS=core,library` / `all`), the two meta tools listed; every skill names its toolset(s) and says to call `enable_toolset` first. Evidence: `mcp_server/tests/test_toolsets.py` (27 with test_check: exactly-one-area guard, core ≤ 26 and real, parse table, unknown → stderr, prune/load round trip idempotent, `list_toolsets` state, `enable_toolset` adds + notifies once, `self_check` keys, and two **SDK stdio-client** runs of the real server: `initialize` says `tools.listChanged: true` and carries the instructions, `list_tools` == core (25), `enable_toolset("library")` → +42 with no duplicates and a `ToolListChangedNotification` received; `ATLAS_MCP_TOOLSETS=all` → 156); the two tool-name regexes (`test_docs`, `test_skills`) now count `async def` tools; `conftest` restores the registry after every test; full suite green; tsc + `make js` (Connect chunk). Backlog 337–339 (merges + "use when" lines). Desktop run 248 (#539) green.
- **Owner ask — Atlas as a tab inside OpenManus (2026-09-15, #539, 154 tools).** Owner: "add the whole project to this as a tab in it" (OpenManus) → "we need to start to do that as well!" Shipped the Atlas half: `ATLAS_FRAME_ANCESTORS` (env, empty by default) + `core/framing.py` (`parse_ancestors` — whole http(s) origins only, lower-cased, de-duplicated, capped at 20, junk dropped; `FrameAncestorsMiddleware` — after `SecurityMiddleware` in `MIDDLEWARE`, replaces `X-Frame-Options: DENY` with `Content-Security-Policy: frame-ancestors 'self' <origins>` when the list is set, leaves exempt responses and existing CSPs alone); `frame_ancestors` in the diagnostics report + text line ("embeddable from: …"), an "Embeddable from" row on the Diagnostics page (`data-testid=frame-ancestors`), `get_diagnostics` docstring; `.env.example`; README "Atlas as a tab in another app"; `docs/integrations/openmanus.md` — the OpenManus half as a patch against its verbatim `app.tsx` / `docker-compose.yml` (a `pages/AtlasPage.tsx` iframe page, a `/atlas` route, an "Atlas" sidebar button after Home, `VITE_ATLAS_URL` under the frontend service, Atlas pinned to `ATLAS_PORT=8001`, same-hostname rule, troubleshooting, the reverse direction and the MCP route) — that repository is outside this session's reach, so the patch is documented, not run. Evidence: `core/tests/test_framing.py` (parse table incl. IPv6 / junk / cap, default DENY on page + login + API, listed origins → exact CSP on page/API/diagnostics/login, exempt + existing-CSP untouched, middleware-order guard, diagnostics API + text); live probe `$S/probe539.py` with a stand-in host page on `127.0.0.1:3000` (allowed: header on /login/, login inside the frame, `POST /api/v1/quick-capture/` from inside the frame → 201 with the CSRF cookie, Diagnostics row reads "http://127.0.0.1:3000 — Atlas can be shown as a tab there"; denied: `X-Frame-Options: DENY`, frame lands on `chrome-error://`); screenshots `$S/shots539/`; full suite green; tsc + `make js` (Diagnostics chunk); schema `--fail-on-warn` clean; no migration. Backlog 336 (the reverse direction as an Atlas "Apps" page).
- **Audit #32 (2026-09-15, #538).** Dependencies clean (pip-audit, npm audit --omit=dev), `scripts/audit.sh` green, fourteen new routes 401 anonymously, feed SSRF guard verified on eight hostile addresses and on redirects, bounds on every id / limit / days / hours, the SQLite repeat of every id and range probe clean. Three findings fixed and pinned: a 10 000-character path was a 500 on `projects/import-folder/` and on `backup-destination/` (`MAX_PATH` 4 096 + an OSError-guarded `is_dir` in `resolve_root` / `save_config`), and `only: [1, null]` was a 500 on the import (text coercion in `plan` and the view). Query counts and latency recorded (facets 22 q / 56 ms, dashboard 64 q / 94 ms, the rest ≤ 30 ms). Report in AUDITS.md. Next audit at #548.
- **Library (return pass) — the softer notices (2026-09-15, #537, 154 tools).** Backlog #321: Crossref's expressions of concern and corrections (corrigenda, errata, addenda, clarifications) are stored on the paper as `Reference.notices` (literature 0015) by the same check that stores the retraction verdict (`lookup_all` — one request, one stamp; a clean answer clears, an error leaves alone). Shipped `?notices=1` + facet (SQLite-safe key transform), the serializer field, `noticed` rows + `status.noticed` on check-retractions, MCP `browse_library(notices)` + row `notices`, the pre-flight **Notices** warn row, the Library's amber concern / corrected chips, "With notices" rail row, detail-pane banner with the notice DOIs, the Reference page banner, two seeded demo papers. Evidence: `literature/tests/test_retractions.py` (lookup_all split, store/clear/keep, filter + facet, API read-only + filter, pre-flight warn never fail, UI needles), `mcp_server/tests/test_client.py`; curl: `?notices=1` → 2, facet notices 2, status noticed 2, pre-flight Notices row ok on the demo manuscript; MCP client browse_library(notices) rows carry notices; Playwright: rail row 2 → /library?notices=true, 2 amber chips (concern, corrected) in list and cards, banner with the notice DOI, reload keeps the filter, Reference page banner, dark and light, no page errors.
- **Owner ask — a backup destination on a drive or cloud folder (2026-09-15, #536, 154 tools).** Owner: "it would be nice if we can have a drive or whatever cloud that can be attached and the backup would go there!" Shipped `core/destination.py` (describe / suggestions / save_config / copy_snapshot verified + atomic + rotated to 14 / mirror never-raising / sync_now / destination_status with live reachability), hooked into `take_snapshot` (result carries `copied`), `GET/POST /api/v1/backup-destination/` + `POST …/sync/`, MCP `get_backup_destination` + `set_backup_destination`, `manage.py snapshot --to DIR`, Diagnostics › Backup destination section (suggestion chips found on the machine, path input, desktop Choose…, attached view with kind chip / Copy newest now / Show in folder / Detach / newest copy / kept + free space / last failure), diagnostics text line, README Backups paragraph, daily skill hint. Evidence: `core/tests/test_destination.py` (kinds, suggestions, validation, copy + verify + rotate + report, failed copy never fails the snapshot + unplugged drive, sync_now once, API attach/status/sync/detach/400, `snapshot --to`, UI needles), `mcp_server/tests/test_client.py::test_backup_destination_calls`; curl: attach → snapshot → copies 1 · in_sync true · sync "already there"; Playwright attached a fake "Google Drive/My Drive" folder from the Diagnostics form (GOOGLE DRIVE chip, 9.2 MB copy, "the newest snapshot is there", 22.3 GB free) in dark and light, detached through the in-app confirm; no page errors.
- **Owner ask — import a folder of projects (2026-09-15, #535, 152 tools).** Owner: "we should have a skill so I have project folder that would basically want to add all the old projects real quick." Shipped `projects/importer.py` (plan without DB/network; import one folder: README → description, Markdown → notes with links/tags synced, PDFs → library + project link, .bib/.ris → files + library, rest → documents in the same structure; safe names, junk/size/depth/count guards, idempotent markers), `POST /api/v1/projects/import-folder/`, MCP `import_projects_folder`, `manage.py import_projects`, the `/atlas-import-projects` skill (five skills now; the frozen server finally bundles `mcp_server/skills`), Projects → **Import a folder…** page (desktop picker, preview table, per-folder sequential import with live results) + ⌘K verb. Evidence: `projects/tests/test_importer.py` (plan, names, full import + second run adds nothing and keeps edits, options, only/bad paths, API dry-run/apply/400, command, spec pin, UI needles), `mcp_server/tests/test_client.py::test_import_projects_folder_posts_the_folder_list`, `core/tests/test_skills.py` (five skills); curl dry run over the demo folder; Playwright drove Projects → Import a folder… → Preview → Import (2 projects created: 1 file · 2 notes · 1 paper without metadata; 2 files) in dark, and the re-run preview in light showing "already in Atlas · adds what is missing"; the imported project's tree/notes/description checked over the API; no page errors.
- **Owner ask — the update link (2026-09-15, #534, 151 tools).** Owner: "still the update link doesn't work; the repo is public now." Verified from here: `latest.json` (0.1.242) answers 200 on the Whorl and the redirected project-manager endpoints, the `.sig` key id equals the bundled pubkey's (`71f2b5f3b358e8dc`), the plugin picks the installer-specific platform key. Shipped: `core/diagnostics.py` `_probe_feed` (GET, 256 KB cap, version, platforms, key match) + `update_verdict` (available / current / unknown_version / wrong_key / unsigned / unreachable / offline / unchecked), `update_verdict` on `/api/v1/diagnostics/` + the text form, an **Update check** row on Diagnostics, `manage.py doctor` reuses it; every release link → `AliZareh-CoE/Whorl` (sidebar "get it manually", README badges + download, Tauri homepage, issue template); the updater's 404 wording; README + desktop README auto-update sections rewritten for a public repository. Live probe from the build machine with ATLAS_VERSION=0.1.221: "0.1.242 is available and signed for this app (you run 0.1.221)"; with 0.1.242: "up to date"; with dev: "cannot compare". Tests: core/tests/test_diagnostics.py (nine verdict cases incl. the second-endpoint fallthrough, rows, key id + version parsing, the runaway-body cap, the API + UI needles, doctor), core/tests/test_desktop_scaffold.py (the Whorl links, the new wording). Ship review caught that the frozen server did not carry `desktop/tauri.conf.json` (the verdict would read "not checked" on an installed app): the spec now bundles it and `UPDATER_FALLBACK` covers older bundles, pinned to the conf by test. **What I cannot see from here is the owner's installed app** — if the sidebar still says "Updates unavailable", the Diagnostics Update check row (with the probe ticked) now says exactly why; paste it.
- **Library (return pass) — slice 10 (2026-09-15, #533, 151 tools).** The watches on the dashboard and in the brief: `core/dashboard.py::watches_everywhere` (from the stored rows — the three newest open feed entries deduped across feeds, undated last, with the open count and the feeds' health; the three newest open citing works with the papers they cite; the modes' addresses), `watches` on `/api/v1/dashboard/` + MCP `get_dashboard`, a "## From your watches" section in the daily brief + `watches` in its summary, a **Watches** block on the dashboard (feed rows → that feed's mode, citation rows → the watch narrowed to the cited paper, "All feeds → · All new citations →", an amber "n feeds failed to fetch", an empty state that offers to follow a feed). Dashboard budget pin 116 → 122 (six fixed queries). Backlog #328 struck; #331 added (a project-scoped glance). Tests: core/tests/test_dashboard_v2.py::test_watches_everywhere_lists_feed_entries_and_new_citations (empty shape, counts incl. a failed feed, dedupe by DOI case-insensitively, dismissed / library-own / seen-citation / in-library rows left out, undated last, limit, the API payload, UI needles), core/tests/test_brief.py::test_daily_brief_carries_the_watches (absent when empty, the exact section, before This month, the summary count, the API). Playwright, both themes: the block reads "3 from feeds · 2 new citations" with five rows on the demo; a feed row lands on `/library?feeds=1&feed=2` (the Nature feed panel), a citation row on `/library?citing=1&reference=1` (the watch narrowed to lavie2010attention); no page errors.
- **Library (return pass) — slice 9 (2026-09-15, #532, 151 tools).** Every Library mode has an address: `/library?feeds=1[&feed=<id>][&seen=1][&fq=<words>]`, `/library?citing=1[&reference=<id>][&seen=1]`, `/library?duplicates=1` — read at mount and on an incoming address (the sidebar's `/library` clears the mode with the filters), written back with the same replace-navigation after the feed filter's debounce, precedence feeds > citing > duplicates, an unknown feed id widens to every feed; **Copy link** in each mode's header; MCP `get_feed_items` / `get_new_citations` / `list_feeds` (answer + every row) carry `url`. Backlog #326 and #329 struck. Tests: literature/tests/test_library_read_flow.py::test_library_address_ui_wiring (the Mode union, modeFromUrl / addressOf, the mode initialisers, applyMode, the dup panel + mode Copy link ids), mcp_server/tests/test_client.py (the three `url`s: bare, narrowed + seen + fq). Playwright, both themes: arrive on `?feeds=1&feed=2` → the Nature feed panel; "one feed ×" → `?feeds=1`; seen → `&seen=1`; typing → `&fq=reward`; back to the list → `/library`; `?citing=1&reference=1&seen=1&tag=load` → Seen citations · one paper with Copy link giving `?tag=load&citing=1&reference=1&seen=1`; `?duplicates=1`; `?citing=1&feeds=1` → feeds; `?feeds=1&feed=999` → `?feeds=1`; rail clicks write `?citing=1` / `?feeds=1&feed=1`; sidebar Library → `/library`; pushState to a mode address opens it; back from a mode leaves the page.
- **Library (return pass) — slice 8 (2026-09-15, #531, 151 tools).** Journal and arXiv feeds inside the Library: `Feed` + `FeedItem` (literature 0014); `literature/feeds.py` (`parse_feed` for RSS 2.0 / Atom / RSS 1.0-RDF by local element names — DOI from `prism:doi` / `dc:identifier` / link / text, arXiv id from link / OAI guid / prefix, authors from `dc:creator` (split on ", ") and Atom `author/name`, the abstract as plain text with arXiv's announce prefix stripped, arXiv `replace` announcements skipped, a DTD / entity declaration and an HTML body refused; `discover_feed_url` for a journal home page; `fetch_feed` streamed to 2 MB with If-None-Match / If-Modified-Since, 304 = unchanged, upsert by guid with an unmoving first-seen stamp, library-own entries linked, prune to 500, failures stored in `last_error` moving only `last_fetched_at`; `refresh` with the breaker, `stale_feeds` never-first then 12 h, `refresh_stale`, `add_feed` behind the inbox's URL guard with nothing persisted on a refusal, `items` with feed / project / q / dismissed and cross-feed dedupe, `add_item` through `add_reference_by_identifier` into the chosen or the feed's project, `dismiss` + undo, `link_reference` from the Reference post_save); sweeps from huey (every 6 h at :20), `manage.py refresh_feeds`, the desktop scheduler thread; `/feeds/` CRUD (POST fetches once, 400 with the reason), `GET|POST /feeds/refresh/`, `POST /feeds/{id}/refresh/`, `GET /feeds/items/`, `POST /feeds/items/add/`, `POST /feeds/items/dismiss/`; MCP `list_feeds` + `add_feed` + `remove_feed` + `refresh_feeds` + `get_feed_items` + `add_feed_item` + `dismiss_feed_items`; UI: the rail's **Feeds** section (All feeds + per-feed unread counts, amber dot on a failed fetch, inline "Follow a feed" form filing under the current project with the refusal shown in place, Refresh feeds now), the Feeds mode (title → paper, authors · feed · date · id, two-line abstract, Add / no id / Dismiss, title-and-abstract filter, seen list + Restore, mark all seen, Refresh now, stop following with a confirm); seed_demo follows q-bio.NC and Nature Human Behaviour with five entries. Backlog #327 (mute list), #328 (dashboard / brief), #329 (an address). Tests: literature/tests/test_feeds.py (the three dialects incl. CDATA / HTML stripping / `doi:` prefix / RFC-2822 dates, replace skipped, DTD + HTML + junk refused, autodiscovery, fetch → link → conditional 304 → changed title taken with the first-seen stamp kept, 503 / offline / HTML / oversize store the reason and keep the entries then a good fetch clears it, prune keeps the library's own, breaker + stale order + a fresh sweep asks nothing, add_feed refusals (private, ftp, plain text, 404) persist nothing + discovery + idempotence, items filters + dedupe + status + counts, add_item default / explicit project + the save hook + no feed query on an unrelated save, API 401 / 400s / 201-200 / list / PATCH / items filters / add 201-400-404 / dismiss + undo + junk / refresh GET-ids-stale-junk-one / DELETE keeps the added paper, the management command + the two sweep hooks, UI needles), mcp_server/tests/test_client.py (list + status, add bodies, DELETE, refresh ids / stale, items params, add, dismiss + undo). Live: all four dialects fetched live at the start of the slice (arXiv RSS 674 KB, arXiv Atom, Nature RDF, Cell RDF) and parsed by the shipped parser (204 / 8 / 8 / 53 entries, every one with an id and a date).
- **Library (return pass) — slice 7 (2026-09-15, #530, 144 tools).** The citation watch: `CitingWork` (literature 0013 — openalex_id unique, doi, title, authors, year, published_on, venue, cited_by_count, `cites` M2M, `reference` once in the library, `dismissed_at`) + `Reference.cited_by_checked_at`; `literature/citing.py` (`watched`, `ensure_openalex_ids` one batched `doi:` request per 50 that raises on a non-200, `lookup_citing` one `cites:W1|W2|…,from_publication_date:` request per 50 papers newest first up to four pages, `_store_work` upsert by OpenAlex id with an unmoving first-seen stamp + links to the batch papers in `referenced_works` (whole batch when none listed) + a library-own work linked to its reference, `check_references` with the year-back first window / 45-day overlap / no stamp on a failed batch / breaker after three, `stale_references` never-first then 7 days, `check_stale`, `watch_status`, `alerts`, `dismiss` + undo, `link_reference` from a Reference post_save so Add by DOI clears the row); sweeps from huey (05:00), `manage.py check_citations`, the desktop scheduler thread; `GET /references/new-citations/` (project / reference / dismissed / limit + status), `GET|POST …/new-citations/check/`, `POST …/new-citations/dismiss/`; `new_citations` facet; `cited_by_checked_at` on rows; MCP `get_new_citations` + `check_citations` + `dismiss_citations`; UI: the rail's sky **New citations** row + "Check citations now", the New citations mode (rows: title → DOI/OpenAlex, authors · venue · year · published · citations, `cites <key>` chips → the cited paper, Add / Dismiss / no DOI, seen list + Restore, mark all seen, Check now, swept date), the detail pane's "n new papers cite this — see them" line, the Reference page's sky section with Seen; seed_demo stores two citing works on the Lavie paper and stamps every demo paper checked. Backlog #325 (`from_created_date` with a key), #326 (an address for the mode). Tests: literature/tests/test_citing.py (watched + open alerts, the `cites:` filter / per-page / sort / select / mailto / pagination until a short page / max pages / 429 raises, DOI resolution in one request + 429 raises, first sweep stores + links (specific, whole-batch fallback, library-own) + stamps, second sweep sees-not-news with the overlap window, changed titles taken, a failed request stamps nothing + the breaker, DOI-only papers resolved first + unknown skipped, stale order + status + check_stale asks nothing when fresh, the feed's filters / limit / dismiss / undo / facet / the add hook, API 401 / ids / junk / stale / GET status / feed filters / 400s / dismiss + undo + junk / facet + row field / by-doi clears the feed, the management command + the two sweep hooks, UI needles), mcp_server/tests/test_client.py (feed params, check bodies, status GET, dismiss + undo). Playwright: rail "New citations 2" → the mode lists both seeded works with "cites lavie2010attention", the chip opens the Lavie detail pane whose line reads "2 new papers cite this — see them" and narrows the feed to "citing lavie2010attention", Dismiss drops the no-DOI row to "New citations 1", "seen (1)" lists it, Restore brings it back, the Reference page section reads "2 new papers cite this one."; both themes, no page errors. Live: OpenAlex metered the egress out for the day before the slice (recorded in DECISIONS; the `cites:` path is exercised through a mock transport), the Add path is live — `POST /references/by-doi/ 10.1038/nature14539` (Crossref) filed `lecun2015deep` and the citing row left the feed through the hook. Desktop run 239 (#529) green.
- **Library (return pass) — slice 6 (2026-09-15, #529, 141 tools).** The preprint watch: `Reference.published_doi / published_venue / published_checked_at` (literature 0012); `literature/preprints.py` (`is_preprint` / `preprint_q` — an arXiv id and no publisher DOI; `lookup_arxiv` one batched request for the author-deposited DOI + journal_ref; `lookup_s2` one Semantic Scholar batch request for the rest, optional `ATLAS_S2_API_KEY`; `check_references` arXiv-then-S2 in chunks of 50 with the three-second arXiv pause and a breaker after three unanswered chunks; the monotonic rule — a found DOI is never cleared by a miss, a miss moves only the stamp, a failed request moves nothing; `stale_references` skips known publications; `upgrade` — published DOI + Crossref/OpenAlex metadata, offline partial, cite key + arXiv id kept, `extra.preprint`, `raw_bibtex` cleared, `UpgradeConflict` on a DOI clash); the sweep from huey (04:40), `manage.py check_preprints`, the desktop scheduler thread; `?preprints=1` / `?published_available=1` + facets + `preprint` + three read-only fields; `GET|POST /references/check-published/`, `POST /references/{id}/upgrade/` (400 / 409 / row + `upgrade.metadata`); the pre-flight's **Preprints** warn row; MCP `check_preprints` + `upgrade_preprint` + `browse_library(preprints, published_available)` + `preprint` / `published` on rows; UI: amber "published version" chip (rows + cards), detail-pane banner with venue / DOI / **Use the published version** / found date, the "Preprint · no published version found" line, the rail's Preprints + Published version rows + "Check preprints now", the Reference page banner + button; seed_demo files `okafor2024load` (arXiv 2401.00001) with its published version on record, outside the manuscript. Backlog #323 (the other direction), #324 (journal_ref parsing). Tests: literature/tests/test_preprints.py (preprint rules agree between Python and the queryset, arXiv Atom parsing incl. arXiv-DOI skip / 429 / non-XML, S2 parsing incl. venue precedence, the batch order and bodies, a miss never clears a found DOI, an offline sweep leaves stamps alone and trips the breaker, stale order + status, filters + facets, upgrade full / offline-partial / refusals + collision, API 401 / ids / junk / stale / list / facet / GET / upgrade 400-200-409, pre-flight warn → ok, UI needles), mcp_server/tests/test_client.py (bodies, browse params, status GET, upgrade path + named DOI, row carries published). Playwright: rail "Preprints 1 · Published version 1" → `/library?published_available=true` with one row and its amber chip, the banner reads "A published version exists · Journal of Cognitive Demonstration … 10.0000/demo.published.2025 … Use the published version · found 9/15/2026", the cards view carries the chip, the Reference page banner, and in the light run **Use the published version** turns the row into doi 10.0000/demo.published.2025 / venue Journal of Cognitive Demonstration with the key `okafor2024load` unchanged, `preprint: false`, `extra.preprint.arxiv_id` 2401.00001 and the rail's Published version count at 0; both themes, no page errors. Live probes (arXiv 429, Semantic Scholar 429, OpenAlex metered) are recorded in DECISIONS; the sources are exercised through mock transports. Desktop run 238 (#527) green.
- **Library (return pass) — slice 5 (2026-09-15, #527, 139 tools).** The retraction watch: `Reference.retraction_kind / retraction_notice / retraction_date / retraction_checked_at` (literature 0011); `literature/retractions.py` (`lookup` over Crossref's updates filter, `check_reference` with the fail-closed rule — errors never clear a verdict or move the stamp, `check_references`, `stale_references` never-checked first, `check_stale` bounded and self-limiting, a breaker after five consecutive errors, `partial_retraction` normalised, `watch_status`); the sweep from a huey periodic task (04:20), `manage.py check_retractions`, and the desktop's snapshot scheduler thread; `?retracted=1` + facet + four read-only row fields; `GET|POST /references/check-retractions/` (ids ≤ 50 or stale ≤ 50; 400 on junk); the bib report reports stored verdicts without asking; the pre-flight's Retractions row always present from stored verdicts (fail names the keys) and unioned with live findings on a network run; MCP `check_retractions` + `browse_library(retracted)` + `retraction` on rows; UI: rose RETRACTED chip (rows + cards), detail-pane banner with the notice link / checked date / re-check, the "No retraction notice · checked …" line on clean papers, the rail's Retracted row + "Check retractions now", the Reference page banner; seed_demo retracts `desimone2004study` (outside the manuscript) and stamps every demo paper checked. Backlog #321 (expressions of concern). Tests: literature/tests/test_retractions.py (lookup parsing incl. junk entries and date parts, non-200 raises, set → clear, offline/500 leave verdict + stamp, no-DOI skipped, summary counts, stale order + limit, sweep asks nothing when fresh, status/filter/facet, API ids/stale/GET/401/400/list/facet/row fields, bib report without a lookup, pre-flight row from stored flags, UI needles for both pages); writing/tests/test_preflight.py adjusted (the row is always present); mcp_server/tests/test_client.py (bodies for ids and stale, retracted param, status GET, row carries retraction). Playwright: rail "Retracted 1" → `/library?retracted=true` with one row and its chip, the banner reads "This paper has been retracted · 2019-06-12 … notice … checked 9/15/2026 · re-check", the cards view carries the chip, a clean paper reads "No retraction notice · checked … · check", the Reference page banner; both themes, no page errors. curl: GET status {retracted 1, unchecked 0, with_doi 4}, anon 401, junk ids 400, stale sweep with nothing stale checks 0. Desktop run 237 (#526) green.
- **Library (return pass) — slice 4 (2026-09-15, #526).** Every Library view has an address: `Library.tsx` reads every workbench filter from the URL on load and writes them back (`navigate` with replace, after the search debounce, default sort and empties dropped — a clean library is a clean `/library`); an incoming address (sidebar, ⌘K, a note, the brief, Claude) resets the filters and the search box and still honours `read=` / `add`; `year_min` / `year_max` join the filters with "from 2020" / "to 2024" chips; **Copy link** next to the export group; MCP `browse_library` returns `url` (`library_url()` shares the address bar's rules). Backlog #320 closed. Tests: literature/tests/test_library_read_flow.py (test_library_address_ui_wiring — viewQuery/fromUrl, the replace navigate, the debounce guard, the location effect, the year-range chips, Copy link), mcp_server/tests/test_client.py (browse_library carries `url`; library_url drops empties / zeros / the default sort, encodes values, no limit). Playwright: arriving on `/library?author=Lavie&year_min=2005&sort=-year` shows the "from 2005" and "by Lavie" chips; removing a chip rewrites the address; typing "atten" leaves the address alone until the debounce settles, then writes `q=atten`; Copy link puts the exact address on the clipboard; the sidebar's Library link clears the address, the chips and the search box; `?q=key&read=1` still opens the reader; both themes, no page errors. Desktop run 236 (#525) green.
- **Library (return pass) — slice 3 (2026-09-15, #525, 138 tools).** Export in every format: `literature/export.py` (`export_ris`, `export_csl_json`, `export_csv`, `csl_item`, `render`) next to `export_bibtex`; `GET /references/export/?fmt=bib|ris|csl|csv` over `ids` or the workbench filters (`fmt`, not `format` — DRF's renderer switch), 400 on an unknown format, four media types in the schema; the SPA's ".bib of this view" and "Export .bib" become an ".bib .ris .json .csv" group with tool-naming tooltips (view + selection); MCP `export_references(fmt, reference_ids, project, q, author, tag, reading_status, year_min, year_max)` with `export_bibtex` kept. Tests: literature/tests/test_export.py (RIS round-trips through parse_ris incl. types, SP/EP, KW, folded abstract; CSL-JSON round-trips through parse_csl_json; CSV columns, BOM, authors/tags/projects/has_pdf; render picks writer + name and refuses unknown; API media types, disposition, ids/filters, default BibTeX, 400, 401; UI wiring), mcp_server/tests/test_client.py (export_references drops empty filters, ids win over filters, export_bibtex delegates). Playwright: the view's export group and the selection's, each of the four links fetched with the right content type / file name; both themes, no page errors. Desktop run 235 (#524) green.
- **Library (return pass) — slice 2 (2026-09-15, #524, 137 tools).** The author lens: `filter_references` takes `author` (family name, case-insensitive, matched in Python over one `(id, authors)` query so Postgres and the desktop's SQLite share one path), `facets.authors` (top 12 by papers: name, most common given, count); the rail's **Authors** block, a byline whose names are buttons that set the filter (highlighted while active; an author outside the top eight is shown first in the block), the "by Lavie" chip; the list endpoint's fifteen filters documented in the schema; MCP `browse_library` (every rail filter, `count` + up to 50 compact rows with `progress`); the #523 follow-up — both reading-flow endpoints carry `progress` and the flow card shows "p. 5 of 12". Backlog #320 (read every filter from the URL). Tests: literature/tests/test_library.py (author filter incl. malformed rows, combined filters and blanks; facet counts papers not bylines and picks the spelling/given deterministically; API filter + facet + flow), test_library_read_flow.py (flow rows carry progress on both endpoints; UI wiring), mcp_server/tests/test_client.py (browse_library drops empty params, trims rows). Playwright: rail "Authors" block; clicking Awh → "1 result" + "by Awh ×"; the byline "A. Awh" toggles it off (22 results); from a Lavie row the byline sets "by Lavie", Lavie (outside the top eight) is the rail's first chip, the byline is highlighted; the flow card reads "p. 5 of 12"; both themes, no page errors. Desktop run 234 (#523) green.
- **Library (return pass) — slice 1 (2026-09-15, #523, 136 tools).** Area: **Library (return pass)** (from #523). Reading progress: `Reference.last_page` / `page_count` / `last_read_at` + `ProjectReference.started_at` / `finished_at` (literature 0010 with a backfill from updated_at); `literature/progress.py` (`record_position`, `progress_of`, `stamp_transition` from `ProjectReference.save()` on every write path incl. `update_fields` saves and the per-row auto-skim, `reading_now`); `GET/POST /references/{id}/progress/`, `GET /references/reading-now/`, `progress` on reference rows and reading-queue rows, `started_at` / `finished_at` on project links; MCP `get_reading_progress` + `set_reading_position` + `get_reading_queue` docs + the atlas-literature skill; the reader restores the remembered page from a fresh fetch, reports the page you settle on (4 s quiet, flushed on unmount), the Library rows / cards carry a slim bar with "p. 5 of 12", the detail pane's "Resume · p. 5 of 12" and "skimmed · started Sep 9", the rail's "Continue reading"; seed_demo's twelve-page PDF (replaced on reseed by size) and the Lavie paper skimmed on p. 5 of 12. Tests: literature/tests/test_progress.py (half-up percent, positions incl. refusals and the started stamp, transitions on update_fields saves, created-as-read, auto-skim, reading_now order + exclusions, API GET/POST/400/401/404 + rows + queue + link serializer + read-only position, TSX needles), mcp client test. Playwright: rail "Continue reading · p. 5 of 12", row "p. 5 of 12", "Resume · p. 5 of 12" opens the reader on "p. 5 / 12", scrolling to page 8 lands in the API after the quiet interval, closing refreshes the row and rail to p. 8, reopening reads "p. 8 / 12"; both themes, no page errors. Ritual note: compose containers lingered again after the container reset — services.sh now recreates them when `up` fails. Desktop run 233 (#522) green.

- **Plan — slice 10 + verdict (2026-09-15, #522).** The narrow-width pass on the Plan cards view: the phase header's window + close nudge + likely-end chip and the milestone row's chips + due date are wrap-aware groups (inline from lg, their own line under the title below it), the page title keeps a floor so Review plan and the view tabs wrap under it, the conflict banner's list takes its own line; from lg both titles keep a 13-rem floor so a full chip row drops under the title (at 1024 px the second phase's title measured 13 px before the floor, 637 after); CSS rebuilt for the new utilities. **Verdict:** the Plan is judged best-in-field for a single-user research tool (DECISIONS lists what a Linear / Asana / MS Project / Notion user would still miss and where each went: backlog #317 cross-project roadmap, #318 roadmap export, #316 phase-date drift); current area → **Library (return pass)** from #523. Tests: core/tests/test_polish.py (test_plan_narrow_width_groups — the two groups, the title floor, the banner list, and the built CSS carrying the utilities when it exists). Playwright: before, the cards view measured 1086 px wide at 640 and 768 px (the review, drawer and roadmap held); after, scrollWidth = clientWidth at 640, 768, 1024, 1152, 1280 and 1440 in both themes, no title under 208 px at any of them, the close flow (nudge → lessons → Close phase → decision log) still runs end to end; no page errors. Desktop: `static/**` is on the release workflow's path filter, so the rebuilt Plan bundle triggers run 233. Desktop run 232 (#521) green.

- **Plan — slice 9 (2026-09-15, #521, 134 tools).** Phase close-out: `plans/closeout.py` (`phase_report` — planned window vs actual end and overrun, counts with on-time landings, median lateness, drift and moves, every milestone's first / held / landed / late / bucket, attached questions, `closable`, paste-ready Markdown; `close_phase` — status done + a "Phase closed: <name>" decision record with the report as context and the lessons as the decision); `closable` on plan phases; `GET /phases/{id}/report/`, `POST /phases/{id}/close/ {lessons}`; MCP `get_phase_report`, `close_phase` + the atlas-plan skill; the Plan page's "all done — close the phase" nudge, the kebab's "Phase report / close…", `PhaseReport.tsx` (facts grid, landing table, questions, lessons box, Close phase, copy as Markdown; a closed phase says where its report lives). Tests: plans/tests/test_closeout.py (report numbers incl. late_first and buckets, closable rules for open / done / empty phases, close files the decision with lessons or the numbers, API GET/POST/400/401/404 + plan flag + decision detail, TSX needles), mcp client tests. Playwright: the demo's literature phase report reads "2/2 done · 1 on time · median 2 d late · planned end 2026-07-17 · actual end 2026-07-17 on the day · 0/1 answered" with two landing rows; reopened over the API it shows the nudge, the lessons box takes a sentence, Close phase toasts "Phase closed — “Literature & hypotheses”. The report is in the decision log.", the pill reads done and the decision log lists it; both themes, no page errors. Desktop run 231 (#520) green.

- **Plan — slice 8 (2026-09-15, #520).** Likely dates where the plan is read: roadmap milestone rows carry `likely` and phase rows `likely_end` (no new queries; `range_end` covers the latest likely end); the Roadmap draws a dotted line from each open diamond forward to a dotted ○ at its likely date, "likely lands …" on the diamond hover and "likely ends … at your pace" on the bar hover, legend note; review queue rows carry `likely` and the review card shows a dashed "likely …" chip; overdue milestones carry no likely mark anywhere; MCP `get_roadmap` / `get_plan_review` docs. Tests: plans/tests/test_calibration.py (roadmap rows + phase end + range, review queue rows, Roadmap/Review TSX needles). Playwright: five likely marks on the demo roadmap (the overdue pilot carries none), hover "Pilot analysed, design frozen · due 2026-10-10 · likely lands 2026-10-15", bar hover "… · likely ends 2026-10-15 at your pace", review card "due in 25 d · likely 2026-10-15"; both themes, no page errors. Ritual note: the container came back with a stale containerd pid file that kept dockerd from starting (`/var/run/docker/containerd` removed, compose containers recreated on their volumes). Desktop run 230 (#519) green.

- **Plan — slice 7 (2026-09-15, #519, 132 tools).** Calibration: `plans/calibration.py` (`landing_rows` — every completed milestone that held a date with `late` against the held date and `late_first` against the baseline; `calibration` — count, on_time, median / p80 lateness, median against first dates, five buckets, worst landing, `shift` from three landings; `likely_date`, `phase_likely_end`, `calibration_report`); `likely` on open plan rows, `likely_end` on phases, `calibration` on the plan payload (no new queries); `GET /projects/{slug}/plan/calibration/`; MCP `get_plan_calibration` + `get_plan` docs + the atlas-plan skill; the Plan header's Calibration line, dashed "likely …" chips on open rows, a "likely ends … · n d past target" chip on overrunning phases, the drawer's likely sentence; seed_demo lands the finished milestones −2 / +5 / +9 d and the design phase's target end three days short. Tests: plans/tests/test_calibration.py (landing rows incl. baseline, totals / buckets / worst / half-up medians, under-sample and empty projects, likely dates and phase ends, API report + plan rows + 401 + 404, TSX needles), mcp client test. Playwright: "Calibration milestones land a median 5 d late · 1 of 3 on time · Paradigm implemented in PsychoPy landed latest (+9 d) · open dates carry a likely landing 5 d later", five likely chips, "likely ends 2026-10-15 · 3 d past target" on the design phase, the drawer reads "Likely lands 2026-10-15 — your milestones land a median 5 d after their dates."; both themes, no page errors.

- **Plan — slice 6 (2026-09-14, #517, 131 tools).** The plan review: `PlanReview` (plans 0004) per sitting; `plans/review.py` (`review_queue` — open milestones overdue first, then by date, undated last, each with phase, days, blockers, slack, conflict, drift, open tasks; `review_state` — last / days_since / due at seven days / open / last summary; `finish_review`); `GET`/`POST /projects/{slug}/plan/review/`; `review` on the plan payload; MCP `get_plan_review`, `finish_plan_review`, `move_milestone`; the Plan header's "Review plan" button + chip (amber when due), `Review.tsx` card walk with one-key verdicts (k d w W m s ← → Esc) applied as PATCHes, a summary card with a note and "Finish review"; the shortcuts sheet's Plan review group; seed_demo records a sitting nine days ago. Tests: plans/tests/test_review.py (queue order + flags, state never / recent / due / nothing open, finish, API GET/POST/400/401/404 + plan payload, TSX + sheet needles), mcp client tests (three calls). Playwright: chip "reviewed 9 d ago", first card "Pilot data collected (n=12) · 3 d late · slipped 27 d · 2× · 1/3 tasks", `w` pushes it and advances to "1 / 6", `k`/`d`/`k`… reach the summary "4 kept · 1 completed · 1 moved · 0 skipped", Finish toasts and the chip reads "reviewed today"; both themes, no page errors.

- **Plan — slice 5 (2026-09-14, #516, 128 tools).** Plan drift: `MilestoneDateChange` (plans 0003) written by `Milestone.save()` when the loaded due date differs (folds within ten minutes, an undo deletes the fold; equal saves, check-offs and outline round-trips log nothing); `plans/drift.py` (baseline = first recorded date, slipped, moves, history per milestone; project total / moved / most slipped / baseline vs current end) on one query per project; baseline / moves / slipped / history on plan rows, `drift` on the plan payload, baseline / moves / slipped on roadmap rows; `GET /projects/{slug}/plan/drift/`; MCP `get_plan_drift` + `get_plan` docs; the Plan page's Drift line, "slipped n d · 2×" / "pulled in n d" chips, the drawer's Date history, the roadmap's dashed ghost diamonds tied to the live ones plus hover text and legend; seed_demo backdates three moves. Budget: dashboard 115 → 116 (one log read per active project's roadmap). Tests: plans/tests/test_drift.py (logging rules incl. update_fields and equal saves, folding + undo, baseline rules, totals / most / ends, plan + roadmap + report over the API, 401, a PATCH logging, TSX needles), mcp client test. Playwright: "Drift +37 d since the baseline · 2 milestones moved · Pilot data collected (n=12) slipped most (+27 d)", chips "slipped 27 d · 2×" and "slipped 10 d", the drawer lists both moves with the reason, the roadmap shows two ghosts; dragging a diamond a week adds a ghost and a "slipped 7 d" chip, and a second drag in the light run folds into it ("slipped 14 d"); both themes, no page errors.

- **Plan — slice 4 (2026-09-14, #515).** Slack and the critical chain: `slack_map` (days a milestone can slip before it pushes a dated dependant; negative when the dates already contradict) and `critical_chain` (from the last dated milestone upstream along the blocker due latest at each step; ids, titles, span, least slack) in plans/dependencies.py, on the same dependency query; `slack` on plan + roadmap milestone rows and `critical_chain` on both payloads; MCP `get_plan` / `get_roadmap` docs; the Plan page's "Critical chain … · n d · n d of slack / no slack / n d over" line, "n d slack" chips at seven days or fewer, route marks on the chain's rows; the Roadmap's heavier chain arrows, ringed diamonds, hover slack, legend; seed_demo draft due at +96 d (5 d of slack). Tests: plans/tests/test_slack.py (slack values incl. undated dependants, the chain and its tie-break, API plan + roadmap, TSX needles). Playwright: the demo plan reads "Ethics amendment approved → Full sample collected → Pre-registered analysis complete → Manuscript draft to co-authors · 56 d · 11 d over" with a "5 d slack" chip on the pre-registered analysis; the roadmap draws three heavy arrows and the legend names the chain; both themes, no page errors.

- **Plan — slice 3 (2026-09-14, #514).** Dependencies on the roadmap: roadmap milestone rows carry `blocked_by` (all blocker ids) and `conflict`, all from one `dependency_edges` query (overview budget unchanged at 60); Roadmap.tsx draws cubic arrows blocker → dependant under the diamonds (SVG overlay, direction-aware, per-colour arrowhead markers), waiting milestones dashed, conflicting ones amber (arrow too), hovering a diamond lights its transitive chain and dims the rest, the status line says what it waits for, legend extended; MCP `get_roadmap` docs. Tests: plans/tests/test_dependencies.py (roadmap rows carry edges + conflict flag; TSX needles). Playwright: the demo roadmap draws one arrow (Ethics amendment approved → Full sample collected, amber for the conflict), hovering the sample diamond dims the other diamonds; both themes, no page errors. Desktop run 225 (the Edge 152 hotfix) built green.

- **Owner report (2026-09-14, desktop 0.1.221, Edge 152): blank boot — "Uncaught SyntaxError: Unexpected strict mode reserved word @ /static/js/islands/preload-helper-chunk.js:1".** Vite 8's emitted preload helper (imported by spa.js; its deps list is always empty here) failed to parse in the owner's WebView while parsing fine in Node 22 and Chromium 141. Fix: `make js` rewrites the helper to a three-token module after every build (frontend/scripts/simplify-preload.mjs), guarded by core/tests/test_preload_helper.py; the watchdog now reports line:column. Ships as the next desktop build; the owner should update from 0.1.221.

- **Plan — slice 2 (2026-09-14, #513, 127 tools).** Dependency date conflicts: `date_conflicts` (open milestones due on or before their latest open dated blocker, `suggested` the day after) and `resolve_conflicts` (dependency order, cascading, undated left alone) in plans/dependencies.py; `conflicts` on the plan payload; `POST /projects/{slug}/plan/reschedule-conflicts/`; MCP `fix_plan_conflicts` + `get_plan` docs; the Plan page's amber banner with "Push the dates" and a "due before its blocker" row chip, a toast listing the moves; seed_demo dates the full sample before the ethics amendment. Tests: plans/tests/test_conflicts.py (detection incl. completed blockers, cascading fix order, undated untouched, API 401/404 + payload + fix, TSX needles), mcp client test. Playwright: the banner names the conflict with the suggestion, one click toasts "Moved Full sample collected → 2026-10-25" and clears the banner and chip; both themes, no page errors.

- **Plan — slice 1 (2026-09-14, #512, 126 tools).** Area: **Plan** (from #512). Milestone dependencies: `Milestone.blocked_by` (plans 0002), `plans/dependencies.py` (`set_blockers` — self/foreign/unknown/loop refused; `blocked_map` one query per project; `open_blockers`, `is_blocked`, `unblocked_by`), the milestone serializer's `blocked_by` / `blocks` / `blocked` with the same checks as a 400, the plan payload and roadmap rows carrying the flags, `upcoming_milestones` sorting blocked last (overview + dashboard "next"), MCP `set_milestone_dependencies` + `complete_milestone` / `get_plan` docs; the Plan page's lock chip, the drawer's "Waits for" section (chips, picker of open milestones, server reason on refusal), the "Unblocked: …" toast; seed_demo: the full sample waits on the ethics amendment. Tests: plans/tests/test_dependencies.py (rules incl. loops, flags follow completion, upcoming + roadmap ordering, API 401/400/200 + plan payload + freeing over the API, TSX needles), mcp client test. Playwright: "waits for Ethics amendment approved" chip, the drawer lists it with the picker, completing the ethics milestone toasts "Unblocked: Full sample collected" and clears the chip, undo restores it; both themes, no page errors.

- **Notes + graph — slice 9 + verdict (2026-09-14, #511).** The graph as a constellation: `frontend/src/app/graph/stars.ts` (glow halo, bright core with highlight, optional ring, labels as you zoom — hubs first — dimming as alpha, a seeded 220-star field behind the dark theme, a guard for unplaced nodes) shared by the graph page's 2D mode (custom paint + pointer area, radius capped at 13 px, charge −110 / link 42 so the stars have room) and the note editor's local graph; the 3D mode carries a glow-sprite path behind a `window.THREE` guard (the vendored bundle does not expose it, so spheres stay). Backlog #292's graph item closed as far as the bundle allows; #315 (block transclusion) parked. **Verdict:** Notes + knowledge graph judged best-in-field for a single-user research tool (DECISIONS); current area → **Plan** from #512. Tests: core/tests/test_graph_v2.py (renderer exports, both pages wired, THREE guarded). Playwright: 2D graph and local graph render as stars in both themes, zoom shows hub labels, 3D still boots; no page errors after the finite guard (the first frames had no layout).

- **Notes + graph — slice 8 (2026-09-14, #510, 125 tools).** Related notes: `notes/related.py` (`related_notes` — unlinked notes scored by shared cited papers ×3, shared #tags ×2, shared link targets ×2, shared informative words ×0.5 capped at six; reasons in words; four grouped queries), `GET /notes/{id}/related/?limit=` (1–20, 400 on junk), MCP `get_related_notes`; the editor rail's "Related · not linked yet" panel with a Link button that appends "See also [[Title]]." through the editor; seed_demo adds "Capacity limits reading list" (cites two of the hub's papers, #method, unlinked); backlog #314 (embeddings) parked. Tests: notes/tests/test_related.py (scores and reasons, linked/unrelated exclusion, shared link targets, limit clamp, tokenizer, API + 401 + 400 + 404, TSX needles), mcp client test. Playwright: the hub shows the reading list with "cites 2 of the same papers · shares 5 terms: …"; Link appends the See-also line and the row leaves the panel; both themes, no page errors.

- **Notes + graph — slice 7 (2026-09-14, #509).** Research markdown in every rendered body: `$math$` / `$$display$$` lifted before Markdown (underscores survive; dollars in prose and code untouched) and typeset in the SPA by vendored KaTeX 0.16.22 (`static/vendor/katex/`, 604 KB, loaded on first use, offline in the desktop) through a `useMath` hook in `Prose`; `- [ ]` task boxes (inserted after nh3, never an `<input>` through the sanitiser); `> [!kind]` callouts in the Obsidian set with a fallback to note, split per marker; footnotes; `==highlights==`; nh3 keeps only safe `class` tokens and footnote ids; the note preview now goes through `Prose`. CSS for callouts (kind colours), tasks, math, footnotes, marks in both themes. seed_demo's Strategic allocation hypothesis carries a callout, inline and display math, a footnote, tasks and a highlight. Tests: core/tests/test_rendering.py (math lifting incl. code/prose dollars, tasks/callouts/footnotes/marks, folded quotes → two callouts, sanitizer allowlist, KaTeX vendored + Prose wiring with hooks above the early return). Playwright: the demo note shows 3 KaTeX spans (1 display), the Prediction callout, 2 boxes (1 checked), the mark and the footnote; both themes, no page errors.

- **AUDIT #29 (2026-09-14, #508).** pip-audit clean, npm audit clean, `scripts/audit.sh` green; new surfaces since #498 probed (anon/wrong key 401 everywhere; SSRF guard refuses loopback in decimal/hex spellings, link-local, `file://`; depth/rid/tag/sources bounds; a 1 MB note). **Two findings fixed:** (1) catastrophic backtracking — 50 k `[[` took 64 s in the wiki-link reader, 256 KB of unclosed `<meta ` took 96 s in the page-title reader (remote bytes) → link classes exclude `[`, the meta walk is bounded (2 ms / 17 ms), `core/tests/test_regex_budgets.py` pins every text parser; (2) the note list read the project, backlinks and papers per row (47 queries for 40 notes; `select_related()` on a related manager bypassed the prefetch) → 7 queries, `test_api_notes_list_budget`. Hot endpoints 4–91 ms in-process. Desktop runs 209–219 green. Report: AUDITS.md › Audit #29. **Next audit due at #518.**

- **Notes + graph — slice 6 (2026-09-14, #507, 124 tools).** Outline + measure: `notes/outline.py` (`outline` — ATX headings outside code fences with line numbers; `measure` — words, characters, minutes at 200 wpm, headings, distinct links/citations, tasks done/total), `GET /notes/{id}/outline/`, MCP `get_note_outline`; the editor's Outline pane (two or more headings; click → cursor on the line, `MdHandle.goToLine`) and the live "75 words · 1 min · 1/3 tasks" header line; the renderer no longer turns a line-leading `#tag` into a heading; list snippets drop heading marks; seed_demo's Pilot observations gets sections and task boxes. Tests: notes/tests/test_outline.py (outline rules incl. fences/closing hashes/seven hashes/`#tag`, measure incl. distinct links and rounding, API + 401 + 404, TSX/editor needles), core/tests/test_rendering.py (tag line stays prose), mcp client test. Playwright: rows Setup/Observations/Anomalies/Next, clicking Next puts the cursor on "## Next", typing a task box turns the line into "80 words · 1 min · 1/4 tasks"; both themes, no page errors.

- **Notes + graph — slice 5 (2026-09-14, #506, 123 tools).** The graph in time: nodes carry `created_at` (a paper's filing day in the project, a note's creation) and note nodes `tags`; `stats.first`/`last`; the graph page's timeline row (▶ play / day slider / "Jan 2026 · 10 papers · 0 notes" caption / today reset) replays the graph as it was filed, a tag row lights the tagged notes and the papers they cite; the graph is fed through `graphData()` with cached node objects (positions persist; no rebuild per change); MCP `get_project_graph` (new — the page's payload was not on the MCP); seed_demo spreads filing days over the past year. Tests: core/tests/test_graph_v2.py (filing day from the project link, note day, tags, first/last, empty project; TSX needles), mcp client test. Playwright (2D): "Oct 2025 … Sep 2026", the slider at a third reads "Jan 2026 · 10 papers · 0 notes", play runs to "Sep 2026 · 22 papers · 3 notes", #pilot presses; both themes, no page errors.

- **Notes + graph — slice 4 (2026-09-14, #505, 122 tools).** History: `NoteRevision` (notes 0009), `notes/history.py` (`snapshot` from the pre-save state, ten-minute coalescing, no duplicates, fifty kept; `revision_rows`, `revision_diff` with a unified then→now diff; `restore` files the current state first and re-syncs links/citations/tags/renamed [[links]]), the note PATCH files on changed title/body, `GET /notes/{id}/revisions/`, `GET …/{rid}/`, `POST …/{rid}/restore/` (404 on unknown ids), MCP `list_note_revisions` / `get_note_revision` / `restore_note_revision`; the editor rail's History panel (rows with word deltas, inline coloured diff, Restore behind a confirm, copy text; the editor is rewritten on restore); seed_demo files a two-day-old revision of the hub note. Tests: notes/tests/test_history.py (coalescing, duplicates, retention cap, diff counts, restore refiles + rename follows back, API list/detail/404/401, PATCH files once, restore reply, TSX needles), mcp client test. Playwright: the hub note shows "Sep 12 · 10 w · +7", the diff opens with −1/+3 lines and a Restore button; both themes.

- **Owner messages (2026-09-14, after #504).** (1) Commits are now authored as `AliZareh-CoE <100804412+AliZareh-CoE@users.noreply.github.com>` (repo-local git config); the gmail address belongs to the AliZareh-Official account, which is what the history showed. Past/merged commits are not rewritten — moving that e-mail to the AliZareh-CoE account re-attributes them. (2) Diagnostics dump from a 0.1.139 desktop: the feed serves 0.1.214 anonymously (installer downloads); the updater endpoints now start with `AliZareh-CoE/Whorl`, then the old repo name, then the mirror. Ships as the next desktop build.

- **Notes + graph — slice 3 (2026-09-14, #504, 119 tools).** #tags: `notes/tags.py` (`parse_tags`, `sync_note_tags`, `project_tags`), `Note.tags` (notes 0008 + backfill), synced on API create/update, `?tag=` on `/notes/`, `GET /notes/tags/?project=` (400 without a project), suggest `kind=tag`, `#` completion in the CodeMirror editor, tag chips with counts in the rail (filter toggle), tags under the editor title; MCP `list_notes(tag)` + `list_note_tags`; seed_demo tags Pilot observations #pilot #method. Tests: notes/tests/test_tags.py (parser rules incl. headings/code/anchors/`#123`, sync + counts + order, suggest, API filter/tags/401/save-keeps-tags, TSX + editor needles), mcp client test. Playwright: chips "#method 1 · #pilot 1", clicking filters to one note, the header shows both, typing "#me" offers "#method · 1 note"; both themes.

- **Notes + graph — slice 2 (2026-09-14, #503, 118 tools).** "Around this note": `note_neighbourhood(note, depth)` in core/graph.py (BFS over the project graph, both directions, `hops` per node, `stats`), `GET /notes/{id}/graph/?depth=` (1–3 clamped, 400 on a non-integer), MCP `get_note_graph`; the editor's link rail draws the neighbourhood with the vendored 2D force-graph (ringed current note, status colours, arrows, hop switch, click to open). Tests: notes/tests/test_note_graph.py (walk + depth clamp + islands excluded, API + 401, TSX needles), mcp client test. Playwright: the demo's "Strategic allocation hypothesis" shows 2 notes · 5 papers within 2 hops (18 papers at 3), both themes.

- **Notes + graph — slice 1 (2026-09-14, #502, 117 tools).** Area: **Notes + knowledge graph** (from #502). `notes/relink.py` (`rename_links` across notes / decisions / lab entries / captures with alias-preserving, case-insensitive rewrites and link re-sync; `link_mentions` wraps the first plain mention), the note PATCH replies `relinked`, `POST /notes/{id}/link-mentions/` (400 on bad `sources`), MCP `link_mentions` + `update_note` docs; editor toast on rename, Link / Link all in the backlinks panel; seed_demo's Pilot observations mentions Load theory overview without a link. Tests: notes/tests/test_relink.py (3: rewrite counts per kind, other projects untouched, case-only rename no-op, mention wrapping/word bounds/sources filter, API + 401 + TSX needles), mcp client test. Playwright: Link turns the mention into a backlink; renaming the title shows "Renamed — 1 link updated in 1 note" and the source's body carries the new title; both themes.

- **Inbox — slice 7 + verdict (2026-09-14, #501).** `/inbox?capture=` deep link (captured once, parameter dropped, "Captured from the browser" toast) + the Connect page's step 4 bookmarklet (opens a 560×480 Atlas window with title + URL); narrow-width check: nothing to fix at 640 px (buttons and the selection bar wrap; 420 px is the rail, backlog #312); Connect's stale "88 tools" line fixed. **Verdict:** the Inbox is judged best-in-field for a single-user tool (DECISIONS); backlog #313 (desktop global hotkey / phone share sheet); current area → **Notes + knowledge graph** from #502. Tests: notes/tests/test_capture.py (test_browser_capture_deep_link_and_bookmarklet_are_wired). Playwright: `/inbox?capture=…` lands the capture, the URL is clean, a reload does not duplicate it; the Connect section renders.

- **Inbox — slice 6 (2026-09-14, #500).** Dates in captures: `notes/when.py` (`parse_when`, `parse_date`, `parse_time` — relative words, weekdays with a lead-in or at the line end, month dates, ISO dates, am/pm and 24 h times, noon/midnight), `detect()` gains `due` / `due_time`, `convert()` sets a todo's `due_at` in the caller's `tz` (IANA or offset; `_zone`, `due_instant`) and a milestone's `due_date`, both without the phrase in the title; `ConvertCaptureSerializer.tz`, the SPA sends the browser zone, the MCP client its machine offset (`local_tz`); Inbox rows show a "due …" chip; seed_demo adds a dated capture. Tests: notes/tests/test_when.py (20 parser cases incl. non-matches, first line only, detect/convert/API with Europe/Berlin, +05:30, an unknown zone, milestone explicit-due precedence, TSX needles), mcp client test. Playwright (Berlin-zoned browser): the demo row shows "due Fri, Sep 18 · 3 PM"; converting it puts "Fri 3:00 PM" on Today; both themes.

- **Inbox — slice 5 (2026-09-14, #499, 116 tools).** Link titles: `link_title` / `link_fetched_at` (notes 0007), `notes/links.py` (`check_url` refuses non-http and private/unresolvable hosts, `fetch_link_title` streams ≤ 256 KB with a 4 s timeout and reads og:title / <title>, `enrich_capture` remembers the result — failures too — with `force` to retry), `POST /quick-capture/{id}/enrich/` (+ `link_error`), the SPA enriches new link captures at once and old unfetched ones on load (five per load), rows show "↗ Title — site" with "fetching…" / "no title found · try again"; a bare link → note takes the page title; MCP `enrich_capture`; seed_demo adds a bare arXiv link with its title. Tests: notes/tests/test_links.py (title parsing, host rules, mocked transport incl. PDF/404/oversize, enrich once/force/error, convert title, API + 401, TSX needles), mcp client test. Live: a nature.com link fetched its real title through the proxy; both themes.

- **AUDIT #28 (2026-09-14, #498).** pip-audit clean, npm audit clean, `scripts/audit.sh` green; new surfaces since #488 probed (anon 401 everywhere; day date, history limit, snooze word, bulk ids validated; grouped queries for pulses/trends/brief/history). **Finding fixed:** the capture list read the project per row (36 queries / 54 ms for 61 captures) → `select_related("project")` (6 queries), `test_api_inbox_list_budget` pins ≤ 12 for 40 rows. Hot endpoints 15–90 ms warm. Desktop runs 201–208 green. Report: AUDITS.md › Audit #28. **Next audit due at #508.**

- **Inbox — slice 4 (2026-09-14, #497, 115 tools).** Batch triage: `bulk_triage` in notes/capture.py (file / dismiss / snooze / todo / wake; untriaged only; ≤ 200; changed ids back), `POST /quick-capture/bulk/` (400 on a bad action, an empty list, a missing project or an unknown snooze word), classic `inbox_bulk` rewired through it, MCP `triage_captures`; Inbox rows get a checkbox (`space`, `⌘A`, `Esc`), a sticky selection bar (File under project · Today · Later… · Dismiss) with a batch undo; shortcuts sheet rows. Tests: notes/tests/test_capture.py (test_bulk_triage_files_dismisses_snoozes_and_converts — service, API guards, classic view, TSX needles), mcp client test. Playwright: two ticked rows show "2 selected", Dismiss drops them with "Dismissed 2 captures — Undo", ⌘A/Esc work from a focused checkbox, both themes.

- **Inbox — slice 3 (2026-09-14, #496, 114 tools).** Where did it go: `became_kind` / `became_id` / `triaged_at` on captures (notes 0006), set by `convert` and by every triage path; `became()` + `triage_history()` (titles in one query per kind, deleted objects flagged); `GET /quick-capture/history/?limit=`; `became` and `triaged_at` on the capture serializer (PATCH processed stamps/clears the time); Inbox "Recently triaged · where did it go?" toggle — → Note · Title links, "filed under …", "dismissed", Put back; MCP `get_inbox_history`; seed_demo adds a converted and a dismissed capture. Tests: notes/tests/test_capture.py (test_captures_remember_what_they_became), mcp client test. Playwright: the demo shows "→ Note · Load theory overview" and a dismissed row with Put back, both themes.

- **Inbox — slice 2 (2026-09-14, #495, 113 tools).** Snooze: `QuickCapture.snoozed_until` (notes 0005), `snooze_date` / `snooze` / `open_captures` / `snoozed_captures` in notes/capture.py, every untriaged count routed through `open_captures` (dashboard, brief, achievements, classic inbox), `POST /quick-capture/{id}/snooze/` (`until` keyword or date, "" wakes; 400 otherwise), `?snoozed=` filter, Inbox rows get *Later* (Tomorrow / Monday / Next week / Weekend / Pick a date…) + `s`/`w` keys + undo, a "n snoozed · next back …" toggle with the sleeping rows and *Wake*; MCP `snooze_capture`, `list_inbox(snoozed)`; seed_demo adds a capture asleep until Monday. Tests: notes/tests/test_capture.py (test_snooze_parses_keywords_and_hides_the_capture_until_its_day, test_snoozed_captures_leave_the_untriaged_counts), mcp client test. Playwright: Later → Tomorrow drops the row to "2 snoozed", the undo toast offers to wake it, `s` snoozes the highlighted row, both themes.

- **Inbox — slice 1 (2026-09-13, #494).** Area: **Inbox / capture** (from #494). `project_index` + `suggest_project` (name ×3, description, phases, questions, notes, decisions, papers, tags; ≥ 2 points, no tie), `hint.project` on captures (index once per request), Inbox rows preselect it with a "suggested · Project" chip; MCP `list_inbox` docstring. Tests: notes/tests/test_capture.py (test_suggest_project_from_the_capture_words).

- **Dashboard — slice 7 + verdict (2026-09-13, #493).** Narrow widths: single-column grids below `lg` with `min-w-0` panels, stat tiles stack under 480 px, attention rows wrap, project labels truncate — no sideways scroll from 640 px up (phone width is a shell concern, backlog #312). **Verdict:** the Dashboard is judged best-in-field for a single-user tool (DECISIONS); current area → **Inbox / capture** from #494; Audit #28 due at #498.

- **Dashboard — slice 6 (2026-09-13, #492, 112 tools).** Clickable heatmap: cells are buttons (aria-pressed, ring), a day panel under the grid lists that day's timeline events across every project with kind chips, links and projects; `GET /dashboard/day/?date=` (`day_activity`, 400 on a bad date, today by default); MCP `get_day_activity`. Tests: core/tests/test_dashboard_v2.py (test_day_activity_lists_one_day_across_projects), mcp client test. Playwright: clicking 2026-09-12 lists the demo's milestones, papers and notes, both themes.

- **Dashboard — slice 5 (2026-09-13, #491, 111 tools).** `core/brief.py::daily_brief` (needs you, on your list, this week everywhere, next to read, writing, projects with rhythm, this month with deltas → markdown), `GET /dashboard/brief/`, hero *Copy today's brief* (clipboard + wide preview), MCP `get_daily_brief`. Tests: core/tests/test_brief.py (3), mcp client test. Playwright: the button copies the demo brief ("Today's brief copied"), both themes; anonymous → 401.

- **Dashboard — slice 4 (2026-09-13, #490).** `stats_trend` (six months per stat with the tiles' own definitions, `previous` = last month, one grouped query per stat) on the dashboard payload as `trends`; stat tiles show six trend bars and a calm "▲ n vs Aug" delta; MCP `get_dashboard` docstring. Tests: core/tests/test_dashboard_v2.py (test_stats_trend_bins_six_months_with_the_current_month_last). Playwright: the demo tiles show the bars with September brightest, both themes.

- **Dashboard — slice 3 (2026-09-13, #489).** `pulses_everywhere` (nine grouped queries for every active project's 12-week pulse) + `quiet_projects` (flat ≥ 3 weeks); `active[].pulse` and `attention.quiet` on the dashboard payload; project cards carry a mini pulse strip with "n in 12 wk" / "quiet n wk"; a *quiet* needs-attention row; MCP `get_dashboard` docstring. Tests: core/tests/test_dashboard_v2.py (test_pulses_everywhere_bins_per_project_and_flags_quiet_ones, ≤ 9 queries asserted). Playwright: the demo card shows the strip with "41 in 12 wk", both themes.

- **AUDIT #27 (2026-09-13, #488).** pip-audit clean, npm audit clean, `scripts/audit.sh` green; new surfaces since #478 probed (anon 401 everywhere, status-update window clamped 1–90 / 400 on non-integers, memo instance-scoped, deep-link `?id=` safe). **Finding fixed:** the dashboard's writing panel ran a pre-flight per working manuscript per active project (39 → 102 queries with three more projects); the pre-flight now runs only for the rows shown (at most four per load, most urgent first) and the live count is one aggregate (→ 78); `test_api_dashboard_budget` pins ≤ 100 with three busy projects. Hot endpoints 23–98 ms warm. Desktop runs 194–200 green. Report: AUDITS.md › Audit #27. **Next audit due at #498.**

- **Dashboard — slice 2 (2026-09-13, #487).** `writing_everywhere` (live count + up to six manuscripts across planning/active projects, overview-glance rows with project, sorted nearest deadline → nudge due → id) on the dashboard payload as `writing`; the *Deadlines* panel becomes *Writing* (status · project · deadline · clock/nudge · readiness pill, calendar subscribe kept). Tests: core/tests/test_dashboard_v2.py (test_writing_everywhere_sorts_live_manuscripts_by_urgency). Playwright: the demo paper shows "revision · 17 d left · 41 d revising · 1 blocking", both themes. Audit #27 due next (#488).

- **Dashboard — slice 1 (2026-09-13, #486).** Area: **Dashboard** (from #486). `reading_queue_everywhere` (to_read, high_priority, projects, next[5] by priority then age across planning/active projects) on the dashboard payload as `reading`; *Next to read* panel under *Deadlines*; MCP `get_dashboard` docstring. Tests: core/tests/test_dashboard_v2.py (test_reading_queue_everywhere_orders_by_priority_then_age). Playwright: the demo shows "8 unread · 1 high priority", Draheim 2022 at the head, both themes. Audit #27 due at #488.

- **Project overview — slice 7 + verdict (2026-09-13, #485).** Next-milestones check-off on the overview (optimistic row removal + ring/count update, PATCH /milestones/{id}/), recent decisions deep-link into the log (`?id=` scroll + expand + ring). **Verdict:** the Project overview is judged best-in-field for a single-user tool (DECISIONS); current area → **Dashboard** from #486; Audit #27 due at #488. Tests: projects/tests/test_overview_v2.py (test_overview_polish_check_off_and_decision_deep_links).

- **Project overview — slice 6 (2026-09-13, #484).** Performance pass on the API overview: `core/memo.py` request-scoped memo (timeline, roadmap, current phase, progress computed once per request), pre-flight bibliography N+1 fixed, deferred-field loads removed, question phases prefetched; 89 queries / 117 ms → 61 / 94 ms warm on the demo project; `test_api_overview_budget` pins ≤ 60 queries on a busy project. Tests: core/tests/test_query_budgets.py.

- **Project overview — slice 5 (2026-09-13, #483).** `pulse` (12 Monday-based weeks: count + kinds, total, busiest, quiet_weeks, last_activity, days_since) on the overview payload; a *Pulse* bar strip in the header (sqrt scale, hollow silent weeks, glowing peak, per-week tooltips, caption "41 events in 12 wk · peak Sep 7" / "quiet n wk · last …"). Tests: projects/tests/test_overview_v2.py (test_pulse_bins_twelve_weeks_and_finds_the_quiet_streak). Playwright: 12 bars, the last titled "Sep 7 – Sep 13: 3 milestones, 22 papers added, …", both themes.

- **Project overview — slice 4 (2026-09-13, #482, 110 tools).** `projects/status.py::status_update` (phase + health, manuscripts with clock/readiness/deadline, done-by-kind in the window, next, open questions, blockers → markdown), `GET /projects/{slug}/status-update/?days=`, overview menu *Copy status update…* (clipboard + wide preview), MCP `get_status_update`. Tests: projects/tests/test_status.py (4), mcp client test. Playwright: the menu item copies the demo note to the clipboard ("Status update copied"), both themes; anonymous → 401.

- **Project overview — slice 3 (2026-09-13, #481).** `notebook_glance` (notes: total, edited_this_week, unlinked, last_edited, recent[3]; experiments: total, this_month, last, quiet ≥ 14 d; datasets) on the overview payload as `notebook`; a *Notebook* panel in the lower row (now three columns) with the three last-touched notes, the last lab entry and the quiet chip; MCP docstring. Tests: projects/tests/test_overview_v2.py (test_notebook_glance_notes_lab_log_and_datasets). Playwright: the demo shows 3 notes · 3 edited this week, 2 lab entries · 2 this month · 1 dataset, both themes.

- **Project overview — slice 2 (2026-09-13, #480).** `literature_glance` (to_read, high_priority_unread, read_this_month, by_status, next_up, last_added) on the overview payload; *Literature* panel with the to-read link into the queue, the high-priority chip, reads this month and *Next up*; row grid 2/4 columns. Desktop run 193 green. Tests: projects/tests/test_overview_v2.py.

- **Project overview, third pass — slice 1 (2026-09-13, #479).** `manuscripts_glance` rows carry `clock` (+ `nudge` while waiting) and `readiness` (pre-flight ready/fails/warns/summary while working); overview manuscript rows show the clock chip (amber "· nudge?") and a readiness pill; MCP `get_project_overview` docstring updated. **Desktop CI is green again:** run 192 (0.1.192) succeeded after the #478 fix — the first green build since 0.1.139. Tests: projects/tests/test_overview_v2.py.

- **AUDIT #26 (2026-09-13, #478).** **Desktop CI finding fixed:** runners are back and every build since run 187 was red at *Write release notes* (`head -12` closed the pipe → grep "Broken pipe" under pipefail → unterminated heredoc); the pipeline no longer uses `head`. Also noticed: the GitHub repo is now named **Whorl** (owner's rename; URLs keep working through redirects; nothing renamed on our side). pip-audit clean, npm audit clean, `scripts/audit.sh` green; new surfaces since #468 probed (anon 401 everywhere, replace ignores paths outside the tree, forged lint spans skipped, bad regex → 400); hot endpoints 18–80 ms; one cap added (search pattern ≤ 500 chars, test); regex backtracking recorded as an accepted single-user risk. Report: AUDITS.md › Audit #26. **Next audit due at #488.**

- **Go to definition + Writing verdict (2026-09-13, #477).** ⌘⇧D / palette: `\ref{key}` → its `\label` across files, `\cite{key}` → the `.bib` entry or the Bibliography tab; adapter `getCursor()`; shortcut sheet rows for ⌘⇧D and ⌘⇧F. Writing judged best-in-field for a single-user tool (DECISIONS); current area → Project overview after Audit #26 (#478). Tests: writing/tests/test_goto.py. Playwright: caret on `\ref{tab:dprime}` in sections/method.tex → main.tex label line.

- **Find in project (2026-09-13, #476, 109 tools).** `writing/search.py` (search_files / replace_in_files: plain or regex, case, 500-hit cap, subset of files, saved like an editor save), `GET /manuscripts/{id}/search/`, `POST /manuscripts/{id}/replace/`, Studio *Search* tab (⌘⇧F, grouped hits with highlight, jump, Replace all with confirm + buffer save + reload), MCP `search_manuscript` / `replace_in_manuscript`. Tests: writing/tests/test_search.py (5), mcp client test. Playwright: ⌘⇧F focuses the box; "load" → hits across main.tex and sections/method.tex; a hit jumps to its line; Replace all → the editor shows the new word; demo texts restored.

- **Nudge the editor (2026-09-13, #475).** `writing/clock.py::nudge` (1.5× venue median, 60-d floor, 90-d default, note-with-"nudge" resets) + `waiting_manuscripts`; `clock.nudge` on the manuscript serializer, `attention.waiting` on the dashboard payload; manuscript page amber chip + hint + *Log a nudge* (note event), board "· nudge?", dashboard *waiting* rows. Tests: writing/tests/test_clock.py (7). Playwright (demo set to under review, then restored): dashboard row "waiting … 96 d, usually 82 — a nudge is fair", the hint on the page, Log a nudge → hint gone, note on the timeline.

- **Status clock (2026-09-13, #474, 107 tools).** `writing/clock.py` (`status_clock` from the submission events, `venue_turnaround` median/first-decision/fastest/slowest per venue), `clock` on the manuscript serializer (events prefetched), `GET /manuscripts/venue-turnaround/?venue=&exclude=`, board card line + manuscript-page chip with the venue median, MCP `get_venue_turnaround`. Tests: writing/tests/test_clock.py (4), mcp client test. Live: the demo paper reads "41 d revising" (since reviews_received 2026-08-03); the manuscript page adds "your median here: 55 d to a decision" from the paper's own first round. Caught and fixed before shipping: a hook below the manuscript page's early return (the #28 class of bug).

- **Figure audit (2026-09-13, #473, 106 tools).** `writing/figures.py` (header-parsed PNG/JPEG/GIF sizes, printed width from the `\includegraphics` options, effective dpi: 300 bar / 150 floor, 10 MB cap, vector passes, missing fails, unused assets), pre-flight *Figure quality* row (worst figure's line), `GET /manuscripts/{id}/figure-audit/`, Studio pre-flight figures table (click → line), MCP `audit_figures`; seed_demo attaches a 1600-px `figures/pilot-dprime.png` + a figure environment. Tests: writing/tests/test_figures.py (4). Live: the demo figure reads png 1600×1000 at 3.9 in — 410 dpi, "The figure prints sharp."; the table row jumps to main.tex.

- **One-click lint fixes (2026-09-13, #472, 105 tools).** Findings carry `original` + exact `col`; `apply_fixes` (bottom-up, verified, saved like an editor save), `POST /manuscripts/{id}/lint/fix/` with optional `only`, Studio *Fix* per row + *Fix all n* + palette action, MCP `fix_lint`. Tests: writing/tests/test_lint.py (13), mcp client test. Playwright: "Fix all 3" → one row fixed → "lint · 2" → fix all → empty panel, editor line reads `250\,ms … Table~\ref{tab:dprime}`, DB matches; demo restored.

- **One linter (2026-09-12, #471, backlog #308).** `unmatched-env` + `unclosed-brace` error rules in `writing/lint.py` (comments, verbatim, `\verb`, `\url` skipped; an outer `\end` reports every inner environment it swallows); the `codemirror-lang-latex` linter is off (`enableLinting: false`), so every underline is a Problems row. Tests: writing/tests/test_lint.py (9). Playwright: only the real finding's line is underlined after a file switch (was lines 1–6 too); a stray `\begin{itemize}` + `\textbf{` show as two error rows and jump to their lines.

- **Style lint (2026-09-12, #470, 104 tools).** `writing/lint.py` (13 rules, cross-file labels, skips comments/verbatim/url/tables), `GET /manuscripts/{id}/lint/`, pre-flight *Style lint* row (warn, first-finding jump), Studio Problems panel merges the findings (lint chip, gutter underlines, click → line, refreshed on save, "Run the style lint" palette action), MCP `lint_manuscript`; editor diagnostics now kept by line. Demo method.tex carries three realistic hits. Tests: writing/tests/test_lint.py (7). Playwright: chip "lint · 3", three rows, click → sections/method.tex L12, toggle hides them. Found (not caused): the LaTeX language package's own linter underlines without listing — backlog #308.

- **Submit through the pre-flight (2026-09-12, #469, 103 tools).** `writing/services.py::submit_manuscript` (+ `SubmissionBlocked`), `POST /manuscripts/{id}/submit/` (409 with the report unless `force`; revision → `revision_submitted`/under_review), pipeline click → endpoint → in-app "Submit anyway" confirm, event notes carry the readiness rows; MCP `submit_manuscript`. Tests: writing/tests/test_submit.py (5), mcp client test. Playwright (both themes): blocked → Not yet keeps *Revision*; Submit anyway → *Under review* + "Revision submitted" event with the "Submitted anyway" note; plain steps stay PATCH; *Submitted* from *Drafting* also routes through. Demo data restored afterwards.

- **AUDIT #25 (2026-09-07, #468).** pip-audit: django 5.2.17, DRF 3.18.0, mcp 1.29.1 (pinned <2 + guard test), pydantic-settings 2.15.0, sqlparse 0.6.0, cryptography 50.0.1 → clean; npm: react-router-dom 7.18.3 → clean; new surfaces reviewed, no code findings; hot endpoints < 100 ms. Report: AUDITS.md › Audit #25. Next audit was #478 (done).

- **Readiness card + backlog sweep (2026-09-07, #467).** `PreflightCard` on the manuscript page (pill, fail/warn rows, "Every check in the Studio →"); Studio `?panel=<tab>`; 15 backlog lines struck with reasons (34 open remain). Test: writing/tests/test_preflight.py::test_manuscript_page_has_the_readiness_card_and_the_studio_opens_the_panel.

- **Submission pre-flight (2026-09-07, #466, 102 tools).** `writing/preflight.py` (13 checks, fix pointers, `ready`), `GET /manuscripts/{id}/preflight/`, Studio *Pre-flight* tab + palette action, MCP `preflight_manuscript`. Tests: writing/tests/test_preflight.py (broken paper fires every fail; clean paper is ready; API; TSX guard), mcp client test. Live on the demo manuscript: "Ready with 2 things to look at" (unused entries, TODO markers).

- **Two small ones (2026-09-07, #465).** #53 `reference_abstract_trgm` (literature 0009, Postgres-only DDL; applied on dev Postgres and on a fresh SQLite data dir); #56 `calendar_lines` weekday + month speech, six milestone reactions. Tests: core/tests/test_pet_calendar_lines.py, migration guard in test_desktop_settings.

- **MCP take_snapshot (2026-09-07, #464, 101 tools).** `client.take_snapshot(list_only)`, server tool, README pins, atlas-daily skill convention. Test: mcp_server/tests/test_client.py::test_take_snapshot_client_calls. Live: client wrote a 4.7 MB zip against the dev server.

- **Restore from a snapshot (2026-09-07, #463).** `POST /restore/ {snapshot}` stages a listed file by name (404 for unknown / path-like names); Diagnostics lists the files with *Restore…* → the existing staged-restore flow. Test: core/tests/test_snapshots.py::test_restore_from_a_snapshot_by_name. Playwright: Restore… → confirm → "A restore is staged" → cancel.

- **Automatic snapshots (2026-09-07, #462).** `core/snapshots.py` (take/prune/due/run_if_due/status/scheduler), core 0013 `BackupRecord.kind/path`, `manage.py snapshot --if-due`, `GET/POST /api/v1/snapshots/`, diagnostics `snapshots` block, Diagnostics *Automatic snapshots* section, desktop scheduler thread in run_desktop, README Backups rewritten. Tests: core/tests/test_snapshots.py. Playwright: Snapshot now → 4.4 MB zip, count 0→1 (docs/screenshots/diagnostics-snapshots.png).

- **Pet hatch animation (2026-09-07, #461).** Creature.tsx stage memory + `mochi-egg-crack` / `mochi-born` keyframes. Test: core/tests/test_pet_hatch_animation.py. Playwright: forced egg→hatchling plays the crack class.

- **Compile rhythm (2026-09-07, #460).** `compile_rhythm` / `compiles_since`; `progress.compiles`; Writing dot row; Studio `compiles today`; Mochi line. Tests: writing/tests/test_compile_rhythm.py.

- **Context-aware completions (2026-09-07, #459).** `editor/context.ts` (enclosingEnvironment, boostFor) + `contextAwareLatex` wrapper. Tests: writing/tests/test_context_completions.py (node-run).

- **Gutter chevrons (2026-09-07, #458).** Split gutter builder adds a collapse button per divider (sidebar / preview). Test: writing/tests/test_gutter_chevrons.py.

- **Theme candidates + section copy (2026-09-07, #457).** Matrix `theme-queue` link on every theme; Review `section-copy` per section. Test: core/tests/test_polish_457.py.

- **Revision retention surfaced (2026-09-07, #456).** `auto_revisions_keep` (writing 0017) drives the trim; revisions endpoint `retention`; History panel line + change control. Tests: writing/tests/test_revision_retention.py.

- **Compile dedupe (2026-09-07, #455).** `source_hash` + `compile_source_hash`/`compiled_source_hash` (writing 0016); action answers deduped/unchanged; `force`; Studio flash; MCP force. Tests: writing/tests/test_compile_dedupe.py.

- **Run from source (2026-09-07, #454).** `make standalone` + README/desktop README sections; verified fresh data dir → login 200 in 5 s. Test: core/tests/test_docs_standalone.py.

- **Archive name guard (2026-09-07, #453).** `core/archives.py` behind the submission zip, vault and backup. Test: core/tests/test_archives.py.

- **Studio PDF text layer (2026-09-07, #452).** `.studio-page` wrap + pdf.js TextLayer per page; lookups moved to the wrap. Test: writing/tests/test_studio_textlayer.py. Playwright: textLayer spans present, selection returns text.

- **Library reading flow (2026-09-07, #450).** `GET /references/reading-flow/?<filters>` (link resolution), ReadingFlow library mode at `/library/read`, Library "Read these →". Test: literature/tests/test_library_read_flow.py.

- **HTML sink guard (2026-09-07, #449).** core/tests/test_html_sinks.py: dangerouslySetInnerHTML only from `*html` fields, innerHTML only static, no |safe, mark_safe in one file.

- **Abstract peek + hover prefetch (2026-09-07, #448).** `_bibliography_rows` abstract; BibPanel ▸ peek; Projects cards prefetch `["overview", slug]`. Test: writing/tests/test_bib_peek.py.

- **Studio actions palette (2026-09-07, #447).** `ActionPalette` on ⌘⇧P with bindings; shortcuts sheet row. Test: writing/tests/test_action_palette.py. Playwright: ⌘⇧P → rows → Enter toggles the preview.

- **Duplicate manuscript (2026-09-07, #446).** `duplicate_manuscript` service + `POST /manuscripts/{id}/duplicate/`; Writing card menu; MCP tool (100 tools). Tests: writing/tests/test_duplicate.py. Playwright: card menu → prompt → copy opens with 2 files.

- **Cite hint for missing papers (2026-09-07, #445).** editor/index.ts self-filtered cite options + add-paper row (`onAddPaper`); Studio prompts, adds by DOI into the project, links via cite-library/, replaces the fragment, reloads. Test: writing/tests/test_cite_hint.py. Playwright: the add row appears for `\cite{zzz`.

- **Timeline bodies opt-out (2026-09-07, #444).** `project_timeline(bodies=False)` for the overview's week digest (no markdown rendering on the overview). Test: core/tests/test_timeline_bodies.py::test_bodies_can_be_skipped_and_the_overview_digest_skips_them. #108 struck.

- **Timeline expand (2026-09-07, #443).** `core/timeline.py` events carry `body_html` (decision, experiment, note, milestone notes, manuscript event notes); Timeline.tsx chevron expands a Prose block. Test: core/tests/test_timeline_bodies.py. Playwright: docs/screenshots/timeline-expand.png. Sweep: #69/#70/#110/#114/#121/#125/#136 struck.

- **.bbl in the submission zip (2026-09-07, #442).** compile `--keep-intermediates` → `Manuscript.compiled_bbl` (writing 0015); submission.zip ships `<main>.bbl`. Tests: writing/tests/test_bbl.py. Live: demo compile → zip lists main.bbl.

- **MCP figure upload (2026-09-07, #441).** `attach_manuscript_asset` (client, multipart) + `attach_manuscript_figure` tool (99 tools) with the figure snippet. Tests: mcp_server/tests/test_attach_figure.py (client kwargs, PATCH path, API multipart contract).

- **Undo for triage (2026-09-07, #440).** `components/UndoToast.tsx` (showUndo/UndoHost in main.tsx); Inbox triage + dashboard TriageControls undo via PATCH processed:false. Test: core/tests/test_undo_triage.py. Playwright: dismiss → Undo → row back.

- **Comments searchable + resolvable (2026-09-07, #439).** `Comment.resolved_at` (core 0012) + `target_route()`; PATCH /comments/{id}/; search kind `comment` (both paths, describe); Studio + Reference resolve toggles; gutter marks only for open. Tests: core/tests/test_comments_resolve_search.py.

- **Templates that plan (2026-09-07, #438).** `project_templates.py` plan/questions/themes per template; `instantiate_research_scaffold` (only when empty); template_list counts; NewProject card line `template-plan`. Tests: projects/tests/test_templates.py::TestResearchScaffold. Backlog #57/#58 struck.

- **Matrix → Related work .tex (2026-09-07, #437).** `related_work_latex` + `POST /manuscripts/{id}/related-work/` (409 guard, bibliography links, input line); Matrix button + picker + done strip; MCP `draft_related_work` (98 tools). Tests: writing/tests/test_related_work.py. Playwright: docs/screenshots/matrix-related-work.png.

- **Related on the Reference page + MCP (2026-09-07, #436).** Reference.tsx related-section over `/references/{id}/related/`; MCP `get_related_in_library` (97 tools); backlog #1/#3/#11 swept. Test: literature/tests/test_related_surface.py. Playwright: docs/screenshots/reference-related.png.

- **Search recents + pins (2026-09-07, #435).** Search.tsx localStorage recents (8, only searches with results) and ☆ pins; chips when the box is empty. Test: core/tests/test_search_recents.py. Playwright: docs/screenshots/search-pins.png.

- **Conditional files (2026-09-07, #434).** `core/files.py::file_response` (ETag mtime-size, Last-Modified, 304 on If-None-Match / If-Modified-Since, shared Cache-Control + nosniff) behind document_download, document_preview and DocumentViewSet.raw. Test: core/tests/test_conditional_files.py.

- **Library ranges (2026-09-07, #433).** anchorRef + selectRange; shift-click / shift-x ranges, ⌘A selects the view. Test: literature/tests/test_selection_keys.py. Playwright: shift-click 1→4 = 4 selected, ⌘A = all, Esc = 0.

- **⌘K creation verbs (2026-09-07, #432).** `paper:`/`doi:`/bare DOI or arXiv → by-doi (project-scoped); verbs Add a paper (`/library?add=1`), New note, New manuscript (`/writing?new=1`), New project; empty state `palette-empty`. Test: core/tests/test_palette_verbs.py. Playwright: docs/screenshots/palette-paper.png.

- **Today due times (2026-09-07, #431).** `TodoItem.due_at` (core 0011), browser parser `dueTime.ts` (Today input/edit, ⌘K todo:), due chips, carried-over count, `TodoNudge` in the sidebar, dashboard hero times, MCP `add_todo(due_at)`. Tests: core/tests/test_due_times.py (API, MCP client, node-run parser, TSX needles). Playwright: docs/screenshots/today-due.png.

- **Demo GIF (2026-09-07, #430).** `scripts/demo_gif.py` (11 captioned keyframes, compile-first studio, toast hidden) → `docs/demo.gif` 3.0 MB, README embed, `make demo-gif`. Test: core/tests/test_demo_gif.py.

- **Plan drag (2026-09-07, #429).** `POST /projects/{slug}/phases/reorder/`; Plan.tsx phase handle + milestone rows draggable, cards as drop targets, optimistic. Test: plans/tests/test_plans.py::test_phase_reorder_and_milestone_move. Playwright (real mouse drag, both directions, ring on the target): docs/screenshots/plan-drag.png.

- **Search coverage (2026-09-07, #428).** Protocols, datasets, captures in both search paths + describe(); SPA labels. Tests: core/tests/test_search.py::test_finds_protocols_datasets_captures (both paths).

- **Pet species (2026-09-07, #427).** `pet_species` (4 plumages + golden 1/64), `species` in pet state, CSS palettes `.mochi-species-*`, Creature `species` prop, pet page line, hatchling blurb. Tests: core/tests/test_pet.py::TestSpecies. Playwright: docs/screenshots/pet-species.png.

- **Performance pass (2026-09-07, #426).** Probe of 20 endpoints; references list 36→7 queries (`prefetch_related("tags")`); budget test core/tests/test_query_budgets.py::TestApiBudgets. Warm dashboard 23 q / 25 ms; overview 51 q / 60 ms (flat).

- **Shortcuts sheet (2026-09-07, #425).** `app/shortcuts.tsx` (`?` key, ⌘K verb), five groups. Guard: core/tests/test_polish.py::test_shortcuts_sheet. Playwright: docs/screenshots/shortcuts-sheet.png.

- **Backup awareness (2026-09-07, #424).** `core.BackupRecord` (0010) written by the backup view; `core/backups.py`; diagnostics `backups` + text line; dashboard attention row; Diagnostics header line. Tests: core/tests/test_backup_awareness.py (2).

- **Clickable run bars (2026-09-07, #423).** `QuickCapture.bot_run` (notes migration), run context in `bots/registry.py`, run ids in `/bots/`, `?run=` on `/quick-capture/`, Inbox banner, Automations bars as links. Tests: bots/tests/test_bots.py::TestRunLinks (2). Playwright: docs/screenshots/inbox-run-filter.png.

- **Template lint (2026-09-07, #422).** core/tests/test_template_lint.py: every template compiles; title/breadcrumb blocks carry no script/style.

- **Documents sparse state (2026-09-07, #421).** Dashed footer at ≤3 files with the page's purpose + Files link; empty-state upload CTA. Guard: core/tests/test_polish.py::test_documents_sparse_state. Playwright: docs/screenshots/documents-sparse.png.

- **⚠ CI not starting (2026-09-07, from run 139 on).** Desktop-release jobs fail within 4 s with no runner and no steps (run 139: the post-build prune/mirror jobs; runs 140–141: every job). Nothing in the workflow changed since run 138 (green); the code is unaffected (full suite 1181 green locally). This is the signature of the GitHub Actions minutes/spending limit on a private repo — owner action: check Settings → Billing → Actions on GitHub; the loop keeps committing, and the next push after the limit is lifted will build.

- **Narrow-width fixes (2026-09-07, #420).** 900 px audit: DocumentsTable scroller `relative`, manuscript columns `min-w-0`, Literature header `flex-wrap`. Audit clean at 900 dark / 1280 light desktop. Guard: core/tests/test_polish.py::test_narrow_width_overflow_fixes.

- **Pet habit signals (2026-09-07, #419).** Streak, unusual-hour and words-today lines in `core/pet.py::_speech_candidates`. Tests: core/tests/test_pet.py::TestHabitSignals (2).

- **Words written this month (2026-09-07, #418).** `core/dashboard.py::words_written_since`, `stats.words_written`, Dashboard six-cell stat row (→ /writing). Test: writing/tests/test_progress.py::test_dashboard_words_written_this_month.

- **MCP bots (2026-09-07, #417).** `list_bots`, `run_bot`, `toggle_bot` over the bots API (96 tools); README list + count. Tests: mcp_server/tests/test_docs.py.

- **Markdown vault export (2026-09-07, #416).** `projects/vault.py`, `GET /projects/{slug}/vault/` (zip; `?documents=0`), overview kebab item + ⌘K verb. Tests: projects/tests/test_vault.py (3). Verified: the demo vault downloaded and listed.

- **Achievements batch three (2026-09-07, #415).** Five seasonal secrets, Embrace the dark + two souls-only (Pet.souls_since, core 0009), the Platinum computed from the rest (95 total). Tests: core/tests/test_achievements.py::test_batch_three_seasonal_souls_and_platinum.

- **Studio comments (2026-09-07, #414).** `GET /manuscripts/{id}/comments/`, `DELETE /comments/{id}/`; Studio Comments tab (list/jump/delete, + line N, gutter click while open), `setCommentLines` marks per active file. Tests: writing/tests/test_studio_comments.py (2). Playwright: docs/screenshots/studio-comments.png.

- **Writing progress (2026-09-07, #413).** `writing.WordCountSample` (0014), `writing/progress.py`, tex-save hook + word-count endpoint records, `GET /manuscripts/{id}/progress/`, serializer `progress` (14-day deltas), MCP `get_writing_progress` (93 tools), Studio "+n today · streak", Writing card sparkline, demo seeded. Tests: writing/tests/test_progress.py (3). Playwright: docs/screenshots/writing-progress.png, studio-words-today.png.

- **Read this note to me (2026-09-07, #412).** `listen.ts::speakable` (markdown → speech text, checked in node), Notes editor listen/stop button with i/n progress, stops on note switch, errors in the toast. Guard: core/tests/test_polish.py::test_notes_read_aloud. Playwright: error path without a voice model (docs/screenshots/note-listen.png).

- **Reference usage (2026-09-07, #411).** `literature/usage.py`, `GET /references/{id}/usage/`, MCP `get_reference_usage` (92 tools), Reference page "Where it appears" section. Tests: literature/tests/test_usage.py (2). Playwright: docs/screenshots/reference-usage.png.

- **Files drag-to-move (2026-09-07, #410).** File rows draggable (`application/x-atlas-doc`), folder rows + root accept and call `moveDoc`; manuscript sources/folders excluded. Guard: core/tests/test_polish.py::test_files_rows_drag_to_move. Playwright: root file → folder → root, folder ids verified through the API (docs/screenshots/files-drag-move.png).

- **QueryBoundary (2026-09-07, #409).** `components/QueryBoundary.tsx` (`queryGate`, `QueryBoundary`); Reference, ReadingFlow, Automations, Graph, Prompts, Pet, Connect gated; Report + Diagnostics inline ErrorState. Guard: core/tests/test_query_boundary.py (every querying page has an error branch; allowlist for decorative queries). Playwright: API blocked → ErrorState with retry → recovers (docs/screenshots/query-gate-error.png).

- **Matrix gap hints (2026-09-07, #408).** Theme rows carry `read`; the header shows "n read" (green complete / neutral link / amber nothing-read) linking to the queue's `?theme=` candidates. Test: literature/tests/test_matrix.py::test_theme_rows_carry_read_counts. Playwright: docs/screenshots/matrix-gap-hints.png.

- **★ MENTIONS EVERYWHERE (2026-09-07, #407).** `core/rendering.py` (shared resolver + renderer; notes preview uses it), `*_html` companions on decisions / experiment entries / protocols / captures, `components/Prose.tsx`, Decisions clamp + expand, experiment entry expands, protocol body rendered, Inbox capture rendered; demo decision + entry carry mentions. Tests: core/tests/test_rendering.py (5), comments tests updated to the visible-gap contract. Playwright: mention in a decision → SPA navigation without reload (docs/screenshots/mentions-decisions.png, mentions-research.png).

- **★ WATCHED FOLDER (2026-09-07, #406).** `literature/watch.py` (15 s poll, ledger, import pipeline, optional project), `GET/POST /api/v1/watch-folder/` + `/scan/`, desktop launcher resumes it, Rust `pick_folder`, Library rail block. Tests: literature/tests/test_watch.py (3). Playwright: set a folder → scan now → imported (docs/screenshots/watch-folder.png).

- **AppImage retry (2026-09-07, #405) — red, reverted.** Run 126's Linux job: deb + rpm fine, AppImage `failed to run linuxdeploy`. Target removed again; #288 closed in DECISIONS. Same commit hardens `scripts/boot_check.py`: run 124's Windows job hung in `page.click` waiting for the post-login navigation (run 125 passed) — the click is now decoupled from the navigation (90 s `wait_for_url`, a login screenshot and the collected problems on failure).

- **Read-aloud chunked + prefetched (2026-09-07, #404).** `app/listen.ts` (sentence chunks, next chunk fetched while one plays, stop aborts); reading flow + reference page use it. Chunker checked with node; guard in literature/tests/test_neighbours.py.

- **Queue neighbours (2026-09-07, #403).** Row menu → "Similar in your library" panel (related endpoint, similarity %, + add here, explore beyond → Library lenses). Guard: literature/tests/test_neighbours.py.

- **Polish batch (2026-09-07, #402).** Active nav icon takes the accent; Files typeahead miss → red shaking hint with "no match"; #163/#162 retired as moot. Guard: core/tests/test_polish.py.

- **Calendar feed token (2026-09-07, #401).** `FeedToken` (migration core.0008), `GET/POST /api/v1/feed-token/`, the `?key=` authenticator takes the feed token for the feed only; the Dashboard copies the token URL and can rotate it. Test in core/tests/test_calendar.py (+1).

- **Smart views drag-to-reorder (2026-09-07, #400).** `POST /library-views/reorder/`, facets ordered by position, draggable rail rows. Test in literature/tests/test_tags_views.py (+1).

- **Access log (2026-09-07, #399).** `AccessEvent` (migration core.0007) fed by login/lockout/API-key hooks; `GET /api/v1/access-events/`; Diagnostics › Access + report line; excluded from the data version. Tests: core/tests/test_access_log.py (4).

- **Overview themes (2026-09-07, #398).** `themes` on the overview API (keywords across papers/notes/decisions/questions, weighted by source count), chip row under the counts linking to search. Test in projects/tests/test_overview_v2.py (+1).

- **Library cards view + toast position (2026-09-07, #397).** List/Cards toggle (remembered), cover-style cards with accent band and the same interactions; the unlock toast moved top-right so page flashes never sit under it. Playwright: toggled to cards, right-click menu on a card, screenshot (docs/screenshots/library-cards.png). Guard in test_tags_views.

- **Reader margin comments (2026-09-07, #396).** Bubble per page-anchored comment in a gutter beside each page, `+` on hover to comment on the page (prompt lists existing ones). Playwright: added a comment from the margin → marker with the text as tooltip (docs/screenshots/reader-margin-comments.png). Guard in literature/tests/test_tldr.py.

- **tl;dr per section (2026-09-07, #395).** The reading flow's `s` key now uses the same section tl;dr (PDF text first, abstract as fallback). Heading detection over the extracted PDF text, two sentences per section, page starts; `GET /references/{id}/tldr/`, MCP `get_reference_tldr` (91 tools), detail-pane block with p.N jumps. Tests: literature/tests/test_tldr.py (4).

- **Studio follow-cursor sync (2026-09-07, #394).** Setting "PDF follows the cursor" → debounced forward SyncTeX on every cursor line change. Guard in writing/tests/test_synctex.py.

- **Prompt defaults + remembered fill-ins (2026-09-07, #393).** `{{name|default}}` parsed on both sides, `variables` on the API, `render_prompt()`; the gallery shows defaults as placeholders and remembers typed values per prompt. Tests +3 in prompts/tests.

- **★ PLAN ORBIT (2026-09-07, #392).** Phases as arcs (status colours, the current one glowing with its progress), milestones as moons (done / overdue), counts under each arc, click → the phase card. Playwright both themes: 4 phases, 9 moons, no errors (docs/screenshots/plan-orbit.png, -light.png). Guard in core/tests/test_constellation.py.

- **⌘K safe verbs (2026-09-07, #391).** Copy project / library .bib to the clipboard, this week's review, quick capture, LaTeX warm-up, backup download, web inspector (desktop). Playwright: "bib" → Enter → "Copied N BibTeX entries" with the clipboard holding the export (docs/screenshots/palette-verbs.png). Guard in core/tests/test_today.py.

- **CI boot check: Windows green (2026-09-07).** Run 113's Windows job passed the smoke test *with* the browser boot check (dashboard, projects, library, diagnostics drawn against the frozen Windows server) and uploaded `boot-check-windows-latest`. The owner's blank window is therefore not the frozen server or the SPA on Windows Chromium — WebView2 or the update path remain; #382's panel/inspector will say which.

- **CI boot check on the frozen server (2026-09-07, #390).** `scripts/boot_check.py` runs in the release workflow on Linux and Windows against the frozen server (Tauri stub, four pages, fails on empty root / errors / panels); screenshots uploaded as `boot-check-<platform>`. Ran green locally against the dev server.

- **Desktop web inspector on demand (2026-09-07, #389).** `devtools` feature + `open_devtools` command; F12 / Ctrl+Shift+I, Diagnostics button, boot-panel button. Pins in core/tests/test_client_errors.py; cargo check green (see log).

- **Achievements batch two (2026-09-07, #388).** 29 more (86 total; 10 hidden), 16 new facts; tests +2 in core/tests/test_achievements.py; API count checked live (docs/screenshots/achievements-85.png).

- **★ CONSTELLATION HEADER (2026-09-07, #387).** The overview draws the project's papers + notes + links as a drifting sky (canvas, in-file layout, accent-tinted, hover title, click opens, caption + graph link, reduced-motion aware). Playwright in both themes: hover tooltip, click → /references/20, no console errors (docs/screenshots/overview-constellation.png, -light.png). Guard: core/tests/test_constellation.py.

- **Library Find PDF per row (2026-09-07, #386).** Row menu item (disabled with "needs a DOI"), "looking…" pill during the lookup, "no PDF found" pill with the outcome afterwards. Playwright: fetched for "Deep learning" → miss pill with the outcome tooltip; no-DOI rows show the disabled item (docs/screenshots/library-find-pdf.png). Guard extended in test_tags_views. Full suite after #384: 1124 passed, 1 skipped.

- **htmx + Alpine vendored (2026-09-07, #385).** The classic shell's last CDN loads now come from static/vendor; guard core/tests/test_no_cdn.py (2). The desktop draws every page offline.

- **ETag honesty (2026-09-07, #384).** Data version bumped by save/delete/M2M signals and folded into every ETag; M2M changes stamp updated_at on both sides; five bare `update()` sites fixed; AST guard `core/tests/test_etag_honesty.py` (4 tests) fails on the next bare `update()`. Full suite re-run after the signal change (see below).

- **Today drag-to-reorder (2026-09-07, #383).** Grip on hover, insertion line, optimistic order; `POST /todos/reorder/` (full order, bumps updated_at → ETag moves), keyboard reorder uses it too; `reorder_todos` MCP tool (90). Playwright: dragged the third item above the first → page and API agree (docs/screenshots/today-drag.png). Tests: core/tests/test_today.py (+1, wiring extended).

- **★ BLANK WINDOW, SECOND REPORT (2026-09-07, #382).** Owner: 0.1.106 (with the #379 hotfix) is still blank. Reproduced nothing: the SPA boots under the desktop settings (SQLite, DEBUG off, empty + seeded) and under a fresh PyInstaller freeze with a Tauri stub. Shipped the two things that hold regardless: (1) a boot watchdog in spa.html + `ErrorBoundary` (app + page scope) + global handlers, all posting to `POST /api/v1/client-errors/` → server log + Diagnostics "Front-end errors"; (2) `collectstatic --clear` on a version change and `WHITENOISE_MAX_AGE = 0` on the desktop, so an upgraded install can never serve a half-updated chunk set. Playwright: aborted spa.js → panel with "the app script failed to load" (docs/screenshots/boot-failure.png); a throwing Dashboard chunk → page boundary with the sidebar intact, recovered on navigation (render-failure.png); both on Diagnostics (diagnostics-client-errors.png). Tests: core/tests/test_client_errors.py (4). **Next report from the owner should carry the panel's text or the Diagnostics "Front-end errors" section.**

- **Library tag colours (2026-09-07, #381).** Right-click a rail tag → eight swatches / no colour / rename / delete; tinted chips on rows, detail pane and duplicate cards; `PATCH /library-tags/` validates the hex and rename uniqueness. Fixed on the way: tag/untag/rename/delete never moved `updated_at`, so the reference list ETag stayed put and the SPA showed stale tags (304s) — `touch_references` bumps the rows now (regression test). Playwright: tagged a paper → Rose from the rail → both chips tinted → renamed → deleted (docs/screenshots/library-tag-colours.png). Tests: literature/tests/test_tags_views.py (+2, guard extended). Full suite before this slice: 1113 passed, 1 skipped.

- **Hero to-dos + rank chip (2026-09-07, #380).** Dashboard API `todos` (top 4 open), tick in place, "n more" link; rank/score chip → /achievements. Test in test_dashboard_v2; Playwright ticked an item from the hero (docs/screenshots/hero-todos.png).

- **Audit in desktop mode (2026-09-07).** `scripts/ui_audit.py … desktop` injects a `window.__TAURI__` stub so desktop-only paths run in the sweep; run before every desktop-facing commit from now on.

- **★ HOTFIX — BLANK APP (2026-09-07, #379).** Owner: "the new update isn't even showing anything" (screenshot: bare aurora). Cause: warm-up hooks below the dashboard's early returns → React #310. Fixed Dashboard + a latent Today case; `core/tests/test_hook_order.py` walks every component for hooks after early returns. Verified: SPA boots with a `window.__TAURI__` stub (root mounts, sidebar renders). Builds 0.1.99–0.1.104 are affected; the next build carries the fix.

- **★ SYNCTEX (2026-09-07, #378).** Compile with --synctex, compact per-line map on the manuscript, API, Locate (⌘⇧J) with a marker bar, PDF double-click → file:line. Tests: writing/tests/test_synctex.py (5) + the real-compile test asserts the map. Migration writing.0013.

- **Studio to-do panel (2026-09-07, #377).** Markers across all files with click-to-line, count in the Outline tab; demo source carries a TODO and a FIXME. Tests: writing/tests/test_studio_todos.py.

- **★ RESTORE FROM A BACKUP (2026-09-07, #376).** Staged upload (validated), applied at the next desktop launch before the DB opens with the previous data kept beside it; Diagnostics section (restart / cancel / last result); `manage.py restore_backup`. Playwright: uploaded a real backup of the dev app → staged card with counts → cancelled (docs/screenshots/restore-staged.png). Tests: core/tests/test_backup.py (+4).

- **Release notes from commits (2026-09-07, #375).** The desktop-preview release body (and so the updater's notes dialog) now lists the last feat/fix/perf commit subjects; checkout fetches 40 commits for it. Scaffold test pins it.

- **★ ACHIEVEMENTS + SOULS MODE (2026-09-07, #374).** 57 achievements / 4 tiers / 8 hidden, progress + first-unlock timestamps, score & ranks, `/achievements` page, unlock toast, `get_achievements` (89 tools), Souls mode (grim speech, deaths/bonfires/bosses/souls, studio YOU DIED flash). Playwright: 57 cards, 8 unlocked on the demo, toast "First light", souls toggle → counters, pet page souls line (docs/screenshots/achievements.png, achievements-souls.png, pet-souls.png). Tests: core/tests/test_achievements.py (9), MCP client test; migration core.0006.

- **★ APP ICON + server threads (2026-09-07, #373).** Owner: "the app doesn't have a proper desktop icon" — it was a flat indigo square. `scripts/make_icon.py` renders the Observatory mark (navy rounded square, aurora, orbit ring, star) in pure Python; `tauri icon` regenerated every size (.ico has 6 sizes, .icns, PNGs). Owner's pasted 0.1.86 report also showed waitress queue-depth warnings → 8 threads, notice silenced; the media 404 in it is #365 (fixed in ≥0.1.90). Scaffold test pins the icon.

- **LaTeX warm-up, first-run card (2026-09-07, #372).** The empty-app Welcome panel offers "Prepare the LaTeX engine" (hidden once the cache is warm), polling while it runs; guard test in test_first_run.

- **LaTeX warm-up (2026-09-07, #372).** Cache detection per platform, background warm-up compile, Diagnostics row + button + polling, report text. Playwright with the real engine: clicked Warm up → "warming up…" → "ready · last warm-up 3s" (docs/screenshots/latex-warmup.png). Tests: writing/tests/test_warmup.py (6).

- **⌘K: `todo:` + recent jumps (2026-09-06, #371).** Playwright: `todo: PW palette item` created a project-scoped Today item; jumping to the Library then reopening showed it under Recent jumps (docs/screenshots/palette-jumps.png). Guard test in core/tests/test_today.py.

- **Updater polish (2026-09-06, #370).** Progress events + bar, release-notes confirm, 6-hourly re-check; cargo tests green; scaffold test pins the wiring. Unverifiable here (needs the public feed); the code paths are the plugin's documented callbacks.

- **★ CONNECT: TEST THE CONNECTION (2026-09-06, #369).** Four server-side checks with fixes, MCP `--check` self-test, real bug fixed (`-m mcp_server.server` exposed 29/88 tools — entry point moved to the end, AST test). Playwright: all four rows green in dev, 88 tools · 1 project (docs/screenshots/connect-test.png). Tests: core/tests/test_connect_test.py (5), mcp_server/tests/test_check.py (5).

- **Desktop: open with the system app / show in folder (2026-09-06, #368).** `local_path` in the tree (desktop only, tested both modes), `open_path`/`reveal_path` commands (cargo test: only existing regular files pass), Files menu items. Release run 90 (CRUD everywhere + async picker + media) built green on Windows and Linux.

- **Desktop compiles off the request thread (2026-09-06, #367).** `enqueue_compile` runs the compile on a daemon thread in immediate mode; tests `writing/tests/test_enqueue_compile.py`. Also: the desktop swallows the webview's own right-click menu outside text fields.

- **CRUD sweep, third pass (2026-09-06).** Library rows: right-click → open, read & highlight, reference page, find metadata, copy \\cite, delete. Files hides "Open in a new tab" on the desktop (no tabs there).

- **CRUD sweep, second pass (2026-09-06).** Documents table rows: right-click/⋯ → download, comments, rename, edit description, delete (no more exit to the classic edit page); Writing board cards: open, studio, move forward, shelve, delete (typed title). Playwright: uploaded → renamed → deleted a document from the table; card menu rendered; no console errors (docs/screenshots/documents-menu.png).

- **★ CRUD EVERYWHERE (2026-09-06, #364–#366).** Owner: can't delete a test project, left-click "doesn't have much functionality", "CRUD missing from the whole project", open-from-disk dead, *Missing PDF* after compile. Shipped: in-app dialogs (`components/Dialog.tsx`, no native prompts anywhere), context menus + ⋯ (`components/Menu.tsx`), project settings/archive/delete (typed name), Files right-click/F2/Del/upload-here/drop-on-folder, decisions/figures/prompts/literature/reference/research(questions, experiments, datasets, hypotheses)/plan phases/manuscripts edit+delete, `/media/` served with DEBUG off, async Tauri picker + "Add to this project". Playwright: project renamed → archived → deleted via typed confirm; folder created/renamed/deleted from the context menu; decision/prompt create→edit→delete; question ask→reword→delete; phase add→rename→delete; manuscript delete — no console errors (docs/screenshots/project-delete.png, files-context-menu.png, research-crud.png). Tests: `core/tests/test_crud_everywhere.py` (15), `test_media_serving.py` (3); suite 1050 passed. **Lesson logged:** never delete API rows from a list filtered by `?q=` unless the viewset declares `q_fields` — a blind `xargs DELETE` wiped the demo phases (re-seeded).

- **★ ONE-FILE BACKUP (2026-09-06, #363).** `/api/v1/backup.zip` (consistent SQLite copy + SQL dump or JSON dump, media, manifest, restore notes), Diagnostics button. Tests: zip contents + manifest, sqlite dump consistency.

- **Connect: tools on this machine (2026-09-06, #362).** claude / node / git / engine detection with versions, chips + install hint. Tests: detection with stubs, version never raises, API carries tools.

- **MCP +2 (2026-09-06, #361): `get_diagnostics`, `suggest_review_themes` — 88 tools.** Client tests + README list/count + skills updated.

- **★ CALENDAR FEED SERVED (2026-09-06, #360).** `/api/v1/calendar.ics` (all projects or `?project=`), `?key=` auth for calendar apps, dashboard subscribe button. Test: feed auth (none/wrong/key/session), scoping, content type.

- **★ FIGURES INTO THE MANUSCRIPT (2026-09-06, #359).** Studio: ⊕ on image assets inserts a figure environment; Project figures list copies a gallery image into `figures/` and inserts it; demo PNG seeded. Playwright: one click added `figures/…` to the file tree and `\includegraphics` to the source (docs/screenshots/studio-figures.png). CI smoke test of the frozen server passed on Windows and Linux (run 83).

- **★ NOTES EDITOR v2 (2026-09-06, #358).** CodeMirror Markdown editor with `[[`/`@` completions, formatting shortcuts, palette for both themes; textarea autocomplete removed. Playwright: `[[Load` offered note titles, `@lav` offered cite keys, both inserted, autosaved, preview linked (docs/screenshots/notes-editor-v2.png).

- **★ FROZEN-SERVER SMOKE TEST IN CI (2026-09-06, #357).** Release workflow boots the frozen server on Windows and Linux, checks login, diagnostics (bundled engine) and the SPA bundle before building the installer; scaffold test pins it. Watching the next run for the Windows result.

- **★ TODAY v2 + MATRIX SUGGESTIONS (2026-09-06, #356).** Today keyboard/inline edit/reorder/age chips/project links; matrix `suggest_themes` + API + chips. Tests: suggest ranking/skip-existing + API. Playwright: added, edited (e), deleted (x) an item; suggestions rendered as chips (docs/screenshots/today-v2.png, matrix-suggest.png).

- **★ EXACT HIGHLIGHT MARKS (2026-09-06, #355).** `Highlight.rects` + validation, reader captures selection boxes and paints an `.hl-layer`; service/API pass-through. Test: rects round-trip + validation. Playwright: dragged a selection, saved yellow, boxes painted and returned by the API (docs/screenshots/highlight-rects.png).

- **★ DIAGNOSTICS PAGE (2026-09-06, #354).** `/diagnostics` + `/api/v1/diagnostics/` (version, paths, engine, feed probe, last failed compile, log tail, copyable text). Tests: `core/tests/test_diagnostics.py`. Next: highlight rectangles; re-judge Today.

- **fix(writing): compile hardening for the desktop (2026-09-06, #353).** 900 s timeout with a first-run explanation, frozen-path engine lookup, engine path in the log, no console window on Windows. Owner reports handled this round: ACL refusal (#352), external links (#352), update feed (#347), first run (#351).

- **★ DESKTOP ACL FIX (2026-09-06, #353).** Owner: "terminal_spawn not allowed by ACL". Root cause: the capability lacked `remote.urls` for the http://127.0.0.1 origin the webview loads, so no Tauri command worked in the installed app (terminal, updater, file picker, external links). Fixed in `desktop/capabilities/default.json` + scaffold test; updater button explains the old refusal. Owner must reinstall once from the releases page (the old build cannot update itself).

- **★ DESKTOP OUTBOUND LINKS (2026-09-06, #352).** `desktop/src/external.rs` `open_external` (http/https/mailto only, OS browser) + `frontend/src/app/external.ts` capturing click handler installed by the layout; "get it manually" uses it. cargo check + Rust unit test; scaffold test. **Owner reports answered (20:40 UTC):** "check for update failed" = private repo 404 (#347 — needs the public feed or a public repo); "get it manually failed" = this fix; "Connect Claude Code removed the todo list" = the old build's classic exit (#342/#346 fixed it) — the installed app must be reinstalled once from the releases page because it cannot update itself yet.

- **★ FIRST RUN ON THE DESKTOP (2026-09-06, #351).** Login hint (desktop + default password), dashboard Welcome panel with "Load the demo project" (`/api/v1/demo/`), cascade-delete mirror bug fixed (seed_demo re-runnable; regression test), doctor: engine resolver + update-feed check. Verified on an empty desktop-settings instance: hint shown, welcome shown, demo loaded and the welcome disappeared (docs/screenshots/first-run-login.png, first-run-dashboard.png). Tests: `core/tests/test_first_run.py` (4). Next: highlight rectangles, matrix suggest-themes, re-judge Today/Dashboard.

- **★ INBOX KEYBOARD TRIAGE + MATRIX CSV (2026-09-06, #350).** Inbox cursor (j/k/arrows, ↵ suggested, 1–5 targets, f file, x dismiss, ? legend, hover syncs), matrix CSV download. Playwright: j moved the cursor, ? showed the legend, ↵ on a "todo:" capture created the Today item and removed the row, x dismissed the other, no console errors (docs/screenshots/inbox-keys.png). Next: highlight rectangles, matrix suggest-themes, then judge the Dashboard/Today again with fresh eyes.

- **★ QUOTES FROM HIGHLIGHTS (2026-09-06, #349).** Studio bibliography ✎ → per-paper highlights with "quote →" (`\begin{quote}… \citep{key}, p.~N`), Library highlight "quote" copy, `?quote=<id>` deep link. Playwright: opened a cited paper's highlights (1 row), inserted the quote (begin{quote} + citep present), deep link inserted and cleared its param, no console errors (docs/screenshots/studio-quotes.png). Next: matrix suggest-themes/CSV, inbox keyboard triage, highlight rectangles.

- **★ LIBRARY HEALTH + PROTOCOLS IN-APP (2026-09-06, #348).** `pages/Report.tsx` at `/projects/:slug/report` (four checker cards, merge duplicates in place, network checks on demand), `ProtocolPanel` in Research.tsx (create, open, new version), Literature links to the report, classic report URL mapped. Playwright: "Library health · 3 findings", 6 duplicate rows, 2 missing-field rows; wrote a protocol, revised it to v2, no console errors (docs/screenshots/library-health.png, research-protocols.png). Next: keep cycling — Library second pass (highlight rectangles, quotes into manuscripts), matrix suggest-themes/CSV, inbox keyboard triage.

- **★ AUTO-UPDATE DIAGNOSED + WIRED (2026-09-06, #347).** Signing works (`.sig` + `latest.json` on every build since 0.1.67) but the repo is private → the app's feed fetch 404s (curl-verified) and the silent launch check hid it. Shipped: `mirror` workflow job (installers + sigs + rewritten `latest.json` → public `RELEASES_REPO` via `RELEASES_TOKEN`), public feed first in `tauri.conf.json` endpoints, `UpdaterButton` shows "Updates unavailable — why?" with a plain explanation, README *Auto-update* section, scaffold tests. **Owner action needed:** create the public `atlas-releases` repo + two secrets, or make the repo public.

- **★ UI AUDIT PASS 1 SHIPPED (2026-09-06, #346).** 30-route Playwright sweep (overflow / tiny text / empty / JS errors / redirects) + eye review. Fixed: Projects index rewritten (status groups, phase + progress + health + counts cards, Markdown stripped; `ProjectSerializer.summary` + 2 API tests), `/projects/new` 404 spam guarded, five loop-back classic links replaced (Documents → Files, Files → Documents, Literature → matrix + explicit `?classic=1` bib report, Plan/Reference classic links removed), Connect Claude Code ported into the app (`/connect`, API + 1 test, "Run it here" on the desktop). `scripts/ui_audit.py` keeps the sweep reproducible. Re-sweep of the touched pages: no JS errors, no redirects. Next cycle: continue the loop (Library second pass ideas, matrix suggest-themes/CSV, inbox keyboard triage) and keep CI green.

- **★ MOCHI v2 SHIPPED (2026-09-06, #345): a living companion.** `pet/Creature.tsx` (cursor-tracking pupils, blink/breathe/twitch/flap/sleep, hop/celebrate/love/nom/think reactions with particle bursts, stage looks), `pages/Pet.tsx` at `/pet` (poke, voice line, week/streak/lifetime, stage road, stats, legend, achievements, rename), sidebar creature at 44 px → `/pet`; `core/pet.py` streak/achievements/legend/rename, `POST /api/v1/pet/`; classic `/pet/` maps to the app. Tests: `core/tests/test_pet_v2.py` (streak maths, state fields, rename API + validation). Playwright (dark + light): pupils moved to the cursor (`--pupil-x: 2.40px`), 4 stats, 10 achievements (2 unlocked), "55 points to sage · 31%", poke added `mochi-love`, sidebar creature present, rename round-trip, no console errors (docs/screenshots/pet-dark.png, pet-light.png). Next: the general UI audit (owner: "general UI has many flaws").

- **★ TERMINAL DOCK + CLAUDE SKILLS SHIPPED (2026-09-06, #344).** Global `TerminalDock` (⌃`, tabs, resize, maximize, Claude tab) in the layout; `terminal.rs` multi-PTY with ids + `terminal_kill`; Files page uses the dock. `mcp_server/skills/` (4 playbooks), `core/skills.py` list/install, Connect page section + `POST /connect/claude/skills/`. Tests: `core/tests/test_skills.py` (frontmatter + real-tool guard, install/upgrade, page + endpoint), terminal isolation guard still green. Playwright (browser): ⌃` opens/closes the dock, sidebar button opens it, Connect page lists 4 skills (docs/screenshots/terminal-dock.png, connect-skills.png). PTY behaviour itself is desktop-only (cargo check run locally). Next: the pet overhaul, then the general UI audit.

- **★ LATEX STUDIO SHIPPED (2026-09-06, #343).** `pages/Studio.tsx` at `/manuscripts/:id/editor` (full window: files / outline / bibliography / history sidebar, tabbed CM6 editor with the shared LaTeX core, pdf.js preview, problems panel, status bar, settings, ⌘P quick open, shortcuts; CSS-variable theme for both looks). Engine: `tectonic_path()` resolver (env → bundled bin → PATH), release workflow fetches Tectonic per platform and the PyInstaller spec bundles it; stalled-compile hint. Workbench `files/` bootstraps main.tex; `seed_demo` seeds a two-file manuscript. Tests: engine resolver (3) + bootstrap test; suite green. Playwright (dark + light, worker + local tectonic): opened the demo manuscript → 2 files, 4 outline items, 6 bib rows, typed → "Saved 07:47 PM", 264 words, Recompile → "compiled", 2 PDF pages rendered, 0 problems after dedupe, quick open 6 rows, no console errors (docs/screenshots/studio-dark-compiled.png, studio-light-compiled.png). Next: the global terminal panel + Atlas skills for Claude Code (owner ask), then the pet, then the UI audit.

- **★ ONE FRONT DOOR SHIPPED (2026-09-06, #342): the "jumps back to the old UI" bug.** `core/spa_routes.py` + `core/ui_middleware.py` redirect browser GETs for classic pages to their SPA twin; `?classic=1` / `/classic/` opt in via cookie; classic banner with "Back to the app"; sidebar classic link removed; links.ts gains references/manuscripts/project-writing maps. Tests: `core/tests/test_front_door.py` (10 tests incl. TS/Python map agreement). Playwright: `/library/` → `/library`, `/projects/…/writing/1/` → `/manuscripts/1`, `/pet/` shows the banner, "Back to the app" clears the cookie, zero `/classic/` links in the SPA (docs/screenshots/classic-banner.png). Next: the in-app LaTeX studio (the editor is the biggest remaining classic exit) + Tectonic bundled in desktop builds.

- **★ RESEARCH v2 SLICE 1 SHIPPED (2026-09-06, #341): the hypothesis ledger.** Writable research viewsets + `/evidence/` resource, nested evidence with tallies and `suggested_status`, evidence writes bump the hypothesis ETag (regression test `test_evidence_changes_bust_hypothesis_etag`), MCP `add_hypothesis`/`set_hypothesis_status`/`add_evidence`/`log_experiment` (86 tools). Research.tsx rewritten (propose box, cards with balance bar, inline evidence form with paper/note suggest, experiment log, datasets). Tests: 3 ledger tests + client tests; suite 995 green. Playwright: proposed a hypothesis, attached "@lavie2010attention" evidence, card showed "evidence says supported →", logged an experiment (docs/screenshots/research-v2-dark.png). **Owner report (19:30 UTC) sets the next loop:** (1) the SPA still exits into the classic UI (LaTeX editor, /pet/, unmapped ⌘K/search results) and classic has no way back — that is the "jumps back to the older UI, todo list disappears" bug; (2) the LaTeX editor must become a real in-app studio (and the desktop build ships no Tectonic, so Recompile fails there); (3) a VS-Code-style terminal panel + Atlas skills for Claude Code; (4) the pet needs a real UI; (5) general UI flaws.

- **★ REVIEW MATRIX v2 SHIPPED (2026-09-06, #339): the extraction table.** `literature/matrix.py` (`add_theme`, `resolve_theme`, `set_mark`, `matrix`, `matrix_markdown`), API (`table` on review-matrix, theme add/rename/delete, `mark`, `markdown`), MCP `set_review_mark` + `add_review_theme` (82 tools). `pages/Matrix.tsx` at `/projects/:slug/matrix` (+ overview quick link): sticky header with coverage bars, click-to-mark, click-to-type finding, right-click clear, theme rename/delete, filter, Copy as Markdown, .md, Draft synthesis note. Tests: 3 matrix tests + client test; suite 991 green. Playwright: added a theme, marked a cell, typed "n = 12, dual task", copied Markdown with the new column, deleted the theme (docs/screenshots/matrix.png). Next: judge the Library second pass; then consider a "suggest themes" helper and CSV export; then the Research page (hypotheses/experiments) which has had no v2 pass.

- **Updater is live (2026-09-06 19:05 UTC).** The owner added `TAURI_SIGNING_PRIVATE_KEY`: run 67 published `Atlas_0.1.67_*` installers *with* `.sig` files and `latest.json` on the desktop-preview release, so installed apps now update themselves. Light-theme ("Paper") sweep of the rewritten pages (dashboard, library, plan, notes, overview, writing, inbox, search): all render correctly, no page errors.

- **★ SEARCH v2 + REFERENCE PARITY SHIPPED (2026-09-06, #337).** `core/search.py`: `excerpt`, `describe` (snippet / page / where / app_url / meta per kind); `/search/` returns them + `project_name` (MCP `search` gains them). Search.tsx rewritten (Observatory: `?q=` sync, grouped by kind, marked terms, PDF page chips, keyboard). Reference.tsx: Cite block, Highlights section, "open in the Library", PDF iframe → "Read & highlight →" deep link; Library.tsx honours `?q=` and `?read=<id>` (opens the reader once rows arrive). Tests: 2 search v2 tests; suite green (987 + 2 skipped). Playwright: "dissociation" → Papers group with "in the PDF · p.1" and the marked snippet; Enter opened the reference; its Read button opened the Library reader (docs/screenshots/search.png).

- **docs(readme): front page rewritten (2026-09-06).** Hero with the download link, two badges, a corrected pitch (React islands are prebuilt; Node only for island development), a 14-shot screen grid with matching captions, and "What's inside" regrouped per area (Plan / Library / Notes & graph / Writing / Overview-dashboard-inbox-today / Claude) instead of run-on bullets; comparison table gains `@key` notes and reviewer/budget rows. Guard tests (MCP tool list + count, desktop scaffold, smoke) green. Inbox v2 judged done after slice 1; the next loop starts with a second pass over the Library.

- **Current feature area: Inbox (Inbox v2).** Dashboard v2 judged done after slice 1.
- **★ INBOX v2 SLICE 1 SHIPPED (2026-09-06, #335): smart capture triage.** `notes/capture.py` (`detect`, `convert` for paper/note/todo/milestone/decision), serializer `hint`, `ConvertCaptureSerializer`, `POST /quick-capture/{id}/convert/`, `?processed=` filter, MCP `list_inbox` + `convert_capture` (80 tools). Inbox.tsx rewritten (Observatory): live hint while typing, ⌘Enter, rows with DOI/arXiv/link chips and one-click targets (suggested highlighted), project select, File, Dismiss, optimistic removal, toast with open link. Tests: 4 capture tests (hints, four conversions + errors, paper via identifier, API + filter) + client test; suite 994 green. Playwright: captured a DOI line → "Looks like a paper", row chip "doi 10.1038/nature14539" with Paper highlighted; "todo:" capture → Today (toast "On today's list"), dismissed the other (docs/screenshots/inbox.png). Next: judge the Inbox (ideas: bulk triage, keyboard j/k + 1-5 targets, capture from the ⌘K palette with hint) — then loop back to the Library with fresh eyes (highlight rectangles, quotes into manuscripts).

- **Current feature area: Dashboard (Dashboard v2).** Overview v2 judged done after slice 1.
- **★ DASHBOARD v2 SLICE 1 SHIPPED (2026-09-06, #333): this week, everywhere.** `core/dashboard.py`: `week_everywhere` (cross-project overdue + due-within-7-days milestones/tasks with project colour/phase), `project_health`. API `/dashboard/` gains `week`, `todos_open`, `heatmap`, per-project `health`; MCP `get_dashboard` (78 tools). Dashboard.tsx: WeekEverywhere panel (tick-to-complete, optimistic), health pills on active projects, "on today's list" tile → /today, Heatmap (26 weeks, month labels, hidden in calm mode). Also: the overview description now strips Markdown markers. Tests: 2 dashboard v2 tests + client test; suite 989 green. Playwright: "1 overdue · 1 due", health pill "behind — 1/3 done, 66% of time used", 189 heatmap cells (docs/screenshots/dashboard.png). Next: judge the Dashboard (ideas: venue overruns everywhere, reading streak, "papers to read today" from the queue) — then Inbox/capture, then loop back to the Library with fresh eyes.

- **Current feature area: Project overview (Overview v2).** Writing v2 judged best-in-field after slice 3.
- **★ OVERVIEW v2 SLICE 1 SHIPPED (2026-09-06, #331).** `projects/overview.py` (`week_digest`, `open_questions`, `manuscripts_glance`, `hypotheses_summary`) folded into `GET /projects/{slug}/overview/` (so MCP `get_project_overview` gains them too). `ProjectOverview.tsx` rewritten (Observatory): ring + gradient count header with quick links, current-phase card with health/objective/window, compact This-week strip, digest / questions / manuscripts row, linked count tiles, next milestones, recent documents/decisions. Tests: 3 overview tests (+ API keys); suite 986 green. Playwright: demo overview rendered "behind — 1/3 done, 66% of time used", digest "22 papers added · 4 notes · 4 documents · 3 milestones done …", 2 questions with status, manuscript with "18 d left" (docs/screenshots/overview.png). Next: judge the Overview (ideas: activity sparkline per project, pinned docs) — then the Dashboard (cross-project this-week, heatmap, deadlines everywhere, venue overruns).

- **★ WRITING v2 SLICE 3 SHIPPED (2026-09-06, #329): the venue budget.** `Manuscript.venue_limits` (migration 0012), `writing/budget.py` (`clean_limits`, `usage`, `budget` with ok/near/over states, PDF page count after a compile), serializer validation, API `GET /manuscripts/{id}/budget/`, MCP `get_manuscript_budget` + `set_venue_limits` (77 tools). UI: BudgetCard (limits form for six keys, colour bars, "n over" labels, summary with venue). Tests: 3 budget tests (+ client test); suite 983 green. Playwright: set limits on the demo manuscript → "over the limit on references · JEP:G", References 6 / 5 · 1 over, bars rendered; limits reset afterwards (docs/screenshots/writing-budget.png). Writing v2 after three slices: studio, reviewer-response tracker, venue budget — next: judge against Overleaf/Paperpile (remaining: embed the editor in the studio, venue library, figure gallery link) and then move to Project overview / Dashboard.

- **★ WRITING v2 SLICE 2 SHIPPED (2026-09-06, #327): reviewer-response tracker.** `writing/reviews.py`: `parse_review_points` (reviewer headings, numbered/bulleted points, continuation lines), `response_markdown`, `log_reviews` (event + note, unique titles, `[[note]]` link in the event), `response_progress` (per manuscript). API `POST /manuscripts/{id}/reviews/` (`ReviewsInSerializer`), `GET /manuscripts/{id}/response-progress/`; events/bibliography/reviews actions now `_touch_manuscript` so the detail ETag changes (stale-304 bug fixed, regression test `test_related_changes_bump_the_manuscript_etag`). MCP `log_reviews`, `get_response_progress` (75 tools). UI: reviews paste box when "Reviews received" is chosen, response-progress card with bar + link, submission .zip link on the compile card. Tests: 4 reviews tests (parser, log + progress + second round, empty scaffold, API) + ETag test + client test; writing suite green. Playwright: pasted two reviewers' points → "0/4 points answered" card, note scaffold opened in the notes workbench, timeline shows the new event after the ETag fix (docs/screenshots/writing-reviews.png).

- **Current feature area: the Writing studio (Writing v2).** Notes + graph judged best-in-field after slice 3.
- **★ WRITING v2 SLICE 1 SHIPPED (2026-09-06, #325): the manuscript studio.** API (ManuscriptViewSet): `bibliography` GET/POST, `DELETE bibliography/{reference_id}`, `cite-check` (all .tex files or latex_source; `resolvable` map), `bib` (text/x-bibtex), `events` POST, `DELETE events/{id}`; `_bibliography_rows` helper; input serializers `ManuscriptReferenceInSerializer`, `SubmissionEventInSerializer`. MCP: 5 manuscript tools (73 total). UI: `Writing.tsx` rewritten — board (gradient count, new-manuscript form with project select, deadlines-within-two-weeks strip, glass columns) and studio (title/venue/deadline inline edit, status pipeline, AbstractCard, CompileCard with polling while running, BibliographyCard with library search + add/remove + cite copy + .bib, CiteCheckCard with one-click add, TimelineCard). Tests: 4 studio API tests (bibliography add/list/override/remove/bib, cite check over files + fallback, events add/validate/delete, create) + client test; suite 973 green. Playwright: opened the demo manuscript (6 bib rows, cite check listed 6 uncited), added a paper from the library search (6 → 7) and removed it, logged and deleted an event (docs/screenshots/writing-studio.png). Next Writing slices: submission .zip + arXiv-ready export in the studio; reviewer-response tracker (reviews_received → point-by-point response note); word/figure budget vs venue; embed the editor.

- **fix(css): Markdown "prose" styles were never built (2026-09-06).** `assets/css/app.css` now loads `@plugin "@tailwindcss/typography"` (bundled in the standalone Tailwind CLI) — headings, block quotes, lists, code and links in the notes preview and every classic Markdown surface were rendering unstyled. Verified on the literature-note preview; observatory/workbench guard tests green.

- **★ NOTES v2 SLICE 3 SHIPPED (2026-09-06, #323): templates + export with a bibliography. Notes + graph judged best-in-field; next area: Writing studio.** `notes/templates.py`: `TEMPLATES`, `render_template` (literature from a reference with highlights; daily from `week_focus`; meeting; experiment; blank), `create_from_template` (unique titles, daily returns today's), `export_note` (body + References via `bibliography`). API `/notes/templates/`, `/notes/from-template/` (`NoteFromTemplateSerializer`), `/notes/{id}/export/`. MCP `create_note_from_template`, `export_note` (68 tools). UI: template cards on the New-note pane (literature card with inline paper search), "export" in the editor toolbar (clipboard + toast), Library detail "Note" button (literature note in the paper's project, then navigates). Tests: 5 template/export tests (+ API) + client test; suite 968 green. Playwright: Library → Note created "Lavie 2010 — Attention, Distraction…" with @key, metadata, DOI and a Highlights section; export copied Markdown ending in an APA References entry; Daily note "2026-09-06" listed 3 focus items with lateness (docs/screenshots/notes-literature.png).

- **★ NOTES v2 SLICE 2 SHIPPED (2026-09-06, #321): Graph v2.** Vendored `static/vendor/forcegraph/{3d-force-graph,force-graph}.min.js` (+ README with versions/licence); classic template and `Graph.tsx` load from `/static/vendor/…` (guard test `test_graph_libraries_are_vendored_for_the_desktop`). `core/graph.py`: node facts (year, venue, authors, citations, has_pdf, highlights, words, updated_at, degree, app_url) + `stats` (references, notes, links, by_kind, orphans, hubs). Graph.tsx rewritten: toolbar (search + match count + Enter focuses, kind + link-kind toggles, hide unconnected, focus chip with widen/narrow), hubs card, node panel (facts, Open, Focus, neighbours), hover dimming, 3D fly-to, particles on citations, theme-aware canvas, 2D toggle remembered, Esc clears. Fixed a page crash (library cleared React-owned children inside the mount div → mount div is now childless; overlays are siblings). Tests: graph facts/stats + vendoring guard; api shape test updated; suite 962 green. Playwright (swiftshader WebGL): vendored script requested, hubs listed, clicked a hub → panel with 18 neighbours + focus chip, search "load" → 3 matches, 2D canvas rendered (docs/screenshots/graph3d.png). Next: judge Notes + graph (ideas: note templates — literature note / meeting / experiment; daily note; export a note with a bibliography; graph time-slider by year) — then Writing studio.

- **Current feature area: Notes + knowledge graph (Notes v2).** Plan v2 judged best-in-field after slice 4.
- **★ NOTES v2 SLICE 1 SHIPPED (2026-09-06, #319): the notes workbench with @citations.** `notes/services.py`: `CITE_RE`/`parse_cite_keys`, `sync_note_references` (additive, case-insensitive), `note_links` (outgoing/backlinks/references/unresolved/unresolved_keys/mentions), `suggest` (notes or project references, prefix-first). API: create/update sync links + references; `/notes/{id}/links/`, `/notes/suggest/`, `/notes/unwritten/`; preview renders `@key` → paper link (unknown → italic); `references_detail`. MCP: `list_notes`, `get_note`, `update_note`, `get_note_links` (66 tools). UI: `Notes.tsx` rewritten (Observatory): list rail with search + unwritten stubs (one-click create), NewNote pane (title prefill from `?title=`), Editor (title + body textarea | live preview, autosave, ⌘S, Tab indents, delete), autocomplete popover for `[[` and `@` (↑↓ Enter Tab Esc, "new note" row for unknown titles, client-side ranking), link panel. One lazy component for all three routes. Tests: 5 notes link tests + client test; suite 960 green. Playwright: created a note, `[[Load` → picked "Load theory overview", `@lav` → picked @lavie2010attention, preview linked both, panel showed Cites / Links out / unwritten stub, deleted the note (docs/screenshots/notes.png). Next: Graph v2 (vendor 3d-force-graph for the desktop, filters by kind/status, focus a node's neighbourhood, search-to-focus, richer side panel, note→reference edges from @keys).

- **★ PLAN v2 SLICE 4 SHIPPED (2026-09-06, #317): phase context + questions + keyboard reschedule. Plan judged best-in-field; next area: Notes + knowledge graph.** Plan API: per-phase `objective`, `target_start`, `target_end`, `questions`; top-level `questions`. UI: context block under each phase header (objective click-to-edit with blur save / Esc cancel, question chips by status with detach on hover, "+ research question" attach select), date inputs in the header (sm+), Roadmap bars/diamonds focusable with ←/→ (day) and Shift (week) nudges. Test: `test_plan_api_carries_phase_context` (+ attach through /questions/); suite 954 green. Playwright: 4 context blocks, objective edit → one PATCH, Shift+→ moved a phase a week and back (docs/screenshots/plan-context.png). Parked: question creation on the plan, natural-language dates.

- **★ PLAN v2 SLICE 3 SHIPPED (2026-09-06, #315): this week + the milestone drawer.** `plans/focus.py` `week_focus(project, today)` → overdue / due_this_week / next_up (≤3 from the current phase, skipping listed ones) / current_phase, items carry kind, days-until-due, phase and milestone context. API `GET /projects/{slug}/focus/`; overview gains `focus` + `health` (state/label/forecast_end/start/end of the current phase); plan API returns milestone `notes`. MCP `get_week_focus` (62 tools). UI: `pages/plan/Focus.tsx` (strip with three columns, tick-to-complete with optimistic removal, compact mode on the overview, empty state), `pages/plan/MilestoneDrawer.tsx` (title/due/notes autosave 700 ms, mark complete, tasks CRUD, delete with confirm, Esc closes, opaque `.drawer-solid`), Plan milestone titles open the drawer and show a "notes" hint; overview shows the health pill next to the current phase. Tests: 4 focus tests (buckets/order/days, next_up cap, empty, API + overview embed) + client test; suite 953 green. Playwright: strip showed 1 overdue (3 d late) / 1 due / 1 next; drawer saved a note (badge appears after refetch); overview health "behind — 1/3 done, 66% of time used" (docs/screenshots/plan-focus.png, overview.png). Next: judge the Plan (remaining ideas: keyboard reschedule on the roadmap, natural-language dates in quick-add, phase objective inline, research questions ↔ phases on the Plan page) — then Notes + knowledge graph.

- **★ PLAN v2 SLICE 2 SHIPPED (2026-09-06, #313): the roadmap.** `plans/roadmap.py` — `project_roadmap(project, today)`: windows (real or inferred: previous end + 1 / milestone dues / 6 weeks), milestones (due, done, overdue), health (behind / on_track / ahead / blocked / overdue / upcoming / done / empty with a reason line), forecast_end from the pace between the first and last completed milestones, range_start/end. API `GET /projects/{slug}/roadmap/`; MCP `get_roadmap`, `set_phase_dates` (61 tools). UI: `pages/plan/Roadmap.tsx` — Phases | Roadmap | Outline tabs on the Plan page (remembered), month header + gridlines, today line (accent glow) auto-scrolled into view, health-coloured bars with progress fill, dashed suggested bars, striped forecast tail, milestone diamonds; pointer-drag move/resize of phases and slide of milestones → PATCH on release; status strip with hover details. Tests: 5 roadmap tests (inference, all health states + forecast maths, range, empty, API) + client test; suite 948 green. Playwright: 4 bars / 6 diamonds / today line rendered, dragged "Data collection" +7 days → one PATCH with the new dates, dragged back (docs/screenshots/plan-roadmap.png). Next Plan slices: "this week" focus strip (milestones/tasks due in 7 days, overdue first) on the Plan and Overview; phase objective + notes visible inline; milestone notes/detail drawer; keyboard reschedule.

- **Current feature area: the Plan (Plan v2).** Library v2 judged best-in-field after slice 8 (see DECISIONS #311); the cycle moved on.
- **★ PLAN v2 SLICE 1 SHIPPED (2026-09-06, #311): the plan as a document.** `plans/outline.py` — `plan_to_markdown` (always writes `{#id}` tokens), `parse_outline` (loose syntax: `#` phases with `[status]` aliases and `(start → end)`, `>` objective lines, `- [ ]`/`- [x]`/plain bullets, `(due …)`, two-space or tab indent for tasks; every problem reported with its line), `preview` (created/renamed/deleted lists + counts), `apply` (transactional; keeps ids, completion stamps and notes; reorders by position; deletes what disappeared). API `GET/POST /projects/{slug}/outline/` with `dry_run` (400 + per-line errors on parse failure); MCP `get_plan_outline`, `set_plan_outline` (59 tools). Plan.tsx rewritten (Observatory): ring + gradient count + overdue pill, glass phase cards (numeral, status pill cycles not_started→in_progress→blocked→done, glowing accent bar, check-off with glow, "+ task" on hover, "New milestone… Enter to add"), outline editor split view with "This save would" panel and syntax card, empty state opens the editor with a starter outline. Tests: 6 outline tests (export shape, no-op round trip, loose syntax + errors, create/rename/reorder/delete, completion stamp kept, API) + client test; suite 942 green. Playwright: quick-added a milestone, renamed it and added a phase through the outline (preview listed + create 3 / ~ rename 1), saved, cleaned up (docs/screenshots/plan.png, plan-outline.png). Next Plan slices: timeline/Gantt of phases with drag-to-reschedule and a today line; "this week" focus; velocity forecast per phase; Plan parity on the Overview.

- **★ LIBRARY v2 SLICE 8 SHIPPED (2026-09-06, #309): search inside your PDFs.** `literature.ReferenceText` (migration 0007; pages JSON + body + source_name + error) filled by `literature/fulltext.py` (`extract_text` via pypdf, caps 400 pages / 20k chars per page, never raises; `needs_extraction`; `index_missing`; `search_pages` → page + snippet; `search_library`; `pdf_match_filter`); `literature/signals.py` re-indexes on every Reference save with a new PDF (huey task, immediate on desktop/dev) and drops the text when the PDF goes; `manage.py index_pdf_text [--all]`. `filter_references(q=)` now also matches the PDF body and annotates `pdf_match`; global search includes `text__body` on both backends. API: `/references/text-search/`, `/references/{id}/text-search/` (distinct operation id), `/references/{id}/index-text/`, `pdf_match` + `text_status` on the serializer. MCP: `search_pdf_text`, `search_in_pdf` (57 tools). UI: "in PDF" badge on matching rows, "Found in the PDF · “q”" block in the pane (page + snippet → opens the reader on that page with the term), reader find bar (Enter walks pages, ◂ ▸, Esc clears; matches painted violet on the text layer), text status line with Index now / Retry. Tests: 7 fulltext tests (signal, corrupt PDF, snippets, filter annotation, global search, command, API) + client test; suite 935 green. Playwright: searched "dissociation" → 1 row with the badge → pane hit p.1 → reader opened with 1 painted span and "1/1 pages" (docs/screenshots/library-find.png). Next: judge the Library once more (remaining ideas: highlight rectangles, push highlights/quotes into a manuscript, PDF text in the global Search page results as snippets) — then move on to the Plan.

- **★ LIBRARY v2 SLICE 7 SHIPPED (2026-09-06, #307): read and highlight without leaving the workbench.** Model `literature.Highlight` (migration 0006; reference, project?, page, text, comment, colour) + `literature/reading.py` (`add_highlight` validates, mirrors into the project highlights note, bumps to_read → skimmed; `highlights_markdown`; `reading_notes`); the classic reader's save endpoint now writes structured rows too. API: `/highlights/` viewset (`?reference=`, `?project=`, PATCH comment/colour, DELETE), `GET /references/{id}/highlights-markdown/`, `GET /references/{id}/reading-notes/`, `POST /references/{id}/fetch-pdf/`. MCP: 6 reading tools (55 total). UI: `pages/library/PdfReader.tsx` — pdf.js from the vendored build via dynamic import, lazy page render with IntersectionObserver, zoom (+/-), page counter, "highlights → project" select (defaults to the paper's only project), selection → floating colour bar, saved highlights painted onto the text layer by text match, Esc closes; detail pane gains Highlights (page jump, inline comment, recolour, delete, copy as Markdown), Reading notes (per project, autosave), Read/Reading button, Find PDF when there is none; `o` opens the reader in place. Tests: 7 reading tests (service + API + legacy view), client test; full suite 927 green. Playwright: selected text on the demo PDF, saved green, painted 3 spans, listed with p.1 (docs/screenshots/library-reader.png). CI: asset pruning moved to a post-build job (owner hit an empty release mid-run). Next: judge the Library against Zotero/Paperpile (#306 remaining: highlight rectangles, push highlights into a manuscript), then move to the Plan.

- **★ LIBRARY v2 SLICE 6 SHIPPED (2026-09-06, #305): duplicates → merge.** `library.duplicate_groups` (union-find over title-similarity findings + arXiv ids; completeness-scored suggested keep) and `library.merge_references` (transactional move of project links w/ best-state merge + review marks, tags, note links, manuscript refs, evidence, citation edges deduped, comments, PDF file, empty-field fill, `merged_from` trail; releases the merged DOI/file first). API `GET /references/duplicates/`, `POST /references/merge/`; facets `duplicates`; MCP `find_duplicates` + `merge_references` (49 tools). UI: "Duplicates" chip in the rail (amber count) switches the centre pane to grouped cards with keep radios and "Merge into the selected"; merged result opens in the detail pane. Tests: 6 merge/cluster tests (every relation asserted), endpoint test, client test, UI wiring. Also: CI deletes stale draft copies of the preview release. Next (#306): inline PDF preview or reading notes in the pane; then judge the Library.

- **★ IN-APP UPDATES LIVE (2026-09-06, owner request, #303).** Keypair generated with `@tauri-apps/cli signer generate` (public key committed to tauri.conf.json; the private key was handed to the owner for the `TAURI_SIGNING_PRIVATE_KEY` secret — the one step code can't do). desktop-release.yml: flips `createUpdaterArtifacts` on only when the secret exists (builds stay green either way), passes the signing env to tauri-action, publishes desktop-preview as a prerelease (was a draft → unfetchable), prunes older installers from it. Updater endpoint → `releases/download/desktop-preview/latest.json`. Rust: `check_update` (silent) + `install_update` + `restart_app`; `cargo check`/`cargo test` green. UpdaterButton: silent check on launch → "Update to x.y.z" → Installing… → Restart to finish; manual "Check for updates"; error state links to the release page. Doctor wording updated. Guard test `test_in_app_updates_are_live_wired`. Remaining owner action: add the secret (desktop/README "In-app updates").

- **★ LIBRARY v2 SLICE 5 SHIPPED (2026-09-06, #301): tags and smart views.** Models `LibraryTag` (case-insensitive unique via `get_or_create_named`) + `SavedView` (migration literature 0005), admin; `library.py` gains `tag=`/`untagged=` filters, tag/untag bulk actions, facets `tags`/`untagged`/`views`; `ReferenceSerializer.tags` (write-only list of names → created on the fly; read via to_representation); viewsets `library-tags` (counts, case-insensitive reuse on POST) and `library-views` (positions append); MCP `list_library_tags`, `tag_references` (47 tools). UI: rail "Smart views" (+ save when filtering, one-click restore incl. search text, delete on hover) and "Tags" (counts, Untagged), tag chips on rows, "tag…" input with datalist in the bulk bar, chip editor in the detail pane. Tests: 6 tag/view tests + client test; live: tagged 6 demo papers, saved "To cite · recent" (tag + year_min), restored it from the rail in a screenshot. Next: judge the Library (#302 list).

- **★ TODAY LIST SHIPPED (2026-09-06, owner request, #299).** `core.TodoItem` (migration 0005), admin, `TodoItemViewSet` at `/api/v1/todos/` (done/project/q filters, positions append at the bottom, PATCH done stamps done_at, `POST /todos/clear-done/`), MCP `list_todos`/`add_todo`/`complete_todo` (45 tools), SPA page `/today` (Observatory: gradient count in the title, glass input with Enter-to-add and focus kept, optimistic tick with a glowing check, Done section with Clear done, empty state), sidebar "Today" entry right under Dashboard, `/today/` classic URL mapped to the SPA, seed_demo adds three items. Tests: 6 API/model/page tests + client test.

- **★ LIBRARY v2 SLICE 4 SHIPPED (2026-09-06, #297): formatted citations.** `literature/citations.py` — `cite(ref, style)` → {text, html, intext} and `bibliography(refs, style)` (sorted/numbered) for apa/mla/chicago/harvard/vancouver/ieee, author-count rules per style (APA 20/et al., MLA 3+, Vancouver 6, IEEE 6, Harvard 3), n.d./no-author/proceedings fallbacks, HTML escaping + italics. Crossref/OpenAlex mappers now store volume/issue/pages in extra. API: `/references/{id}/cite/`, `/references/cite/?ids=&style=`. MCP: `format_citations` (42 tools). UI: Cite block in the detail pane (style select persisted, rendered preview, Copy citation, in-text copy button) + "Copy citations" in the bulk bar. Tests: 9 citation tests (exact strings per style), API + client tests. Live: `/references/26/cite/?style=apa` rendered LeCun, Bengio & Hinton (2015) with the DOI. Next: judge the Library — remaining gaps: tags + smart views, duplicate merge, inline PDF preview.

- **★ LIBRARY v2 SLICE 3 SHIPPED (2026-09-06, #295): grow the library from any paper.** `literature/discover.py` rewritten: `discover(reference, kind, limit, client)` with kinds similar / references / cited_by on OpenAlex (batched id lookups, `cites:` filter sorted by citations), rows annotated with `in_library`/`library_id`/`addable` in one query, OpenAlex id remembered on first resolution; the classic panel's `discover_similar` keeps its shape on top of it. `library.export_bibtex` + bulk `fetch_pdf`. API: `GET /references/{id}/discover/?kind=&limit=`, `GET /references/export/?ids=|<filters>` (application/x-bibtex). MCP: `discover_related`, `export_bibtex` (41 tools, README updated). UI: detail pane "Discover beyond your library" with three lens tabs, per-row + Add (files into the current project filter) / in library / OpenAlex link; bulk bar gains Export .bib, Copy BibTeX, Fetch OA PDFs; list header gains ".bib of this view". Tests: 8 lens/export/bulk tests (MockTransport routes), 2 API tests, 2 client tests. Live check: added 10.1038/nature14539 by DOI; the "It cites" lens returned real rows (LeCun 2015 → NeurIPS 2018, OverFeat 2013, Rumelhart 1986…) through the per-work fallback because OpenAlex's daily list budget for this network was spent; "Cited by" showed the amber budget explanation with the retry time; export served BibTeX. OpenAlex meters list queries per day now → DiscoverError + per-work fallback + optional ATLAS_OPENALEX_API_KEY (base.py, .env.example). Next: judge the Library (#296 candidates).

- **★ LIBRARY v2 SLICE 2 SHIPPED (2026-09-06, #294): the workbench.** Library.tsx rewritten as a three-pane Observatory workbench: rail (search, sort, projects with colour dots + Unfiled, reading status when a project is active, clickable **year histogram**, Files: has PDF / no PDF / needs metadata, type, venues) | dense list (checkbox multi-select, project dots, PDF/needs-metadata badges, citation counts, keyboard j/k/enter/x/o, load-more) | detail pane (abstract, Open / Read PDF / DOI / cite-key copy, "in projects" with status + file-into-project, related-in-library, **Find metadata** for PDF stubs). **Whole-page drop zone** (any mix of .pdf/.bib/.ris/.json) + import panel (choose files, paste text, link-to-project) with per-item results; "Import from Zotero" button. Bulk bar: link to project, mark-as status, find metadata, delete. Backend `literature/library.py`: `filter_references` (q over title/venue/key/abstract/authors/doi; year, year_min/max, entry_type, venue, has_pdf, needs_metadata, project, reading_status, unfiled; sorts with NULLs last), `facets`, `bulk` (6 actions), `find_metadata` (DOI → arXiv → Crossref title search with a 0.6 Jaccard gate + DOI-collision guard). API: `GET /references/facets/`, list filters, `POST /references/bulk/`, `POST /references/{id}/find-metadata/`; `projects` field on ReferenceSerializer (prefetched). Tests: 10 library-layer, 4 API, 3 workbench guards. Verified with Playwright in both themes (screenshots → docs/screenshots/library.png + README). Note to self: rebuild CSS (`make css`) whenever TSX classes change — the first shot had a collapsed grid because only the JS bundle was rebuilt.

- **Current feature area: the Library (Library v2)** — owner direction 2026-09-06: one feature at a time, best in the whole field, worthy of a viral GitHub star. The `/cycle` command (`.claude/commands/cycle.md`) is the never-ending loop prompt; the session self-schedules it.
- **★ LIBRARY v2 SLICE 1 SHIPPED (2026-09-06, #293): import from anywhere.** `literature/importers.py`: parse_csl_json / parse_ris / parse_bibtex → one `import_metadata_list` with cross-format dedupe (DOI, arXiv id, title+year); `import_pdf` reads DOI/arXiv from the first pages (pypdf, new dep, spec updated), fetches metadata, attaches the file, or keeps a `needs_metadata` stub; `import_file` sniffs by extension/content; `import_from_zotero` pages Zotero 7's local API (23119) as CSL-JSON with actionable errors. API: `POST /references/import/` (multipart files + text, format auto), `POST /references/import-zotero/` (503 with guidance when Zotero is off). MCP: `import_references`, `import_from_zotero` (39 tools). Tests: 15 importer tests (hand-built minimal PDF fixture with a DOI), 5 API tests, 2 client tests. Next: slice 2, the workbench UI (#294).

- **★ OBSERVATORY #291 SHIPPED (2026-09-06): a new visual identity, dark by default.** Owner: "I still don't like the UI at all… make this crazy enough for YouTube." Built the Observatory identity: (1) `assets/css/app.css` — `.dark` token overrides (stone → deep-space canvas #05070f, glass panel rgb(16 20 40/.62), luminous borders; indigo → electric violet #7c6cff), an aurora (`body::before`, 3 drifting radial glows) + star grain, glass via `[class~="dark:bg-stone-900"]` (backdrop-blur + hairline + deep shadow), vendored Inter + Space Grotesk (OFL, `static/fonts/`), `.text-gradient`, `.glow-accent`, `.hairline-gradient`, `.rise` staggered entrance, `.orbit-ring`. Because Tailwind v4 emits `var(--color-*)`, ALL 16 SPA pages + classic templates re-skinned with zero component edits (verified: overview, plan, library, graph, classic dashboard). (2) `static/js/constellation.js` — zero-dependency canvas particle field (anchors = projects, mouse repulsion, twinkle, reduced-motion → static frame), used by `components/Constellation.tsx` (lazy `import()`) and the login template. (3) Layout.tsx — icon rail (lucide), glowing active bar, "Ask Atlas anything ⌘K" spotlight pill, Observatory/Paper toggle. (4) Dashboard.tsx — hero with the constellation of active projects, time-aware greeting + gradient headline ("2 things need you."), spotlight button, glowing attention panel, display-numeral stat tiles, orbit-ring project progress. (5) CommandBar — glass spotlight (hairline gradient, glow, larger input). (6) login.html — split cinematic layout with the constellation + tagline. (7) Theme bootstrap: dark unless the saved choice is "light" (replaces #273's follow-the-OS). Fixed on the way: the plan API returned `project` as a slug while Plan.tsx expected an object (empty breadcrumb, invisible progress bar) → API now also returns `project_name`/`project_color`. Tests: test_observatory.py (6), theme test rewritten, plan API test extended. Screenshots taken with Playwright in both themes. Backlog #292 (second pass on Overview/Plan/Library/Graph + vendor 3d-force-graph).

- **★ DESKTOP ↔ CLAUDE CODE #289 SHIPPED (2026-09-06): the installed app is driveable from Claude Code with one line.** Owner: "make sure it's 100% integratable with Claude Code!" It wasn't: the desktop build ran with an empty API key (API rejected everything) and shipped no MCP server. Now: (1) desktop settings mint + persist `<data dir>/api_key`; `run_desktop` writes `server.json` (live URL/port, mcp_bin). (2) `mcp_server/desktop_config.py` (Django-free) discovers URL + key from the data dir (ATLAS_DATA_DIR → OS app-data folder for com.atlas.research → ~/.atlas); `desktop/server/atlas_mcp.{py,spec}` freeze the MCP server as `atlas-mcp` (console=True stdio; skips `mcp.cli` which sys.exits without typer); tauri.conf ships it as a 2nd resource; main.rs resolves it and passes `ATLAS_MCP_BIN` to the server; CI freezes it. (3) `core/mcp_connect.py` + `core/views.connect_claude` + `templates/core/connect_claude.html` at `/connect/claude/` (both sidebars): the exact `claude mcp add atlas -- "<path>"` line (adds `--env ATLAS_DATA_DIR` only for a non-default data dir; dev installs get the explicit URL/key form), Copy buttons, the key, a JSON snippet. (4) client.py turns ConnectError into "Atlas is not reachable … is the Atlas app running?". Tests: +9 desktop_config, +8 connect page, +3 desktop settings (key persisted/explicit wins, server.json, freeze scaffold), scaffold guards, +1 client. **E2E with the real `claude` CLI (2.1.261) against the frozen binaries:** api_key minted (43 chars) → API 200 with it / 401 without; Connect page printed the right line; a stdio MCP client saw 37 tools, `list_projects` → 0, `create_project` → 201, `list_projects` → 1; `claude mcp add atlas --env ATLAS_DATA_DIR=… -- atlas-mcp` then `claude mcp list` → **√ Connected**. Fixed the frozen MCP's shutdown "I/O operation on closed file" (stdout re-pointed at devnull after run). Backlog #290 (live connection test on the page).

- **★ DESKTOP #286 SHIPPED (2026-09-06): the desktop app is finished — Postgres remnants gone, port clash fixed, docs true again.** The owner came back to "finish the desktop app"; the app itself had shipped (0.1.39 green on desktop-preview, both OSes) but the SQLite switch (#266) was never cleaned up: every installer still carried ~50 MB of unused Postgres binaries, the in-app failure page still blamed Postgres/MSVCR120.dll, `core/desktop_runtime.py` (262 lines, 12 tests) was dead code, and desktop/README.md still said the server was "NOT YET" bundled. Removed all of it (workflow steps, `resources/pg`, ATLAS_PG_BIN in server.rs/main.rs, postgres.exe in the NSIS hooks). New: `server::choose_port` — 8000 if free, else an OS-assigned port — so a dev `runserver` no longer makes the desktop launch fail; `CSRF_TRUSTED_ORIGINS` now follow ATLAS_PORT (they were hardcoded to :8000, which would have 403'd every POST on another port); the diagnostic page names the port it used. Tests: rewrote test_desktop_settings.py (real `run_desktop --setup-only` on a fresh SQLite dir: db file + versioned static + login; CSRF-origins-from-port; Postgres-is-gone guard) + test_desktop_scaffold.py (resources == frozen server only; choose_port wired). Rust: 6 unit tests (`cargo test`, incl. choose_port + tail_file), `cargo fmt --check` clean. E2E: froze the server locally (191 MB one-folder) and ran it on :8077 — `/` 302→`/login/` 200, login POST with Origin :8077 → 302 (would have been 403 before), foreign Origin → 403, static served by WhiteNoise. The desktop-release workflow now also builds from `claude/desktop-app-project-tl12ri`. Backlog: #287 macOS matrix entry, #288 AppImage retry; D3 (updater signing) and code signing remain owner-only steps.

- **★ UI #275: calm mode app-wide via a `.calm` class (2026-06-17).** Promoted calm mode from a dashboard-only React toggle to a class-based variant like dark mode: calm.ts toggles `.calm` on <html>, the base/spa bootstrap scripts apply it before paint from the atlas-calm key, and a `@custom-variant calm` lets any surface use `calm:hidden` with zero per-component state. First adopter: the SPA sidebar pet's speech bubble is quieted in calm mode (the pet stays; its chatter goes). 5 files (calm.ts, base/spa.html, app.css, Layout.tsx) + rebuilt bundle & CSS (calm variant compiled, 1 `.calm` rule). Guard `test_calm_mode_is_class_based_app_wide`. Gate: ruff clean, **811 passed** (+1). Pushed (static + templates → desktop rebuild). Parked #285 (adopt calm:hidden on heatmap/figure counts + a classic-page calm affordance).

- **★ UI #279(header): "Commands" group header in the ⌘K palette (2026-06-17).** Palette rows now show a small section label per kind — Commands (verbs), Go to (navigation), Complete (milestones) — inserted whenever the kind changes down the list, so the dark/calm verbs read as actions distinct from destinations. Pure presentation (no behavior change); arrow/Enter still operate on the flat row list. tsc 0, bundle rebuilt. Guard extended to assert the header is built. Gate: ruff clean, **810 passed**. Pushed (static/js → desktop rebuild). Remaining #279 work (the extra verbs themselves) + #284 (a Recent group) parked.

- **★ UI #283: ErrorState extended to the secondary pages (2026-06-17).** Wired the retryable error state into Decisions, Timeline, Figures, Inbox, and Review — the last of which previously masked a failed load as a perpetual "Assembling your week…". 16 SPA pages now share one consistent, retryable failure state (guard asserts all 16). Chose this lower-risk coverage pass over the #282 render-prop refactor given the container's rollback frequency (churning 11 working pages would've been unwise). tsc 0, bundle rebuilt. Gate: ruff clean, **810 passed**. Pushed (static/js → desktop rebuild — owner asked to refresh the .exe; the accumulated UI advancements all ship in it). #282 (the QueryBoundary abstraction) stays parked for a stable-container cycle.

- **★ UI #281: ErrorState rolled into the primary pages (2026-06-17).** Wired the retryable ErrorState into the six main index/detail pages that previously had NO error branch at all (Projects, Library, Notes, Writing, Literature, Search) — a failed load on any now shows "Try again" instead of silently rendering empty or throwing on `data.map`. 11 SPA pages total now share the error state. Guard strengthened to assert all 11 keep it. tsc 0, bundle rebuilt. Gate: ruff clean, **810 passed**. (Recovered from a mid-cycle container rollback first: reset --hard origin → fefdb84, dockerd+compose+migrate, uv sync starlette 1.2.1→1.3.1, **npm ci** to restore node_modules — the rollback had left @replit/codemirror-vim missing — runserver, web:200.) Pushed (static/js → desktop rebuild). Parked #282 (a shared QueryBoundary to kill the per-page boilerplate + cover secondary pages).

- **★ UI #280: retryable ErrorState across the SPA (2026-06-17).** Replaced the bare red "Couldn't load…" lines on the 6 highest-traffic query failures (Dashboard, Documents, Files content + tree, ProjectOverview, Plan) with a new shared, dark-aware `components/ErrorState.tsx` that offers a "Try again" button wired to react-query's `refetch` — a transient API hiccup now recovers in place without a full reload, and the failure UI is finally consistent + dark-aware (several of the old lines lacked dark: variants). tsc 0; ErrorState built into its own chunk. Guard `test_data_load_failures_use_shared_error_state` asserts it's built and no page regresses to the bare pattern. Gate: ruff clean, **810 passed** (+1). Pushed (static/js → desktop rebuild). Parked #281 (wire the remaining pages + a shared QueryBoundary).

- **★ UI #277+#278: live "Toggle calm mode" from the ⌘K palette (2026-06-17).** Made calm mode shared + live-updatable. Extracted `frontend/src/app/calm.ts` (readCalm/setCalm/toggleCalm + a `useCalm()` hook): setCalm persists to the atlas-calm key AND dispatches a same-tab "atlas-calm-change" event; useCalm also hears cross-tab `storage`. The dashboard now consumes `useCalm()` (no more local state), so flipping calm from the palette updates it live with no remount. Added the "Toggle calm mode" palette verb beside "Toggle dark mode" (discoverable by typing calm/focus/quiet/stats), flashing the resulting state. tsc 0, bundle rebuilt (spa.js 100.61KB). Guards updated: the calm key now lives in calm.ts (asserted), the Dashboard chunk still carries the button label, and both palette verbs are asserted present in source + spa.js. Gate: ruff clean, **809 passed**. Pushed (static/js → desktop rebuild). Parked #279 (more palette verbs + a Commands group header).

- **★ UI #276: "Toggle dark mode" in the ⌘K palette (2026-06-17).** Surfaced the #273 theme toggle as a runnable command-palette verb (new kind:"verb" row), discoverable by typing dark/light/theme/appearance; it flips window.__toggleTheme() and flashes the resulting state, and verb matches rank above nav results. Additive, no refactor of #274. Calm-from-palette deliberately deferred (#278) — it'd need the dashboard to subscribe to live atlas-calm changes, else the toggle wouldn't visibly update. tsc 0, bundle rebuilt (spa.js 99.47→99.90KB, marker present). Guard `test_command_palette_theme_verb_is_built` asserts source + committed spa.js agree. Gate: ruff clean, **809 passed** (+1). Pushed (static/js → desktop rebuild).

- **★ UI #274: dashboard "calm mode" (2026-06-17).** Some research days you want only "what needs me," not the monthly stats. Added a zero-config, localStorage-persisted ("atlas-calm") "Calm mode / Full view" toggle on the SPA dashboard header (same no-settings-page pattern as #273's theme) that hides the vanity stats grid (papers read / notes written / milestones done / inbox count), leaving the actionable surfaces — Needs attention + Active projects + Deadlines + Upcoming milestones. tsc 0, bundle rebuilt (Dashboard-chunk has the marker, served 200). Guard test `test_dashboard_calm_mode_is_built` asserts source + committed chunk agree. Gate: ruff clean, **808 passed** (+1). SPA-only (no desktop rebuild). Parked #275 (promote calm to an app-wide body class). Pushed.

- **★ UI #273: theme follows the OS by default — dark-mode capstone (2026-06-17).** Now that dark mode is complete app-wide (#270), the theme bootstrap no longer ships dark as strict opt-in: base.html + spa.html + the standalone login.html resolve to "explicit saved choice wins, else follow OS prefers-color-scheme," still flash-free before paint. The toggle records an explicit override; with no stored choice we track OS changes live. Crucially, the login page (the SQLite desktop app's FIRST screen) previously had NO theme script at all — always light despite its dark: variants — now it goes dark for OS-dark users. Guard test `test_theme_follows_os_preference_by_default` asserts all three templates consult prefers-color-scheme + the old opt-in comment is gone. Templates-only (no .tsx/CSS class changes). Gate: ruff format/check clean, **807 passed** (+1). Live-verified login + app serve the script after a clean runserver restart. Pushed.

- **★ UI #271: loading skeletons + transitions (2026-06-17).** Added reusable dark-aware Skeleton/SkeletonCard/SkeletonLines/SkeletonPage components; 2 parallel agents replaced the plain "Loading…" text on 12 high-traffic pages with content-shaped skeletons + added transition-colors/active-press feedback on buttons/links. tsc 0. gate running.

- **★ DARK MODE #270 — COMPLETE across the whole app (2026-06-17).** Foundation (toggle, no-flash script, dark .card token, dark sidebars) + the ENTIRE SPA (24 pages + command palette + documents table) + 74 classic templates (literature, notes, projects, writing, research, plan, documents, dashboard/search, and the LOGIN page) all have dark: variants. Done as ~14 parallel worktree agents across several waves; every wave tsc-0 / manage.py-check-clean / 805 pytest. Dark is class-based + opt-in (toggle in both sidebars, persisted, no flash). CSS 62KB→76KB. gate running.

- **◐ DARK MODE #270 STARTED (2026-06-17): foundation in.** Owner: "go crazy, no mobile, don't ask." Class-based dark mode (`@custom-variant dark` on a .dark<html>), a no-flash theme script + persisted toggle (opt-in only until complete, so default users are unaffected) in base.html + spa.html, a Dark/Light toggle in both the SPA sidebar (Layout.tsx) and classic sidebar, and dark variants baked into the shared .card/.card-title tokens + both sidebars + the classic nav/search. Next: parallel waves add dark: variants to every page's content. CSS 65KB→ (with dark utils). foundation gate running.

- **★ UI WAVE 6 shipped + 2 guard fixes (364cd5c): ~65 surfaces across 6 waves; entire app polished. Desktop rebuild triggered.**

- **★ UI WAVE 6 SHIPPED (2026-06-17, classic templates): 36 more templates — literature, notes, projects, writing, core dashboard/search.** Six parallel agents: literature (main pages + matrix/bib/forms — HTMX reading-status + matrix toggles preserved), notes (writerly editor + backlinks + inbox), projects (cards + classic overview + the high-visibility _subnav tab bar, per-project accent colors kept), writing (status board + manuscript detail + submission timeline; latex_editor.html left alone), core dashboard (stat figures + heatmap kept) + search. All {% %}/hx-*/id=/Alpine x-* attributes verified preserved. CSS rebuilt 62KB→64KB. ~65 surfaces across waves 1-6; the entire app is now uniformly polished. full suite running.

- **★ UI WAVE 5 SHIPPED (2026-06-17, classic templates): the login page + 20 server-rendered templates.** Polished the classic Django/Tailwind templates: registration/login.html (now a centered editorial card — the first-impression page), base shell, the shared _fields.html + create/edit forms, all research templates (ledger/experiments/datasets/protocols/_tabs), the HTMX plan templates (phase cards, progress, questions), and the HTMX documents templates (index/tree/rows/rename/tags). Agents preserved every {% %} tag, id= anchor, and hx-* attribute (verified). Tailwind @source scans templates/ + frontend/src, so make css picked up all new classes (app.css 47KB→62KB). 30+ surfaces across waves 1-5; the whole app — SPA + classic — is now on one calm design language. full suite running.

- **★ UI WAVE 4 SHIPPED (2026-06-17, frontend): 6 more surfaces incl. the Cmd+K palette + documents table.** Research (4 consistent section cards), Figures gallery (4:3 thumbnail cards + elegant lightbox), Prompts gallery (quiet copy buttons), Automations (bot status chips + history chart), DocumentsTable component (Library-style table, tabular-nums, quiet row actions, aria-sort kept), CommandBar (floating palette card, magnifier input, indigo-50 active row, group headers, kbd footer). 24 surfaces across waves 1-4. Central tsc 0. full suite running.

- **★ UI WAVE 3 SHIPPED (2026-06-17, frontend): 6 more surfaces — incl. the app sidebar.** Layout/sidebar (clear indigo 'you are here' active state, calmer nav rhythm — seen on every page), Files workspace (crisp explorer tree + framed preview + responsive split), Timeline (softer rail/markers), Graph (segmented 2D/3D toggle + tidy legend + node panel + empty state), ReadingFlow (focused reading card + quiet status chips + queue progress), NewProject (welcoming single-column form + template cards + swatch color picker). 18 pages across waves 1-3. Central tsc 0 errors. full suite running.

- **★ UI WAVE 2 SHIPPED (2026-06-17, frontend): 6 more SPA pages polished in parallel.** Projects index (card grid with per-project accent-color bars), Notes (roomier writerly editor + backlinks), Decisions (logbook timeline + labeled form), Reference detail (clean paper card, comfortable abstract width), Inbox (prominent capture box, quiet triage controls), Search (per-type result chips + focus ring). Same parallel flow: 6 worktree agents, source-only, tsc clean, integrated once + one bundle rebuild + one gate. 12 pages done across waves 1-2. full suite running.

- **★ UI WAVE 1 SHIPPED (2026-06-17, frontend): 6 SPA pages polished in PARALLEL.** Owner: "do the next 100 cycles on UI, in parallel." Launched 6 worktree-isolated agents simultaneously, each polishing ONE distinct page to the Atlas §7 design language (calm/editorial, stone palette, indigo accent used sparingly, slim progress bars with N/M labels, consistent card + section-header tokens, real empty states, subtle hover/focus): Dashboard, ProjectOverview (the heart), Plan (phase→milestone→task hierarchy, restrained overdue cue), Library (dense-but-quiet list), Literature (calm status/priority chips), Writing (board + submission timeline). Each agent ran tsc clean (source-only, no bundle); I integrated all 6, rebuilt the bundle once (6 page chunks), gate once. No behavior/data changes. full suite running.

- **★ OWNER DESKTOP #269 SHIPPED (2026-06-17): the SQLite app LAUNCHED — fixed its missing CSS.** Owner's screenshot showed the SQLite desktop app running (login page!) but UNSTYLED. Cause: static/css/app.css is a gitignored Tailwind build artifact and desktop-release CI never built it → frozen bundle had no stylesheet. Fix: a 'Build Tailwind CSS' CI step (downloads the Tailwind standalone CLI per runner OS, compiles assets/css/app.css→static/css/app.css) BEFORE the freeze bundles static/. Verified locally (v4.3.0, 47KB); guard test. Also told owner the auto-created login is atlas/atlas. Pushing rebuilds the installer WITH styling. full suite running.

- **★ SLICE #267 SHIPPED (2026-06-17, desktop): auto-clean the old Postgres data dir + refresh the GitHub build.** Owner asked to update the installable build. run_desktop now drops the stale pgdata/ + pgsock/ + postgres.log from the data dir on startup, so upgrading a machine from the bundled-Postgres build to SQLite is clean automatically (no manual %APPDATA% wipe) and reclaims space; no-op on a fresh install. Verified SQLite setup completes with a stale pgdata present; guard test (20 desktop tests). Pushing re-triggers the desktop-release build so desktop-preview gets a fresh SQLite installer. Parked #268 (strip the dead pg *bundling* to shrink it). full suite running.

- **Phase:** Backlog (all 6 phases gated ✅); self-improving loop is running — RESUMED after the desktop saga (owner: "go back to the loops"). Desktop is finished (#286, 2026-09-06): SQLite-only installers (.deb/.rpm/.exe/.msi, no Postgres inside; AppImage still not built — retry parked as #288) building green on desktop-preview from both branches; "only deb" was the owner catching the release mid-build (Linux job finishes ~5min before Windows).
- **★ OWNER DESKTOP #263 SHIPPED (2026-06-16): the app now SHOWS the failure in-window.** Owner still couldn't launch on Windows and demanded we stop debugging blind. (1) Web search found the likely root cause: zonky's bundled Postgres binaries link MSVCR120.dll → they need the **Microsoft Visual C++ 2013 Redistributable** (https://aka.ms/highdpimfc2013x64enu); without it initdb/postgres can't start and postgres.log stays empty (matches the symptom exactly). (2) Rewrote desktop/src/main.rs: opens on a local "Starting Atlas…" splash, then a bg watcher either navigates to the app once the port binds, OR — on early child exit (try_wait) or the 10-min deadline — navigates to a diagnostic page (written to the data dir as file://) showing the real atlas-server.log + postgres.log tails, the data-folder path, and the VC++ 2013 fix. server.rs got tail_file + escape_html. on_navigation now also allows our file:// pages. cargo check clean; guard test added (32 desktop tests). Build 0e012f2 GREEN both platforms on desktop-preview. Owner told: install vcredist + clean-install + screenshot the diagnostic page if it still fails. **(Recovered from ANOTHER mid-turn rollback: reset to origin 7f35383, lost the in-progress AUDIT #24 protocol-N+1 fix, restored venv via uv sync (starlette 1.2.1→1.3.1 again), dockerd+compose+migrate+runserver/huey — the "692 errors" in a gate run were just the rolled-back DB being down; 793 pass once recovered.)**
- **★ SLICE #262-spa SHIPPED (2026-06-17, frontend): protocols on the SPA Research page.** Completes #262: Research.tsx now has a read-only Protocols section listing the current protocols (GET /protocols/?project=, filtered to is_current) with a v{n} badge, mirroring the datasets section; empty state links to the classic page to write one (the existing "add & edit on the classic page" affordance covers editing, and the classic research page has a Protocols tab). tsc 0 errors, bundle rebuilt. Frontend-only (won't trigger a desktop rebuild — keeps the in-flight SQLite build 0b1d3b0 as the owner's clean target). 803 pytest (unchanged).
- **★★ DESKTOP → SQLite #266 SHIPPED (2026-06-17, owner-authorized): ended the bundled-Postgres saga.** The #265 diagnostic finally pinned it: postgres.log showed Postgres UP/ready on 5433 but the [setup] log stopped at "starting postgres" — a pg_ctl capture_output pipe-inheritance hang on Windows, the Nth distinct Postgres-on-Windows bug. Owner: "if you see no fast solution then lets go sqlite!" — and SQLite is what every single-user desktop app (Zotero/Obsidian/VS Code/Signal) uses. SWITCHED config/settings/desktop.py to django.db.backends.sqlite3 (a file in the data dir, WAL); run_desktop dropped ensure_postgres (migrate creates the file → instant startup, no server). Postgres-only features degrade cleanly: 4 trigram GinIndex migrations now use core.migration_ops.PostgresAddIndex (creates on Postgres, no-ops on SQLite); search already falls back to icontains off Postgres. Web/server keeps Postgres untouched. Verified fresh-SQLite migrate+check+run_desktop setup-only (195 files, superuser, Setup complete) + Postgres trigram tests still pass; 34 desktop tests. Parked #267 (strip the now-dead Postgres bundling from spec/CI/Rust to shrink the installer). full suite running.
- **★ SLICE #258 SHIPPED (2026-06-17, api/overview): figure count on the project overview.** Added "figures" to the overview API counts (one COUNT on documents whose content_type is in PREVIEWABLE_IMAGE_TYPES — the exact figures-feed whitelist, SVG excluded). The SPA overview renders counts generically so it appears automatically; bumped the grid lg:grid-cols-7→8 for a clean single row (rebuilt ProjectOverview-chunk). Backend test (figures counts only the image), live-verified, schema clean. 802 pytest.
- **★ OWNER DESKTOP #265 SHIPPED (2026-06-17): instrument ensure_postgres — the diagnostic worked and pinned the hang.** The owner's diagnostic page (from #263!) showed postgres.log = Postgres UP and "ready to accept connections" on 127.0.0.1:5433 (so VC++2013 was a red herring — Postgres runs), but atlas-server.log = EMPTY and "did not start within 10 min". Diagnosis: ensure_postgres is SILENT, and it wedges AFTER pg_ctl start but BEFORE run_desktop prints "Postgres is up." — i.e. in the psycopg wait loop (libpq's connect_timeout is likely ignored on Windows, so a connect blocked forever with zero output → empty log + 10-min timeout). FIX: (1) a flushed _log() at every ensure_postgres step so the log is never empty again; (2) replaced the psycopg-only wait with a reliably-bounded raw-socket _wait_for_tcp() precheck THEN a ThreadPoolExecutor-bounded psycopg connect/CREATE-DATABASE (20s hard timeout, shutdown(wait=False) so a hung thread can't block) — a wedged connect now RAISES with the postgres.log tail instead of hanging silently, so the Rust shell shows the diagnostic immediately (not after 10 min). 18 desktop tests + guard. Pushing rebuilds the installer; the next launch either works or names the exact failing step.
- **★ SLICE #261 SHIPPED (2026-06-16, docs/mcp): README MCP tool-list guard.** The hand-maintained README tool list had drifted — it omitted all 11 manuscript/LaTeX tools + get_timeline and the intro said "16 tools" when there are 37. Rewrote the list (grouped by area) to cover every @mcp.tool + fixed the count, and added mcp_server/tests/test_docs.py which parses server.py's @mcp.tool function names and fails if any is undocumented or the count is stale — so it can't drift silently again. 2 guard tests. 801 pytest.
- **★ SLICE #262-prov SHIPPED (2026-06-16, research): experiment → protocol-version provenance.** The payoff of versioned protocols: ExperimentEntry.protocol FK (SET_NULL — the entry survives a protocol delete) records the exact protocol *version* a lab-notebook entry followed. Scoped dropdown in the experiment form (project's protocols, "Title vN" labels), a "Protocol: Title vN" link on the experiment log → #protocol-{pk} on the protocols page, protocol + protocol_label in the ExperimentEntry API, select_related("protocol") so the log stays N+1-free. Migration research/0004; schema --fail-on-warn clean; live-verified end-to-end (experiment→v2→API protocol_label). 5 new tests. Parked: the SPA read-only Protocols section (#262 remainder). 799 pytest.
- **★ AUDIT #24 DONE (2026-06-16): one N+1 fix, rest clean.** Full security+responsiveness pass over the writable Protocol API + figures/calendar/commit/MCP/desktop surfaces. Security: the new WRITABLE Protocol API probes clean live (anon 401, no/invalid project 400, missing-id new-version 404), project-scoped, version/parent read-only (forge-proof history chain), markdown sanitized. Deps: npm 0; pip-audit's starlette-1.2.1 was a stale-venv-after-rollback false alarm (uv.lock already 1.3.1; uv sync → 0 vulns). Perf: **fixed protocol_list O(n) queries** — it ran is_current exists() per row AND the template walked protocol.parent/.lineage per row (~11 q/10 protocols); now loads all protocols once + computes superseded set + lineage_cached in memory + gates History on lineage_cached (not the lazy .parent) → O(1) queries, N+1 guard test added. Report in AUDITS.md. 794 pytest.
- **★ SLICE #260 SHIPPED (2026-06-16, research): protocol library web UI.** A classic Research-tab page (templates/research/protocols.html mirroring datasets.html) at /projects/{slug}/research/protocols/ — lists current protocols (head of each version chain), each with a v{n} badge, markdown body, "New version" link, and a History disclosure (Protocol.lineage walks the parent chain). views: protocol_list + ProtocolCreateView + protocol_new_version (GET prefills from current, POST appends a version); ProtocolForm; 3 urls; a Protocols tab in research/_tabs.html. The research classic pages are the canonical create/edit surface (the SPA Research page links out to them). Fixed a missing `{% load markdown_extras %}`. 4 new tests (lineage + list + create + new-version). Parked #262 (SPA read-only protocols section + ExperimentEntry→Protocol provenance FK). 792 pytest.
- **★ SLICE #7-mcp SHIPPED (2026-06-16, mcp_server): protocol tools for Claude.** Completes #7's "API → Claude" loop: added list_protocols / add_protocol / new_protocol_version to the standalone MCP server (client.py HTTP wrappers + server.py @mcp.tool definitions) so Claude can author and revise versioned protocols directly. README MCP tool list updated. Live-verified end-to-end through the MCP httpx client against the running API (add→new-version→list→cleanup); 3 new client tests (22 mcp tests total); server imports clean. Parked #260 (the protocol web UI). 788 pytest.
- **★ SLICE #7 SHIPPED (2026-06-16, research/api): protocol library with versioning (API-first).** New research.Protocol model (project, title, body markdown, version, parent self-FK) — append-only: protocol.new_version(**overrides) clones with version+1 + parent set; is_current = head of the chain (no later version names me as parent). Exposed as the one WRITABLE research DRF viewset /api/v1/protocols/ (protocols are MCP-managed/machine-friendly so Claude can author+revise them) + a POST {id}/new-version/ action; version+parent read-only so the history chain can't be forged. Admin + router registered; schema --fail-on-warn clean. Migration research/0003; live-verified create→new-version→list (v2 current, v1 superseded, title carried over); 8 tests. Parked #260 (the classic/SPA protocol UI + ExperimentEntry→Protocol provenance link). 785 pytest.
- **★ SLICE #4 SHIPPED (2026-06-16, research): experiment ↔ commit linking.** A lab-notebook ExperimentEntry can now reference the exact code that produced it: added commit_url (URLField) + a commit_label property (GitHub/GitLab commit URL → owner/repo@shortsha, else host). Wired through the form, admin list_display, ExperimentEntrySerializer (read-only commit_label), classic experiments.html (a ⎇ link), and the React Research experiment log. Pure pasted-URL — no GitHub API call, offline/host-agnostic. Migration research/0002; live-verified the read path + label parse (AliZareh-CoE/project-manager@c543d70); tsc clean, bundle rebuilt; 3 new tests. **Also swept #2 (review matrix) + #6 (weekly review) DEAD** — both already implemented (review-matrix action; WeeklyReviewAPIView + Review page). Parked #259 (richer commit metadata fetch). 777 pytest.
- **★ SLICE #257 SHIPPED (2026-06-16, core+frontend): made the Figures page discoverable.** Three entry points: (1) a "View as gallery →" link on the SPA Documents page header; (2) a page-aware "Figures" quick-action in the command bar / assistant context (core/assistant._actions, in-project) → SPA path /projects/{slug}/figures/; (3) registered `figures` in links.ts toSpaUrl so it navigates SPA-internally (no reload). Also swept **#5 (Cmd+K palette) DEAD** — CommandBar.tsx already is a full Cmd/Ctrl+K palette (cycle 65). Fixed a latent UP017 in core/calendar.py (dt_timezone.utc → datetime.UTC) the newer ruff flags; full-repo ruff now clean. Assistant test asserts the action+url; tsc 0 errors, bundle rebuilt. Parked #258 (figure count on overview). 774 pytest.
- **★ SLICE #256 SHIPPED (2026-06-16, frontend): figure-gallery UI page.** Completes #8: app/pages/Figures.tsx — a calm React grid that fetches /projects/{slug}/figures/, groups thumbnails by folder, has a tag-filter chip row, and opens a click-to-lightbox (full image + download/close) over each figure's nosniff'd raw_url; empty state points to Documents. Wired the route in main.tsx + a Figures quick-link on ProjectOverview. **tsc 0 errors** (the 32 pre-existing src/editor errors were just an incomplete node_modules → `npm ci` fixed them; my files added none), **vite build** regenerated spa.js + Figures-chunk.js (route present in bundle), built artifacts committed. The /figures/ data path was live-verified end-to-end in #8-data. Parked #257 (a "view as gallery" link on the Documents page). 774 pytest unchanged (frontend-only).
- **★ SLICE #8-data SHIPPED (2026-06-16, api/documents): figure-gallery data layer.** API-first slice of #8: GET /api/v1/projects/{slug}/figures/ returns every inline-previewable raster image in a project (newest first) — id/title/folder/tags/size/content_type/created_at + a `raw_url` that serves it inline (reusing #250/#254). documents.selectors.project_figures is pure + N+1-free (DB filter content_type startswith image/, exact preview_kind whitelist in Python, SVG excluded); API-key gated, project-scoped, schema clean (--fail-on-warn). Live-verified end-to-end (uploaded PNG → feed → raw_url inline+nosniff+cache; cleaned up). 7 tests. Parked #256 (the gallery UI page). **NOTE: survived a container rollback mid-cycle — local reset to 0aa9d0f + dev env down; recovered via git reset --hard origin/...→1ab46cd, dockerd + compose up + migrate + runserver/huey (with dangerouslyDisableSandbox — the sandbox was killing the port-binding server with exit 144), then rebuilt the lost unpushed figures slice from context.** full suite running.
- **★ SLICE #254 SHIPPED (2026-06-16, api): Cache-Control on the API raw inline endpoint.** AUDIT #23 follow-on: DocumentViewSet.raw set nosniff but no Cache-Control, while document_download/document_preview both cache the same immutable files for a day — so the workspace PDF/image preview re-downloaded on every view. Added `Cache-Control: private, max-age=86400` (uploads are immutable; edits create new files), matching the other two paths; extended the raw test. Parked #255 (factor a cacheable_file_response helper if a 4th path appears). full suite running.
- **★ AUDIT #23 DONE (2026-06-16): one dependency finding, fixed.** Full security+responsiveness pass over the work since #22 (desktop startup chain #244-248, inline-preview sniffing #249/#250, doctor #208, .ics feed #9). pip-audit flagged **starlette 1.2.1 → CVE-2026-54282/54283** (transitive via mcp + sse-starlette, MCP HTTP transport); bumped to **1.3.1** via `uv lock --upgrade-package starlette` + `uv sync` (clean resolve, no other pins moved) → re-audit 0 vulns; MCP imports + 19 tests pass. New code surface clean: the .ics feed is API-key gated (401 anon), project-scoped (404 unknown slug), RFC-5545-escaped (ICS injection neutralized — verified by standalone BEGIN:VEVENT line count), 2 queries / ~19ms; inline-preview sniffing is strictly tighter than #22's whitelist. Report in AUDITS.md. Parked a #11 follow-on (Cache-Control on the API raw action). Full suite re-running after the bump.
- **★ SLICE #9-calendar SHIPPED (2026-06-16, api/core): iCalendar (.ics) deadline export.** Advances backlog #9 (calendar half): GET /api/v1/projects/{slug}/calendar.ics/ returns an RFC 5545 feed of the project's milestone due dates + manuscript deadlines as all-day VEVENTs (core/calendar.py — dependency-free generator with TEXT escaping + 75-octet line folding; completed milestones get ✓/STATUS:CONFIRMED). Authed via the existing X-API-Key; schema validates --fail-on-warn; live-verified against the demo project (7 events, text/calendar). 8 tests. Parked #253 (token-in-URL subscribable feed — deferred past AUDIT #23 as it adds a URL credential). Machine-friendly (product value #5). full suite running.
- **★ SLICE #208 SHIPPED (2026-06-16, core/doctor): factored the doctor's desktop checks into a helper.** handle() had grown a ~30-line inline desktop-updater block; extracted it verbatim into Command._check_desktop(base) so handle() reads as a flat list of sections again (its own parked trigger: "if more accrue, factor them"). Behavior-preserving — the four existing doctor tests still pass through the full command — plus two new tests for the helper's previously-uncovered branches (configured-green path + no-tauri-conf skip). Also swept #217 DEAD (review-page project chips are already Links where they can be; the rest can't nest inside row-level Links). Parked #252. 7 doctor tests; full suite running.
- **★ SLICE #250 SHIPPED (2026-06-16, api/security): the API raw inline endpoint sniffs bytes too.** DocumentViewSet.raw picked its inline content_type purely from the file *extension* (.png → image/png) and never checked the bytes — the same content_type/bytes mismatch #249 closed for the document preview, on the second inline path. It now reads the head, confirms it (sniff_image_type for rasters, a `%PDF-` magic check for PDFs), and 404s when the bytes don't match the extension-claimed type, so a mislabeled file (html named .png) can't reach the inline path. Reused documents.models.sniff_image_type; updated the existing raw test to a full PNG signature + a mislabeled-extension 404 test. Parked #251 (factor a shared inline_response helper so the rule lives in one place). api 63 + documents 50 green; full suite running.
- **★ SLICE #249 SHIPPED (2026-06-15, documents/security): preview sniffs magic bytes, not the label.** document_preview trusted the browser-reported content_type stored at upload, so a forged/mislabeled file with an image content_type was served `inline` as that image type (nosniff blocked execution, but it was the sole defense). Added documents.models.sniff_image_type(head) — matches the real PNG/JPEG/GIF/WebP/BMP signature — and the view now serves inline only when the bytes confirm a whitelisted raster type, else text/plain. Bytes beat the label. Sniffer unit test + spoofed-image (html-as-png → text/plain) view test; the legit-image test now uploads real PNG bytes. Parked #250 (apply the same sniff to the API raw endpoint, which trusts the file extension). 756 pytest.
- **★ SLICE #247 SHIPPED (2026-06-15, writing): cite-checker handles \nocite.** CITE_RE matched \cite/\citep/etc. but not \nocite{key} (include in bib without an in-text cite — a real command), so \nocite'd keys were wrongly flagged "uncited in bib". Added nocite to the regex; \nocite{*} wildcard still skipped. Test added. 754 pytest.
- **★ OWNER WINDOWS #246 (2026-06-15): slow collectstatic → window timed out.** After #245 fixed the crash loop (no more black console — confirmed in the owner's screenshot!), the window STILL showed refused-to-connect: the server hadn't bound :8000 because first-run `collectstatic` with CompressedManifestStaticFilesStorage gzip+brotli-compresses + hashes EVERY file (minutes on Windows). FIX: desktop staticfiles → plain StaticFilesStorage (just copies; WhiteNoise still serves). Measured 2s locally vs the slow compress/hash. Server binds fast → window auto-reloads (main.rs 600s poll). Build 0.1.17. Guard test. (Also recovered the dev env from ANOTHER rollback: dockerd + compose up + migrate + runserver + run_huey — server was 000, now 302.)
- **★ DESKTOP: owner's Windows app reached "working" (2026-06-15).** postgres.log proved #231 fixed Postgres; empty atlas-server.log = no crash. Last barrier was the window loading before the slow first-run server → shipped #225 (build 0.1.12, GREEN): window opens immediately + a bg thread navigates/reloads it when wait_for_port answers (no manual Refresh). cargo check passed. Owner said "continue development". (Survived ANOTHER rollback mid-turn: local reset to 0aa9d0f + Docker/dev-server killed → recovered from origin + restarted everything.)
- **★ SLICE #243 SHIPPED (2026-06-15, search/API): research types get deep-linkable URLs.** After #240, API/MCP search returned url=None for hypotheses/questions/experiments (no get_absolute_url). Added get_absolute_url to all three → list page + #kind-{pk} anchor (matching ids in the templates so it scrolls); simplified the search.html + _suggest.html branches to use it. No migration. 1 API test. 750 pytest.
- **★ OWNER WINDOWS #245 — THE ROOT CAUSE OF THE HANG (2026-06-15): Postgres console-Ctrl+C crash loop.** Owner's postgres.log was the smoking gun: `background worker "logical replication launcher" terminated by exception 0xC000013A` (STATUS_CONTROL_C_EXIT) repeating every ~1-30s — Postgres kept getting Ctrl+C'd, crash-recovering, getting hit again, FOREVER. So it never stayed up to finish setup → empty atlas-server.log → black window/hang. Cause: the frozen server ran in a CONSOLE (atlas_server.spec console=True — that's the black window), and the Postgres it starts shared that console's control group, so console events killed its workers. FIX: console=False (windowed — no console, no shared control group, AND removes the black box) + start the pg helpers with Windows creationflags CREATE_NEW_PROCESS_GROUP|CREATE_NO_WINDOW (_CREATIONFLAGS in desktop_runtime, applied to _run + self-heal) so they're fully isolated from console signals. This explains the ENTIRE 1-hour hang. Build 0.1.16. cargo n/a (spec+py); 16 desktop tests + guard.
- **★ OWNER WINDOWS #244 (2026-06-15): setup can't hang forever.** Owner: "terminal black even after 1 HOUR, window unchanged" — a genuine HANG, and the earlier empty atlas-server.log means it's stuck in ensure_postgres (before the "Postgres is up." write), NOT migrate/collectstatic. Likely the old hung instance (couldn't be replaced due to the #242 file-lock). FIX: `_run` now defaults timeout=180s (wedged initdb/pg_ctl → RuntimeError, not infinite hang); the self-heal pg_ctl stop got timeout=60; psycopg connections get options="-c statement_timeout=15000" (a stuck CREATE DATABASE/SELECT times out). So a hang now surfaces an error in the log (visible via 0.1.13+ verbosity=1) within ~3 min instead of a black window forever. Build 0.1.15. Owner immediate: Task Manager kill atlas-server.exe/postgres.exe → delete %APPDATA%\com.atlas.research → clean-install 0.1.14 → if still hung, send atlas-server.log (now shows the timed-out step).
- **★ SLICE #242 SHIPPED (2026-06-15, desktop): installer kills a running Atlas first.** Owner hit "Error opening file for writing: …pg\bin\postgres.exe" — a previous atlas-server.exe + its postgres.exe were still running and locking the file, so the update couldn't overwrite it. Added an NSIS pre-install (and pre-uninstall) hook (desktop/installer-hooks.nsh, wired via bundle.windows.nsis.installerHooks) that taskkills atlas-server.exe + postgres.exe before copying files. JSON valid; guard test. Build 0.1.14. (Owner immediate: Abort → Task Manager end atlas-server.exe/postgres.exe → reinstall; or click Ignore since postgres.exe is identical across builds.)
- **★ SLICE #241 SHIPPED (2026-06-15, desktop): faster + visible first-run.** Owner's screenshot showed the atlas-server.exe console ALIVE but the window refused-to-connect — diagnosis: run_desktop ran migrate+collectstatic at verbosity=0 (silent → empty log) while the window loaded before serve() bound 8000. Fix: verbosity=1 (log shows progress) + skip the slow static re-collect on repeat launches of the same build (keyed on ATLAS_VERSION, which server.rs passes from env!(CARGO_PKG_VERSION); CI now stamps Cargo.toml too so it changes per build → still re-collects after an update). With 0.1.12's auto-reload, first launch is slow-but-self-loading and later launches are fast. cargo check + 27 desktop tests + YAML pass. Build 0.1.13. (Owner immediate: server is alive doing silent setup → wait + Refresh, or 0.1.12 auto-loads.)
- **★ SLICE #240 SHIPPED (2026-06-15, search): search covers hypotheses, questions, experiments.** Completes the search-coverage gap — global search now spans references/notes/documents/decisions/phases/milestones/manuscripts/hypotheses/questions/experiments. FTS + fallback + search.html branches (→ ledger/questions/experiment-log) + suggest dropdown + a "hypotheses" plural fix. Live-verified. 7 search tests. 747 pytest.
- **★ SLICE #239 SHIPPED (2026-06-15, search): global search now covers manuscripts.** Search (built Phase 3) never covered Phase-4+ types — a researcher couldn't find their own paper. Added Manuscript (title^A + abstract) to the FTS path + the icontains fallback + a search-result template branch; it slots in cleanly (get_absolute_url, __str__=title, "manuscripts" plural). Live-verified ("Strategic" → real manuscript). Backlogged #240 (hypotheses/questions/experiments need get_absolute_url + template work). 746 pytest.
- **★ SLICE #238 SHIPPED (2026-06-15, UI): inbox triage count.** The quick-capture inbox footnotes "N items to triage." (matching #199/#232). Template-only; 1 test. 744 pytest.
- **★ SLICE #237 SHIPPED (2026-06-15, notes/research): "mentioned but not yet written" stubs + N+1 fix.** The notes list now shows titles referenced via [[links]] across the project but unwritten, each a "+ Title" create-link (builds on #236). New services.unwritten_note_titles (one query, deduped, sorted). ALSO fixed a pre-existing N+1 — the list ran note.incoming_links.count() per row → now annotated. 4 tests incl. an N+1 guard. 743 pytest.
- **★ SLICE #236 SHIPPED (2026-06-15, notes/research): clickable unresolved wiki-links.** An unresolved `[[Title]]` in a note body was dead italic text; now it's a "+ Title" link to the create form pre-filled with the title (`?title=` → NoteCreateView.get_initial) — Obsidian-style frictionless note creation. Works in live preview too. 2 tests; live-verified. 739 pytest.
- **★ AUDIT #22 (2026-06-15): CLEAN — no findings.** Swept since #21: the Windows desktop saga (#224/#228/#229/#230/#231), cheap previews + lightbox (#227/#227-fu), and UI/perf slices (#226/#205/#232/#233/#234/#228-review). Headline new surface = the inline preview endpoint — sound: login-gated + project-scoped, raster-whitelist + text-as-text/plain + nosniff, SVG/HTML/PDF→download (6 tests). Desktop #231 psycopg uses bound params + hardcoded DB_NAME; local-trust unchanged; logs hold no secrets. Perf: reading queue 9q/17ms warm, deadline_label 0-query pure property. npm audit --omit=dev = 0. 738 tests. Report in AUDITS.md.
- **★ SLICE #234 SHIPPED (2026-06-15, UI): pluralize the overview count links.** "All 1 documents/decisions →" → "All 1 document/decision →" via |pluralize on the project overview. (Swept the templates for `{{ n }} noun` without pluralize; the only real bugs — the "X/Y milestones" progress ratios and the "X/Y themes" coverage badge are the endorsed idiom, left alone.) 1 guard test.
- **★ SLICE #233 SHIPPED (2026-06-15, UI): humanized manuscript deadline label.** The 3 deadline countdowns (board/detail/overview) each hand-rolled wording with a "1 days" grammar bug + detail-vs-board mismatch. DRYed into Manuscript.deadline_label ("due today"/"N days left"/"overdue by N days", singular at 1) + deadline_is_soon (<7d incl overdue). Parametrized tests cover all cases; updated one existing assertion. 737 pytest.
- **★ SLICE #232 SHIPPED (2026-06-15, UI): count lines on decisions + questions lists.** Extended the #199 pattern — the decision log footnotes "N decisions recorded." and the questions list "N research questions." (pluralized). Template-only, non-empty-guarded; 2 tests. 730 pytest.
- **★ SLICE #205 SHIPPED (2026-06-15, UI): reading-queue empty state offers an action.** The queue's empty branch showed "everything has been read" even on a fresh project with zero references (misleading + dead-end). Now branches on has_references: no refs → "No references are linked yet" + "Link a reference"; all read → the action-free "Queue is clear" terminal state. Audited all other dashed empty states — already actioned. exists() is one constant query (#226 guard holds). 2 tests. 728 pytest.
- **★ SLICE #227-followup SHIPPED (2026-06-15, UI): in-app image lightbox.** The DocumentsTable now opens IMAGE previews in an in-app lightbox (backdrop/Esc to dismiss, Download/Close) instead of a new tab; text still opens a tab. New `Document.preview_kind` ("image"/"text"/None) → `previewKind` in props; image src is the existing safe inline-serve endpoint. is_previewable derives from preview_kind. No migration. tsc+build clean, SPA rebuilt; 2 backend tests + built-chunk verified. 726 pytest.
- **★ SLICE #228 SHIPPED (2026-06-14, UI): review project chips link to the project.** On the cross-project /review page, the milestone/decision/experiment rows (plain divs) now link their project-name chip to that project's overview (using project_slug from #216) for one-click drill-down. Paper/note rows stay text — their whole row is already a Link (no nesting). tsc + build clean; SPA rebuilt; review chunk test green. (npm install was needed first — rollback wiped node_modules again.)
- **★ SLICE #226 SHIPPED (2026-06-14, perf/test): N+1 guards for reading queue + review matrix.** The two per-reference list/grid surfaces that lacked guards (dashboard/documents/library/overview/plan/2 APIs were already in test_query_budgets.py — check-before-building caught the stale premise). Constant-N tests confirm both flat (8 refs == 3 refs queries). The main app surfaces are now N+1-fenced. 723 pytest green.
- **★ OWNER WINDOWS BUG #231 — THE ACTUAL ROOT CAUSE (2026-06-14): missing client tools.** The owner finally ran the new build (uninstall + -setup.exe + version bump #230 worked) and the log's LAST traceback was NEW code (line 152/53): `FileNotFoundError: postgres binary 'pg_isready' not found` — proving #228's `\\?\` strip FIXED initdb (it now succeeds!), and the failure moved to the next step. ROOT CAUSE: the zonky Windows Postgres bundle ships ONLY initdb.exe/pg_ctl.exe/postgres.exe — NOT pg_isready/psql/createdb (verified by extracting the actual jar). My ensure_postgres shelled out to all three. FIX #231: do the readiness-wait + DB-exists-check + CREATE DATABASE via **psycopg** (already in the frozen server — migrate needs it), zero client-tool dependency, cross-platform. Live-verified the psycopg wait+ensure logic end-to-end against the dev Postgres (create-if-missing + idempotent rerun). Guard test asserts no _pg_bin("pg_isready"/"psql"/"createdb"). Pushing rebuilds the installer (core/desktop_runtime.py is in the CI paths now, #229). This should be THE fix — every prior failure was a layer of the same onion (initdb-not-found → socket → \\?\ → missing client tools). No Windows machine needed: the log + jar inspection + live psycopg test nailed it.
- **★ #230 DELIVERY FIX + ROLLBACK SCARE (2026-06-14): the owner kept running OLD code.** After reinstalling, the owner's atlas-server.log STILL showed the old `_run` (CalledProcessError, line 49) — proof they ran a pre-#228 binary. Investigation: a container rollback had reset my LOCAL checkout to 0aa9d0f (lost all session commits locally!), BUT **origin was safe at 39b610a** with all work incl. #228 — `git reset --hard origin/...` restored local. So build #9 DID build #228. Root cause of the old-code delivery: the app version was a FIXED 0.1.0, so the Windows **.msi skips file replacement on a same-version "install"** (and same-named installers cache). FIX #230: CI stamps `0.1.<github.run_number>` into tauri.conf.json each build (jq) → unique installer name + real .msi upgrade. Build #10 (fff588e) building. ALSO verified the zonky Windows pg bundle is complete (bin+lib+share present, postgres.bki there) — so initdb's exit-1 is NOT missing files; the `\\?\` prefix (#228 strips it) remains the prime suspect. OWNER WORKAROUND meanwhile: fully uninstall + delete %LOCALAPPDATA%\Atlas + use the `-setup.exe` (NSIS, not .msi) → gets the #228 binary now. LESSON: always git-fetch + compare origin before trusting local state after a rollback; bump installer versions.
- **★ #210 BUILD CONFIRMED GREEN (run 27508240910 / sha 713a5a8, both OSes, 2026-06-14):** the Windows `initdb.exe` fix + single-instance built successfully on Ubuntu AND Windows — the corrected installers are on the `desktop-preview` draft release for the owner to retry.
- **★ SLICE #227 SHIPPED (2026-06-14, owner #14 / feature): cheap document previews.** Previewable rows (React DocumentsTable + classic fallback) get a "Preview" link → `documents:preview`, which serves raster images inline and any text as text/plain with `X-Content-Type-Options:nosniff`. SECURITY by design: SVG/HTML/PDF/binaries fall back to download (SVG/HTML can carry script that would run in Atlas's origin); text forced to text/plain. New `Document.is_previewable` property + `PREVIEWABLE_IMAGE_TYPES` whitelist; `previewUrl` in island props (null when not previewable). No migration. 5 tests incl. the SVG/HTML security cases; previewUrl verified in the built chunk + props live. SPA rebuilt. 717 pytest green. Backlogged #227-followup (in-app lightbox/thumbnail).
- **★ SLICE #223 SHIPPED (2026-06-14, perf/test): N+1 guards for documents + literature.** Both views now have a constant-N query-budget test (measure warm queries at 3 rows, assert 8 rows cost no more — data-count-independent, so it catches a real per-row regression, not just a fixed budget). Locks in AUDIT #21's "constant query" finding. 712 pytest green. Backlogged #226 (extend the pattern to queue/matrix/dashboard).
- **★ #229 CI PATH-FILTER FIX (2026-06-14): #228 wouldn't have shipped!** #228 touched only core/desktop_runtime.py, but desktop-release.yml's push filter was `desktop/**` only → no installer rebuild (the frozen server bundles that file). Added core/desktop_runtime.py, run_desktop.py, config/settings/desktop.py, pyproject.toml to the trigger paths + guard test. Editing the workflow (in its own paths) kicked **build #9 (ce23ef3, QUEUED)** which carries the #228 initdb fix. Lesson: desktop fixes outside desktop/ silently don't rebuild — now covered.
- **★ OWNER WINDOWS BUG #228 (2026-06-14): initdb.exe exits 1 — the REAL cause.** The #224 logging worked: atlas-server.log showed `initdb.exe` runs but exits status 1 (so #224's socket fix was for a later step). My code didn't surface initdb's stderr, so still partly blind. #228: (1) `_run` now raises RuntimeError WITH stdout+stderr for every pg helper → the next log shows initdb's actual complaint; (2) wipe a half-built pgdata (no PG_VERSION) before initdb so leftover state from prior failed runs can't make every retry fail "directory not empty"; (3) strip the `\\?\` extended-length prefix Tauri puts on the binary path (initdb mis-resolves share/ from it). 3 guard tests. Pushing rebuilds the installer; if still failing, the next log names the exact initdb error. (owner's path: C:\Users\AliZa\AppData\Local\Atlas\pg\bin\initdb.exe; data C:\Users\AliZa\AppData\Roaming\com.atlas.research). Remaining #225: in-app error page.
- **★ OWNER WINDOWS BUG #224 FIXED (2026-06-14): "127.0.0.1 refused to connect".** Owner's install launched (window opened) but the bundled server never served. Root cause: ensure_postgres passed `-k <socket_dir>` inside pg_ctl's space-split `-o` string — Windows has no unix socket there and a space in the username splits the arg, so `pg_ctl start` failed → run_desktop crashed → blank ERR_CONNECTION_REFUSED. FIX: pass `-k` only on POSIX (TCP loopback is used everywhere anyway); raise a RuntimeError with postgres.log's tail on start failure; and tee the frozen server's stdout/stderr to <data-dir>/atlas-server.log (windowed build has no console → crashes were invisible). Pushing rebuilds the installer for the owner to retry. 2 guard tests; can't run Windows here (fix by code reasoning; the new log will confirm). Backlogged #225 (next suspects + a friendly error page if wait_for_port times out). 710 pytest.
- **★ AUDIT #21 (2026-06-14): CLEAN — no findings.** Swept the work since #20 (filter persistence #187/#212/#214, review fix #216, schema-quality #218/#219/#220, breadcrumb #222, desktop D2b). Security: anon gating intact (302/401); the new persisted filters/tag/folder are allow-list-validated or project-scoped (hostile `'; DROP`, `1 OR 1=1`, foreign folder pk → 200/All or 404, never injection/500); breadcrumb folder lookup project-scoped + autoescaped; schema exposes the X-API-Key scheme but NOT the key value. Perf: breadcrumb adds 0 queries (13 root = 13 nested), weekly_review still 5 q, literature 6 q/20ms warm. npm audit --omit=dev = 0. 708 tests green. Report in AUDITS.md.
- **★ SLICE #222 SHIPPED (2026-06-14, owner #14 / UI): clickable folder breadcrumb on the documents page.** Viewing a nested folder previously showed `current_folder.path` as plain text — you couldn't jump to a parent without going back to the tree. Now the header renders a clickable breadcrumb (root "Documents" → each ancestor → current leaf), backed by a new `Folder.ancestors` property that walks the parent chain exactly like `.path` (so zero added queries — it replaces the path render). Live-verified on real data (Data / Pilot); 2 tests; 708 pytest green. Closes owner #14's "breadcrumbed folder navigation" remainder.
- **★ SLICE #220 SHIPPED (2026-06-14, API contract): schema stays clean — regression guard.** test_schema_generates_without_warnings runs `manage.py spectacular --fail-on-warn --validate` in a subprocess and asserts exit 0, so any future undocumented endpoint / unresolved type / enum collision fails the suite rather than silently degrading the MCP-facing contract. Locks in #218/#219. 706 pytest green. Backlogged #221 (in-process generator if test time ever matters).
- **★ SLICE #219 SHIPPED (2026-06-14, API contract): OpenAPI schema is now 100% clean.** The last spectacular warning was an enum-name collision — ManuscriptFile.Kind and SubmissionEvent.Kind share the field name with different choice sets, merged into a generated "Kind0fbEnum". Added ENUM_NAME_OVERRIDES mapping each to a distinct component name. Schema generation: 0 warnings, 0 errors, exit 0 (down from the original 139+2). Guard test asserts both enum component names. 705 pytest (settings+test only). Backlogged #220 (a blanket "0 spectacular warnings" guard test).
- **★ SLICE #218 SHIPPED (2026-06-14, API contract): OpenAPI schema now documents the X-API-Key auth.** drf-spectacular couldn't resolve the custom APIKeyAuthentication → warned on every view and shipped a schema with NO security scheme (the contract the MCP server + clients read, §6). Registered an OpenApiAuthenticationExtension (api/schema.py → apiKey/header/X-API-Key, name ApiKeyAuth) imported from ApiConfig.ready(); added return type hints to 3 SerializerMethodFields; added request= inline_serializers to the two APIViews spectacular was DROPPING (BotAction, Comments). 139 warnings + 2 errors → 1 warning (cosmetic enum collision, #219) + 0 errors. Live-verified the running /api/schema/ exposes securitySchemes.ApiKeyAuth + the two endpoints; guard test. 705 pytest green.
- **★ SLICE #216 SHIPPED (2026-06-14, bugfix): cross-project review note links no longer broken.** On `/review` (no route slug) every "Notes written" item linked to `/projects//notes/{id}` because `weekly_review()` only exposed the project NAME. Added `project_slug` to every review item (papers/notes/milestones/decisions/experiments — no extra queries, select_related already loads the projects) and switched the note Link to `n.project_slug`. Live-verified on real data (note 42 → `/projects/attention-and-memory/notes/42`); data-layer test + built-JS guard; rebuilt the SPA. 704 pytest green. The slug is now in the payload for the API/MCP consumers too (#217: make the project chips links).
- **★ SLICE #214 SHIPPED (2026-06-14, bugfix): Clear-filter links actually clear again.** Persisting the literature (#187) & documents (#212) filters silently regressed their "Clear" links — a bare/scope-only URL now RESTORES the remembered filter from the session. Fixed both templates to send explicit empty params (`?status=&priority=`, `?…&tag=`) so the session stores "" and clears. Reproduced live, fixed, re-verified; 2 regression-guard tests on the rendered hrefs. 703 pytest green. Lesson logged: a persisted control's reset must send the empty value, not omit the param.
- **★ SLICE #212 SHIPPED (2026-06-14, UI/UX): documents list remembers its tag filter, per project.** `documents_index` persists the tag filter via `remembered_choice` with a slug-namespaced session key (`doc_tag:{slug}` — realises the #194 namespace idea, no helper change), validated against the project's live tag pks so "", a deleted tag, or a foreign pk all fall back to All. Folder stays navigational. Live-verified against the real DB (set→restore→clear→foreign-rejected→per-project-isolated); 4 tests; 701 pytest green. Backlogged #213 (a remembered_filters wrapper if a 3rd filtered surface appears).
- **★ SLICE #187 SHIPPED (2026-06-14, UI/UX): per-project literature remembers its filters.** The status & priority filters now persist across visits via the shared `remembered_choice` helper (like the sort order #190); `""` ("All") is a real persistable choice that clears a remembered filter, every value allow-list-validated before the ORM (#192). Live-verified against the real DB (attention-and-memory, 22 refs): set→store→restore→All-clears→hostile-rejected. 3 tests; 697 pytest green. Backlogged #212 (documents tag filter — needs per-project session namespacing, #194).
- **★ SLICE D2b SHIPPED (2026-06-14): in-app update now really restarts the app.** The "Update ready — restart" button only did `window.location.reload()` (reloads the webview, leaves the old binary + old bundled server running → an installed update never took effect). Added a `restart_app` Tauri command (`app.restart()`, updater.rs) registered in main.rs; UpdaterButton.tsx invokes it instead of reloading. Full gate green: ruff fmt+check, tsc 0 errors, cargo check, 694 pytest. Guard test asserts restart_app is exposed + the button calls it.
- **★ AUDIT #20 (2026-06-14): CLEAN.** Reviewed the self-contained desktop epic (#210). The bundled Postgres is a sound local-trust model: trust-auth but bound to 127.0.0.1 only + a private socket, ALLOWED_HOSTS=localhost; no user-controlled SQL/shell in desktop_runtime (hardcoded DB_NAME, subprocess lists); single-instance protects DB integrity; web surfaces still 302 anon; 0 npm vulns; CI uses only GITHUB_TOKEN. One hardening note backlogged (#211: socket-only/password instead of trust). Report in AUDITS.md. 694 tests.
- **★ #210 POST-SHIP FIXES (owner ran the Windows installer, 2026-06-14) — SURVIVED ROLLBACK #6.** Installers built green both OSes; owner launched the Windows one → it ran (freeze + Tauri launch worked!) but crashed "initdb not found" because Windows ships initdb.exe. FIXED: _pg_bin checks <name> + <name>.exe and both ATLAS_PG_BIN + ATLAS_PG_BIN/bin; main.rs passes the pg root. ALSO owner-requested SINGLE INSTANCE → added tauri-plugin-single-instance (focuses existing window, exits — prevents 10 windows / 10 Postgres on one data dir). cargo check + pg_bin tests green. Shipping these auto-rebuilds the installers (watch + iterate). AUDIT #20 overdue after.
- **★ NEXT — OWNER-REQUESTED EPIC (2026-06-14): Desktop distribution & auto-update.** Owner wants ready-to-install Linux + Windows binaries AND an in-app "update the app" button. Plan in DECISIONS backlog (D1 installers + release CI, then D2 tauri-plugin-updater button). Author + structurally verify here (cargo check, workflow lint); actual binaries are produced by CI / on the owner's machine (no GUI in this headless container). Start with D1.
- **★ OWNER wants "one click install + one click in-app update" (2026-06-14) — BUILD NOW AUTO-RUNNING.** BREAKTHROUGH: a branch-scoped `push` trigger (paths desktop/**) on desktop-release.yml DOES fire Actions from my pushes (the old CI just never ran because it was main-only). Commit 273e4ed auto-started run 27503064513 (event=push) building Linux+Windows installers → rolling "desktop-preview" DRAFT release. So installers are produced with ZERO clicks; the owner just downloads from the release. WATCHING the run to green + will fix any failure (first-build risks: tauri-action, deps, frontendDist). Icons hardened (fd9e159). ONE-CLICK UPDATE (D2/D3): button built; still needs the owner's one-time signing keypair (private key can't live in my sandbox — security boundary). Backlog #210 = bundle the Django server into the binary (fully self-contained app).
- **★ INSTALLERS BUILT + OWNER INSTALLED (2026-06-14):** auto-build run 27503064513 succeeded → Linux+Windows installers on the "desktop-preview" draft release (owner found+downloaded them). Owner hit 2 walls: (1) Windows Smart App Control blocks the unsigned .exe → needs code signing; (2) launched app shows "localhost refused to connect" because the shell needs the Atlas server on their machine. → NOW BUILDING EPIC #210 (self-contained app).
- **★ EPIC #210 IN PROGRESS — self-contained desktop app (SQLite + Python sidecar).** 210a SHIPPED: config/settings/desktop.py (SQLite in ATLAS_DATA_DIR, huey immediate/MemoryHuey, WhiteNoise, persisted secret, DEBUG off) — `check` + full `migrate` succeed on SQLite (whole schema is SQLite-compatible!), 2 subprocess tests, doesn't touch dev/prod. NEXT: 210b search FTS→icontains fallback, 210c run_desktop entrypoint, 210d PyInstaller, 210e Tauri sidecar, 210f release CI.
- **210b SHIPPED:** core/search.py falls back to an icontains LIKE search when connection.vendor != postgresql (SQLite desktop), same result shape; Postgres keeps FTS. Verified end-to-end on the SQLite settings + unit test. Global search now works in the bundled build.
- **210c SHIPPED:** run_desktop command (migrate + collectstatic + ensure atlas/atlas login + serve via waitress). LIVE-VERIFIED: the bundled server served the login page + static (200) on SQLite, no Postgres/Redis/Docker — the whole app runs self-contained. waitress added as a dep (gunicorn is Unix-only).
- **210d SHIPPED:** PyInstaller freeze — desktop/server/atlas_server.{py,spec}. BUILT + RAN STANDALONE ON LINUX: the 14MB atlas-server binary served login (200) + static (200) on fresh SQLite with nothing else installed. 4 of 6 #210 pieces done; the self-contained server is real and frozen.
- **★ OWNER DECISION (AskUserQuestion): BUNDLE FULL POSTGRES, not SQLite.** "no need to migrate to sql, just make it install automatically." → #210g SHIPPED: config/settings/desktop.py points at a local postgres; core/desktop_runtime.py auto-initdb + starts + creates db + atexit-stops it; run_desktop starts it before migrate. VERIFIED LIVE (non-root + pg16): the app stood up its own Postgres, migrated, and made the login — fully automatic. (Live initdb can't run as root so the suite guards the pieces; the release CI runs it for real.)
- **210e SHIPPED + FULL E2E PROVEN:** desktop/src/server.rs + main.rs spawn the frozen atlas-server, wait_for_port, load the window; kill on exit. THE WHOLE STACK WORKS: the frozen binary (non-root + pg16) auto-started Postgres, migrated, served login (200), shut Postgres clean. cargo check + 2 Rust tests green. The self-contained app is functionally complete — only packaging the binaries into the installer remains.
- **210f+210i SHIPPED — EPIC #210 FEATURE-COMPLETE:** desktop-release.yml freezes the server (pyinstaller) + fetches portable Postgres (zonky embedded-postgres-binaries) per-OS before the tauri build; tauri.conf bundle.resources ships atlas-server + pg into resource_dir (main.rs resolves them). .gitkeep markers so cargo check passes; real artifacts CI-fetched+gitignored. The desktop/** push auto-triggers the real installer build — WATCH it via mcp__github__actions_list and iterate on cross-platform postgres/pyinstaller failures.
- **★ EPIC #210 COMPLETE — SELF-CONTAINED INSTALLERS BUILT GREEN BOTH OSes (run 27506859617, 2026-06-14):** Windows nsis/msi + Linux deb/rpm, each bundling the frozen Atlas server + portable Postgres. Fix that got there: dropped AppImage (linuxdeploy can't bundle the native pg .so's). The installers are on the "desktop-preview" draft release. The owner's whole ask is delivered: download → double-click → Atlas runs with its own database, nothing else installed. OWNER-SIDE REMAINING (credentials only they can make): Windows code-signing (kills the Smart App Control block) + the updater keypair (D3).
- **Slice in progress:** — (WATCH build #9 (ce23ef3, #228 initdb fix) to green → tell owner to delete %APPDATA%\com.atlas.research, re-download, retry; if still failing the new log shows initdb's exact stderr → fix precisely. Then loop resumes: owner #14 drag-rows / #225 in-app error page / #226 more N+1 guards. AUDIT #22 ~7 cycles out.)
- **Last completed slice (#209, UI/UX):** PROMPTS GALLERY COUNT — "N prompts saved" / "N matching the filter" footnote, matching the library + literature lists (#199 family); pluralized + filter-aware. Live-verified ("3 prompts saved."); 2 test assertions. 685 tests.
- **Last completed slice (#206, tooling/desktop):** DOCTOR UPDATER MISCONFIG CHECK — doctor now distinguishes placeholder-pubkey + createUpdaterArtifacts OFF (warn, benign — #203) from placeholder + createUpdaterArtifacts ON (FAIL, exit 1 — the release build would break since Tauri can't sign without a real key). Test injects a misconfigured tauri.conf via a tmp BASE_DIR and asserts the failure. 684 tests.
- **Last completed slice (#207, Owner #25 density, UI/UX):** RESEARCH-SECTION DENSITY — the three research tabs (ledger/datasets/experiments, which share a tab bar) widened to base_wide max-w-6xl + header mb-6→4 so switching tabs no longer jumps width; the datasets registry table (a genuine wide table the #25 sweep hadn't reached) tightened py-2→1.5. All 3 added to the base_wide guard list (now 12 pages). Live-verified all three 200 at width 1056, no JS errors. 683 tests.
- **Last completed slice (#203, tooling/desktop):** DOCTOR UPDATER-PUBKEY WARNING — `manage.py doctor` now reads desktop/tauri.conf.json and WARNs (exit 0, not a failure) when the updater pubkey is still the REPLACE_ME placeholder, pointing at the desktop/README D3 steps (says "signing configured" once the owner sets it). Surfaces the one-time signing step so a release isn't tagged expecting auto-update. Test covers the warning + exit 0. (Also closed #202 already-covered: ci.yml's audit job runs make audit → npm audit --omit=dev already.) 683 tests.
- **Last completed slice (#204, UI/UX):** WRITING BOARD EMPTY-STATE ACTION — _board.html's {% empty %} now takes an optional empty_action_url/label; the per-project Writing board offers "Start a manuscript" (writing:create) and the global Writing home "Go to a project", honoring the product guideline that every empty state offers its primary action (most others already did — the board was the gap). Live-verified on a fresh project (action renders, test project cleaned up); test asserts the action + create URL. 682 tests.
- **Last completed slice (AUDIT #19):** SECURITY+RESPONSIVENESS AUDIT (since #18; helper refactor, base_wide, perf trims, Vite 8, desktop D1+D2). Clean. Web order/persist/count surfaces auth-gated + non-reflective (hostile ?sort not echoed) + <50ms. The new desktop auto-update path (D2) reviewed safe BY DESIGN: check_for_updates has no caller URL — endpoint is the configured GH feed, only signature-verified builds install (can't run arbitrary code); inert until D3 anyway. Release workflow leaks no secrets (drafts only). withGlobalTauri IPC exposure noted as within the single-user local-trust model (terminal-isolation guard still passes). npm audit --omit=dev = 0 vulns. Report in AUDITS.md. 681 tests.
- **Last completed slice (Owner epic D2, auto-update):** IN-APP "CHECK FOR UPDATES" BUTTON — tauri-plugin-updater wired (Cargo + main.rs plugin + check_for_updates command in updater.rs + updater:default capability + tauri.conf plugins.updater pointing at the GH Releases latest.json, pubkey placeholder, withGlobalTauri). Frontend UpdaterButton.tsx: desktop-only SPA sidebar control (idle/checking/up-to-date/update-ready→restart/error), renders null in browser. createUpdaterArtifacts kept FALSE so D1 installers keep building until the owner's one-time keypair setup (documented in README → backlog D3). cargo check + tsc pass, SPA verified clean, guard test asserts the whole chain. Button only exercisable in a real desktop build. The OWNER EPIC (D1+D2) is feature-complete; D3 is the owner's one-time signing step.
- **Last completed slice (Owner epic D1, desktop dist):** READY-TO-INSTALL INSTALLERS — tauri.conf.json bundle targets now explicit [deb, appimage, rpm, nsis, msi] + publisher/category/longDescription; new .github/workflows/desktop-release.yml (push tag v* or manual dispatch → ubuntu-22.04 + windows-latest matrix → installs webkit/gtk deps → tauri-action builds+bundles+uploads to a DRAFT GitHub Release); desktop/README "Download / install" section. cargo check passes (reinstalled the rollback-wiped webkit2gtk dev libs); yaml/json validated; 2 new scaffold guard tests (targets list + workflow present). Real binaries are produced by CI on a tag — no GUI build possible in this headless container.
- **Last completed slice (#159, security/tooling):** VITE 6→8 UPGRADE — bumped vite ^6→^8 + plugin-react ^4→^6; npm audit --omit=dev now 0 vulns (esbuild advisory GHSA-gv7w-rqvm-qjhr closed). Vite 8 reshuffled chunk module-id matching, so updated chunkFileNames: editor-core chunk now detected by "codemirror" (the shared CM bundle), and Vite 8's opaque "chunk"/"dist"/"index" fallbacks map to a tidy "shared-chunk.js". Clean-rebuilt (rm islands/* first) → no stale orphans (removed client-chunk/index-chunk + the now-inlined TerminalPanel.css). tsc clean; all 5 key surfaces live-verified (0 JS errors, 0 failed JS) + Files screenshot. The big remaining backlog item is now closed.
- **Last completed slice (#196, tech/test):** TIGHTEN BASE_WIDE GUARD — test_wide_pages.py now also fails if any of the 9 wide pages re-declares {% block main_class %} (base_wide.html owns the width; a page re-setting it is redundant/conflicting). 3 guard tests. (Also closed #200 as already-covered: the DocumentsTable island already shows the count via its filter placeholder + "N matches" badge — no change needed.) 678 tests.
- **Last completed slice (#199, UI/UX):** COUNT LINES ON THE LIT LISTS — the per-project Literature list footnotes "N references linked to this project" (or "match the current filter" when filtered) and the reading queue "N papers still to read", matching the library index's existing count — a calm situational-awareness cue (product value #1: "where is what, how is it going"). Pluralized + filter-aware. New test; live-verified both (22 refs / 14 to-read), no JS errors.
- **Last completed slice (#197, perf):** FOLD FOLDER_TREE'S QUERY — folder_tree(project, prefetched=None) now accepts a pre-loaded folder list; its one caller (documents_index) loads project.folders.all() ONCE and feeds it to the nested tree + bulk-move select + row selects. Folder-table queries 4→3 (total 12→11); documents budget tightened 16→15. Fallback keeps folder_tree standalone-callable. 676 tests.
- **Last completed slice (#181, perf):** DOCUMENTS FOLDER-QUERY REDUNDANCY — investigated the folder-tree sidebar queries: folder_tree itself is already one query (in-memory nesting, no N+1); the waste was the bulk-move <select> re-fetching project.folders.all despite the #180 cached `folders` context var. Pointed it at the cache → folder-table queries 5→4, total 13→12; tightened the documents budget test 18→16 to lock the gain. Remaining folder_tree own-query split to #197. 676 tests.
- **Last completed slice (#195, tech/test):** BASE_WIDE GUARD TEST — core/tests/test_wide_pages.py asserts the 9 dense pages extend base_wide.html and that base_wide.html keeps {% extends %} first + max-w-6xl, so the #25 width and the #183 extends-first gotcha can't silently regress. 2 fast grep tests (676 total).
- **Last completed slice (#183, tech/UI):** SHARED WIDE-CONTENT BASE — new templates/base_wide.html extends base.html and sets main_class=max-w-6xl; the 9 density'd pages now extend it instead of each re-declaring the block (one place for the #25 width). GOTCHA caught by live verify before shipping: a leading {% comment %} made {% extends %} non-first → 500 on every page; switched to a {# #} comment after extends. All 9 re-verified 200 at width 1056, no JS errors. 674 tests.
- **Last completed slice (#193, UI/UX):** READING-QUEUE ORDER PERSISTENCE — the queue order (Priority / Fill matrix gaps) now persists via the new core.session.remembered_choice helper ("priority"/"gaps" allow-list); the Priority pill links to ?order=priority so it always records intent. New session test; live-verified "gaps" stays active across a bare revisit. Third consumer of the #191 helper (library sort, project-lit order, queue order). 674 tests.
- **Last completed slice (#191/#192, tech/security):** SESSION-REMEMBER HELPER — new core/session.py remembered_choice(request, param, session_key, allowed, default) centralizes the read-?param-or-restore-from-session dance with VALIDATE-BEFORE-STORE (fixes AUDIT #18's #192: junk is never persisted, still re-validated on read). library_index (#176) + project_literature (#190) refactored onto it (−~15 lines, behavior identical — existing round-trip tests stay green). 4 new helper unit tests incl. hostile-value-not-stored + tampered-session. 673 tests.
- **Last completed slice (AUDIT #18):** SECURITY+RESPONSIVENESS AUDIT (since #17; #180 N+1 fix, #179/#182 density, #168-169 typeahead, #176/#190 session-persisted sort, #189 project-lit order, #177 a11y). All sort/order surfaces whitelisted (hostile→default), non-reflective (exact-payload XSS re-check clean — earlier `<script>`-in-body match was a false positive on the page's own script tags), auth-gated (302 anon). Poisoned session order is validated on read → inert. npm audit --omit=dev = 0 vulns, no new deps. Timings <50ms, sort adds 0 per-row queries (no N+1, #180 holds). One tidy-up backlogged (#192 validate-before-store). Report in AUDITS.md. 669 tests green.
- **Last completed slice (#190, UI/UX):** PERSIST PER-PROJECT LITERATURE ORDER — the order-pill choice saves to session["literature_order"]; visiting the per-project Literature page with no ?sort= restores it (default "added"), mirroring #176's library sort. New round-trip test. Live-verified: choosing Title persists across a bare revisit, no JS errors. 669 tests.
- **Last completed slice (#189, UI/UX):** PER-PROJECT LITERATURE ORDER CONTROL — the per-project literature list (a _link_row list, not a column table) gets order pills: Recently added (default), Title, Year, Most cited — via a LITERATURE_SORTS whitelist (field, desc) with nulls_last + reference-title tiebreaker; the active status/priority/keyword filters carry through base_params. 2 new tests (year ordering + hostile-sort fallback → title). Live-verified: "Most cited" activates (?sort=cited), list reorders, no JS errors, screenshot reviewed.
- **Last completed slice (#177, [REV]/a11y):** DOCUMENTS-TABLE SORT A11Y — found the per-project documents table was ALREADY sortable client-side via its React island (reverted a redundant server-side attempt), then made its sort headers accessible: aria-sort reflects the active column+direction, scope="col", and headers are keyboard-operable (Enter/Space toggles), matching the library's server-rendered aria-sort. Live-verified: default Added=descending, click Title→ascending (Added→none), keyboard Enter toggles, no JS errors. tsc + 666 tests green.
- **Last completed slice (#176, UI/UX):** PERSIST LIBRARY SORT — an explicit ?sort= on /library/ is saved to request.session (library_sort/library_dir); visiting with no sort param restores the last-used order instead of snapping back to title-asc. New test (sorted visit → bare visit keeps order). Live-verified: Year-desc persists across a param-less revisit, desc arrow shown, no JS errors. 666 tests.
- **Last completed slice (#182, Owner #25 density, UI/UX):** PROJECT-LITERATURE INDEX DENSITY — literature/project_literature.html widened max-w-5xl→6xl + header mb-6→4; filter bar / keyword cloud / link list were already tight. Template-only. Live-verified 200 at width 1056, no JS errors, screenshot reviewed. This was the last untouched literature surface — the #25 density sweep is now consistent across overview, dashboard, plan, writing board, library, documents, reading queue, matrix, and project literature.
- **⚠ ROLLBACK #4 (2026-06-14):** container reset local checkout to fb85a2e mid-#168/#169. Recovered: fetch → stash junk → ff-merge to 21a97d7 → dockerd/compose/migrate → revive runserver+huey → npm install (node_modules was incomplete) → regenerate /tmp/shots auth + screenshots. The lost in-flight #168/#169 was re-applied from scratch and re-verified. GitHub remote is the source of truth.
- **Last completed slice (#168/#169, [REV], file epic):** TYPEAHEAD POLISH — the Files tree typeahead now shows the current buffer as a small dark mono pill (top-right of the tree, auto-clears 1s after the last keystroke), and Escape (swallowed) + onBlur clear the buffer so returning never triggers a stale jump. DOM-verified across runs: pill span carries the typed text ('a'/'l'), Escape removes it, jump still works ('l'→Literature), no JS errors. 665 tests, ruff+tsc clean.
- **Last completed slice (#179, Owner #25 density, UI/UX):** READING-QUEUE + REVIEW-MATRIX DENSITY — reading_queue.html + matrix.html widened max-w-5xl→6xl, order-filter/intro margins 6→4, matrix th/td py-2→1.5. Finishes the #25 table/list sweep across literature (library #165, documents #171, queue+matrix #179). Template-only. Both live-verified 200 at width 1056, no JS errors, matrix screenshot reviewed (calm, cohering with the lucide icon nav).
- **Last completed slice (#180, perf/tech, AUDIT #17 fix):** DOCUMENTS MOVE-SELECT N+1 FIXED — `_doc_row.html` no longer re-queries `project.folders.all` per row; the index view passes one cached `list(project.folders.all())` and the row iterates `folders|default:project.folders.all` (HTMX single-row swaps still fall back to one query). Folder-table queries dropped 10→5, now row-independent. Guarded by a new django_assert_max_num_queries(18) test (5 folders × 15 docs) that would fail (~28 q) without the fix. 665 tests.
- **Last completed slice (AUDIT #17):** SECURITY+RESPONSIVENESS AUDIT (since #16; #167 typeahead, #165/#171 density, #161/#173 nav icons, #170 sortable library). The new library ?sort=/?dir= surface is auth-gated (302 anon), whitelisted (hostile sort → title fallback, no ORM injection), and non-reflective (hostile value not echoed; no XSS). Icon partials render static SVG/literals only. npm audit --omit=dev = 0 vulns, no new deps. Timings all <50ms (library 16ms/6q, sort adds 0 query cost). ONE pre-existing find: documents move-select N+1 (folders re-queried per row) → backlogged #180. Report in AUDITS.md. 664 tests green.
- **Last completed slice (#171, Owner #25 density, UI/UX):** DOCUMENTS TABLE DENSITY — the per-project Documents page widened max-w-5xl→6xl (block main_class), header mb-6→4, table header + every _doc_row.html cell py-2→1.5; folder rail already on .card. Template-only. Live-verified: 200, main width 1056, rows render, no JS errors, screenshot reviewed (cohering with the new lucide sidebar + subnav). Completes the #25 table sweep with #165 (library). Remaining #25: reading-queue/matrix (#179).
- **Last completed slice (#170, UI/UX):** SORTABLE LIBRARY COLUMNS — the /library/ Reference/Year/Venue/Cited headers are server-side sort links (?sort=&dir=, no JS) via a reusable literature/_sort_th.html partial; a LIBRARY_SORTS whitelist keeps ?sort= off arbitrary ORM fields (hostile value falls back to title), nulls_last keeps blank years/venues off the top, title is the stable tiebreaker, the search query carries across sorts, and the active column shows ↑/↓ + aria-sort. 4 new tests (incl. injection-style fallback). Live-verified: Year asc 1995→2000, desc 2022→, no JS errors, screenshot reviewed.
- **Last completed slice (#173, UI/UX):** LUCIDE ICONS ON THE PROJECT SUBNAV — all 11 context tabs (Overview/Plan/Documents/Literature/Questions/Writing/Notes/Research/Graph/Decisions/Edit) carry a calm Lucide glyph from core/_nav_icon.html (Literature reuses the library glyph); the bar is flex-wrap so icons never overflow. 2 added guard tests (subnav↔partial lockstep), 6 total in test_nav_icons.py. Live-verified on a project page: 11 SVGs, no JS errors, screenshot reviewed (Overview active, calm). One icon language now spans sidebar + subnav + workspace.
- **Last completed slice (#161, UI/UX):** LUCIDE ICONS IN THE CLASSIC SIDEBAR — new core/_nav_icon.html partial inlines the Lucide (MIT) stroke set so the Django sidebar shares the React workspace's icon language: Dashboard/Projects/Library/Writing/Prompts/Inbox/Assistant + the mobile menu button; ☰ and ✨ emoji retired. 4 guard tests keep base↔partial in lockstep. Live-verified on /library/: 7 nav SVGs render, no JS errors, sidebar screenshot reviewed (calm).
- **Last completed slice (#165, Owner #25 density, UI/UX):** LIBRARY INDEX DENSITY — the /library/ index widened max-w-5xl→6xl (block main_class override), header + search margins mb-6→4, table header/row padding py-2→1.5; calmer, more rows above the fold. Template-only. Live-verified: 200, 25 rows, main width 1056 (>1024), no JS errors, screenshot reviewed; library query-budget + density-token guard tests pass. #25 remaining: documents/folder tables (#171).
- **Last completed slice (#167, [REV], file epic):** TYPE-TO-SELECT in the Files tree — typing letters jumps focus to the next visible row whose name starts with the buffer (Finder/VS Code behavior); 800ms reset window, modifier-aware so Ctrl-P quick-open is untouched, repeated first-letter cycles matches. Built on the [REV] arrow-nav flat-row model. Live-verified with Playwright: typing 'a' on attention-and-memory/files jumped to analysis-notes.md, no JS errors. 655 tests green, ruff+tsc clean.
- **✅ OWNER #30 — FILE-WORKSPACE / IDE / DESKTOP EPIC: FEATURE-COMPLETE (2026-06-13).** Plan: docs/plans/2026-06-12-file-workspace-ide-epic.md. Shipped, all green/pushed:
  - **Unified file tree** — manuscript files + documents in ONE per-project tree; manuscript side stays live via a signal mirror (ManuscriptFile→Document), contract byte-identical (frozen contract tests + 6 MCP tools unaffected). Slices 1a-1c-ii.
  - **Files page / explorer** — nested tree, open-anything preview (CM6 text, vendored pdf.js for PDF, papaparse CSV, images, download fallback), in-place text editing, folder create / file rename / delete / **drag-drop upload** / **move**, **Ctrl-P quick-open**. Manuscript nodes scoped out of the general UI + write-protected (403). Slices 2a-2d-iv, 9.
  - **Desktop app** — `desktop/` Tauri 2 shell wrapping the localhost server; `cargo check` passes; `cargo tauri build` → app. Slice 3.
  - **Terminal** — real PTY (portable-pty) in the Rust shell + xterm.js panel; desktop-only, grep-guard-isolated from web/MCP. Slice 5.
  - **Open any file from disk** — Tauri dialog picks a file, reads only it (text, 5MB cap). Slice 6.
  - **Project templates** — 4 built-in research scaffolds + "save project as a template" (ProjectTemplate model); picker on create, both shells. Slices 7-8.
  - **MCP workspace tools** — list/create projects+templates, list/read/write project files (thin httpx, AST-pure).
  - **AUDIT #15** — clean; npm audit scoped to prod deps; backlog #159 (vite8) / #160 (webview-nav).
- **⚠ RESILIENCE:** survived THREE container rollbacks this session (local .git + DB reset to cycle 104 each time). Recovery recipe (now in the watchdog): git fetch → stash junk → ff-merge to origin → dockerd/compose/migrate/revive runserver+huey → npm install. The real GitHub remote is always the source of truth.
- **Last completed slice (cycle 146, Backlog #156, UI/UX):** SPA NEEDS-ATTENTION — /api/v1/dashboard/ gained the attention block (overdue/deadlines/inbox, same endpoint, no extra fetch); Dashboard.tsx renders the answer-first lead incl. "All clear"; live-verified overdue row + triage links; API test. Both shells lead with the answer now. 562 tests.
- **Last completed slice (cycle 145, [REV], UI/UX):** DASHBOARD AS MISSION CONTROL — new needs_attention() leads the classic dashboard: overdue milestones (red), manuscript deadlines ≤14d (amber/red), untriaged inbox items WITH text + triage links; quiet "All clear" line when empty; stats demoted below, grove+activity share a row (whitespace halved), max-w-6xl + .card tokens. 8 new tests (561 total); live-verified with the real overdue milestone + 2 inbox items.
- **Last completed slice (cycle 144, Backlog #154, UI/UX+tech):** DENSITY LINT + TOKEN SWEEP — test_density_tokens.py fails the build on hand-rolled card markup; the sweep converted all 33 offenders across 15 templates to .card; 8 affected pages browser-verified (200, no errors, no 4xx). 553 tests. **+ owner-prompted DOGFOOD ASSESSMENT:** caught milestone ledger stalled at cycle 131 (logged 132-144, now 66/72) and inbox 54/66 untriaged (triaged to 2 real to-dos); added the anti-decay clause to Loop rules — every ship logs a milestone on atlas-self-build, not just a quick-capture.
- **STANDING RITUAL (per ship):** code+tests → gate → strike+≥1 idea (DECISIONS) → PROGRESS → quick-capture AND log+complete a milestone on atlas-self-build (phase 30) → commit → push → in-sync → arm next chain + fresh watchdog. Triage inbox to single digits each audit cycle.
- **Prior slice (cycle 143, Owner #25 + Backlog #153, UI/UX):** PLAN DENSITY + DENSITY TOKENS — plan page max-w-6xl, margins 6→4, milestone rows py-2→1.5; .card/.card-title born in app.css @layer components with overview + plan as first consumers; HTMX milestone check-off re-verified on the dense layout (toggle→4/9→untoggle→3/9). 551 tests green.
- **Prior slice (cycle 142, Owner #25 density, UI/UX):** PROJECT OVERVIEW DENSITY — the heart page widened max-w-5xl→6xl and tightened throughout (header mb-6→2, desc mb-8→4, phase card py-4→3, cards p-5→4, gaps 4→3, lists space-y-2→1, headers sm→xs); cards reach ~1390px (was ~1300), more above the fold. Before/after screenshots reviewed. Template-only; 551 tests green. #25 remaining: dashboard, plan, tables, library, board.
- **Prior slice (cycle 141, Backlog #151, tech improvement):** ESCAPE DISCIPLINE — esc() hoisted to the top of latex-editor.js and applied to EVERY innerHTML ${} (audit fixed the research panel; this swept diagnostics/hypotheses/comment-date too). New pytest guard writing/tests/test_glue_escapes.py greps the file and fails the build on any unescaped innerHTML interpolation — verified it catches a reverted escape. 551 tests.
- **Prior slice (cycle 140, AUDIT #14):** reviewed cycles 131-139 (TTS+stage voice, CI smoke/artifacts/404 probe, layout presets+check, vim lazy chunk, icon rail, icon sweep). ONE REAL FIND, fixed+proven: stored XSS via external metadata (Crossref titles etc.) in the research panel's innerHTML — esc() on every interpolation, hostile-title test renders inert. TTS surface clean (403 anon, capped, stage server-side); CI artifacts carry no secrets; deps MIT/pinned/local; pip+npm audits clean; in-process timings 5-18ms (<50ms bar). Report in AUDITS.md.
- **Prior slice (cycle 139, Backlog #144+#147, UI/UX):** ACTIVE-LAYOUT CHECK + AUDIT ARTIFACT — preset name sticks in atlas-editor-layout with an indigo SVG check beside the active View-menu preset; any divergence (manual toggle, rail collapse, divider drag) clears it; applyingPreset flag prevents init/preset self-clears. make audit tees to /tmp/audit-output.txt (pipefail, exit preserved) and joins the failure() upload. 9-check battery ALL PASS.
- **Prior slice (cycle 138, Backlog #142, Owner #29 follow-on):** PET VOICE PERSONALITY — STAGE_VOICES shapes Piper per stage (egg slow+soft murmur, hatchling fast peep, scholar default, sage measured); read_aloud derives stage server-side via pet_state(). Durations verified distinct on the real voice (1.94/2.25/2.59s); endpoint 200; 4 new tests incl. STAGES↔STAGE_VOICES sync guard (549 total).
- **Prior slice (cycle 137, Backlog #146, UI/UX):** ICON SWEEP — editor chrome unified on the rail's monochrome stroke-SVG language: Recompile, zoom-fit, upload, download-zip, the 3 layout presets, PDF/research toggles, and the comment gutter dot (now an indigo SVG chat bubble in the island, .comment-dot; smoke locator updated). Ω kept as the semantic symbol-palette label. 12-check battery + smoke ALL PASS, screenshot reviewed.
- **Prior slice (cycle 136, Backlog #143+#145, tech improvement):** SMOKE ARTIFACTS + 404 PROBE — editor_smoke.py check 1 fails on any >=400 response (favicon tolerated, URLs printed); on ANY failure incl. pre-check crashes (try/except) it writes editor-smoke.png + console log + failure list to SMOKE_ARTIFACT_DIR; ci.yml uploads them + /tmp/server.log via upload-artifact on failure(). Both paths verified live (green = no artifacts, forced bad-password = exit 1 + 3 artifacts).
- **Prior slice (cycle 135, [REV], UI/UX):** LEFT ICON RAIL — Overleaf parity-plan cycle 5: ~40px vertical rail (Files/Outline/History/Research, inline-SVG stroke icons), one drawer at a time, active icon collapses, choice persists; Research icon delegates to the right-side panel (deliberate deviation, logged); sidebar restore strip removed (rail = restore). 13-check battery + editor smoke ALL PASS, screenshot verified.
- **Prior slice (cycle 134, Backlog #137, tech improvement):** CM6 BUNDLE SLIM — vim keymap is a dynamic import: first-paint editor payload 208→172KB gz (-17%), vim's 39KB gz fetched only on selection (named chunks core/vim-keymap). Fixed latent bug: Vite's modulePreload helper built URLs against the site base (404 per dynamic import) — polyfill disabled, native import() resolves module-relative. 9-check vim-lazy battery + SPA routes + editor smoke ALL PASS.
- **Prior slice (cycle 133, Backlog #140, UI/UX):** LAYOUT PRESETS — View menu Layout section: ✍ Drafting (editor only), ⇆ Reviewing (editor + PDF 50/50), ▦ Submitting (files + editor + PDF); presets seed the per-layout split-size keys then reuse setSidebar/setPreview. Fixed 2 latent bugs: lone editor pane never grew (flex-grow), collapsed preview didn't survive reload (init skipped hiding). 8-check browser battery ALL PASS.
- **Prior slice (cycle 132, Backlog #141, tech improvement):** CI EDITOR SMOKE — scripts/editor_smoke.py, a 6-check headless Playwright battery (mount + zero CDN editor assets, autosave, multi-file switch preserves buffers, line comment + gutter dot, cite autocomplete, compile wiring) distilled from the 26-check cutover battery; self-seeds a project+manuscript over the API on an empty DB; CI audit job extended (noinput superuser, playwright chromium — no Redis needed, dev huey is immediate). ALL PASS locally; seed POSTs verified; demo manuscript 8 restored.
- **Prior slice (cycle 131, Owner idea #29, UI/UX):** PET VOICE — 🔊 beside the speech bubble on /pet/ and the SPA sidebar speaks the current line via the local Piper /tts/ (200 audio/wav verified on both); strictly opt-in on click, never autoplays. Owner directives #24/#26 core/#27/#28/#29 all delivered.
- **Prior slice (cycle 120, AUDIT #12):** reviewed cycles 111-119 (cite-library, context, MCP LaTeX tools + word-count action, line comments, arXiv zip, pet SVG). Sweep clean, all surfaces authz-scoped, MCP AST constraint holds, timings <50ms, 0 CVEs. ONE defense-in-depth fix: the submission-zip now skips traversal/absolute entry names (+regression test). Report in AUDITS.md.
- **Prior slice (cycle 119, Owner idea #27, UI/UX):** REAL pet — replaced the emoji with a hand-drawn inline-SVG owl, distinct per growth stage (egg→hatchling→scholar→sage w/ cap+sparkle), shared by classic sidebar + /pet/ + React layout (PetSvg.tsx); CSS breathing/blink/sparkle/hop, sleeping closes eyes. Browser-verified all stages + in-app. NEW owner #26 Overleaf-UI plan saved to docs/plans/ for cycles 121+.
- **Prior slice (cycle 117, beyond-Overleaf B6):** line-anchored editor comments — Comment model gains manuscript_file target with page=line; 💬 gutter dot on commented lines, click either gutter → line thread popover (⌘-Enter posts). Closes Owner idea #10 for the editor. Browser-verified comment→dot→reopen→append. 2 tests.
- **Prior slice (cycle 116, beyond-Overleaf B5):** manuscript writing on the research timeline — latest compile + labeled versions become dated timeline events (kind manuscript_compiled, teal dot), not the 50 auto-snapshots. Query budget held. Live-verified the compile event appears on the timeline API + SPA. 1 test.
- **NEW Owner idea #25 (whitespace/density):** stop centering in narrow max-w-5xl; use full width + tighter rhythm like Overleaf. Standing UI rule; queued UI/UX cycles will sweep dashboard/overview/plan/tables/board.
- **Priority now (per Owner loop rules):** broken builds → owner directives (#24 LaTeX epic DONE — all parity + beyond; #25 density ongoing; #26 Overleaf-UI parity [plan saved, cycles 121+]; #27 real pet DONE) → backlog; every cycle: ≥1 new idea + tech improvement + research-backed design + parallel planners.
- **The LaTeX epic (Owner idea #24) is COMPLETE:** all 10 Overleaf-parity slices + all 7 beyond-Overleaf features (B1 library cite autocomplete, B2 live cite-check, B3 research panel, B4 MCP edit/compile, B5 timeline compiles, B6 line comments, B7 arXiv zip).
- **Next 3 slices:**
  1. AUDIT #12 [cycle 120]: full security+responsiveness review since audit #11 (MCP LaTeX tools, word-count action, templates, timeline compiles, line comments, arXiv zip, pet SVG)
  2. CM6 migration sub-epic [122-124, Owner #28]: Slice A (single-file CM6 island, closes #114, 11 CDN tags gone) → B (multi-file+comments+lint) → C (cite B1/B2); THEN Overleaf-UI cycles 2+ (top toolbar, History/Share/Layout, Split.js panels, icon rail, polish)
  3. Owner #25 density passes; SyncTeX (only un-built LaTeX nicety); backlog top-down; loop forever
- **Broken:** nothing

## Gate reports

### Phase 6 — Claude integration / MCP (2026-06-10)

**Built:** `mcp_server/` — standalone FastMCP stdio server (official `mcp` SDK) whose `client.py` is a pure httpx client over `/api/v1/` configured by `ATLAS_API_URL` + `ATLAS_API_KEY` (an AST test enforces zero Django/SDK imports in the client); the full initial tool set: list_projects, get_project_overview, get_plan, complete_milestone, list_documents, search, add_reference_by_doi, get_reading_queue, set_reading_status, add_note, quick_capture, run_bib_check; supporting API endpoints added with schema descriptions: `/projects/{slug}/overview|plan|reading-queue|bib-report/`, `/api/v1/search/`, `/api/v1/notes/` (wiki-link sync on create/update); README documents `claude mcp add atlas ...` and a 5-step smoke-test conversation.

**Evidence per acceptance criterion** (clean run; real MCP stdio client driving the server against the live app):
- *List projects:* `list_projects` → `['attention-and-memory']`.
- *Check off a milestone:* `get_plan` → milestone id 4 "Pilot data collected (n=12)"; `complete_milestone(4)` → completed_at set; `get_project_overview` progress moved 3/9 (33%) → 4/9 (44%).
- *Add a reference by DOI:* `add_reference_by_doi("10.3758/s13423-017-1271-2", project)` → 201, key `white2017testing` from live Crossref; `get_reading_queue` contains it.
- Also exercised live: `add_note` (with a resolving wiki-link), `quick_capture`, `run_bib_check` (4 categories), `search` (mixed types). All 12 tools listed over the protocol.
- Suite: 152 passed; ruff check + format clean.

**Decisions:** `mcp` SDK added (spec-sanctioned, see DECISIONS.md); client kept SDK-free and Django-free so the API stays the single contract.

**Known gaps → Backlog:** none new. **All six phases are now gated; Backlog work begins top-down.**

### Phase 5 — Research thinking tools + dashboards (2026-06-10)

**Built:** `research` app — Hypothesis ledger (evidence balance per hypothesis, status auto-suggested from supports/contradicts/mixed but only ever set manually), Evidence linked to references/notes/documents, dated markdown ExperimentEntry log linked to hypotheses, Dataset registry (location/version/checksum); cross-project dashboard at `/` — active projects with current phase + progress bars, upcoming milestones and manuscript deadlines across everything (overdue flagged), GitHub-style 26-week activity heatmap from created_at/updated_at across 14 models, monthly stats (papers read, notes written, milestones completed, experiments logged), inbox triage count.

**Evidence per acceptance criterion** (clean run from fresh volume):
- *Dashboard answers "what should I work on today, everywhere?" in one screen:* live `/` shows the active project with its in-progress phase and progress bar, the overdue "Pilot data collected" milestone, the manuscript deadline countdown, monthly stats, and the activity heatmap — all above the fold. Test `test_dashboard_answers_today_everywhere` asserts project, phase, milestone, deadline, stats and heatmap in one response.
- Ledger: live page shows "2 supports · 1 contradicts" with "balance suggests" hint while the stored status (Testing) wins; `TestSuggestedStatus` covers supported/contradicted/inconclusive/manual-wins.
- Experiment log + dataset registry render seeded entries (verified live).
- Suite: 140 passed; ruff check + format clean.

**Decisions:** evidence-balance suggestion is advisory-only (never auto-writes status) — keeps the researcher's judgment authoritative.

**Known gaps → Backlog:** none new.

### Phase 4 — Writing studio (2026-06-10)

**Built:** `writing` app — Manuscript (10-state pipeline, venue, deadline with `days_to_deadline`), ManuscriptReference (with `cite_key_override`), SubmissionEvent; status-column board per project and a global `/writing/` board in the sidebar; per-manuscript bibliography picked from the library with `manuscript.bib` export honoring key overrides; cite checker (upload or paste `.tex` → parses `\cite/\citep/\citet/\parencite/\textcite/\autocite/...` incl. optional args and multi-key) reporting cited-but-missing, in-bib-but-uncited, matched; bib checkers v1 run scoped to the manuscript on its detail page; vertical submission timeline with inline event logging; deadline countdown on the project overview.

**Evidence per acceptance criterion** (clean run from fresh volume):
- *Idea → published with events logged:* `test_full_lifecycle_idea_to_published` walks all five submission events and the status change to published, then asserts the rendered timeline. Live: seeded manuscript detail shows "Submission timeline", "Reviews received", and the deadline countdown.
- *Cite checker flags a deliberately broken `.tex` fixture in tests:* `writing/tests/fixtures/broken.tex` + `test_broken_tex_fixture_flagged_correctly` asserts exact missing keys (anotherghost2021, ghostpaper1999, missingkey2020) and the uncited key; live curl upload of the fixture renders ghostpaper1999/missingkey2020 under "cited, missing from bib" and the "never cited" column.
- Bib export: live `export.bib` contains 6 entries; override keys tested.
- Suite: 129 passed; ruff check + format clean.

**Decisions:** none non-obvious (board is plain status columns — no drag-and-drop JS, status changes via the edit form, consistent with "no JS where a form works").

**Known gaps → Backlog:** none new.

### Phase 3 — Knowledge graph, notes, search (2026-06-10)

**Built:** huey 3 + Redis 7 (compose service, `run_huey` worker, MemoryHuey-immediate in tests); `literature/sync.py` — OpenAlex citation-edge sync (batched DOI→openalex_id resolution, referenced_works edges among project refs, citation_count refresh) with `CitationSyncState` (idle/syncing/done/failed + last-synced message) surfaced on the graph page with a sync button; notes — `Note`/`NoteLink`, markdown editor with live HTMX preview, `[[wiki-links]]` parsed to links on save (unresolved titles reported), backlinks panel, note→reference citations; knowledge graph — `core/graph.py` builder, session endpoint `/projects/{slug}/graph.json` + API `GET /api/v1/projects/{slug}/graph/`, page with 3d-force-graph + 2D toggle, node size from citation count, color from reading status/type, click side panel with link to object; global Postgres FTS (`core/search.py`) across projects, references, notes, documents, decisions, phases, milestones with a sidebar search box.

**Evidence per acceptance criterion** (clean run incl. `run_huey` worker):
- *20+ references → navigable 3D citation graph:* seeded project graph.json = 22 reference nodes, 3 note nodes, links {citation: 33, note-link: 4, note-citation: 5}; graph page loads 3d-force-graph.
- *Creating [[links]] updates graph and backlinks:* created "Gate Note" with `[[Load theory overview]]` via UI → backlinks panel on target shows it; graph.json note-links 4→5.
- *Search returns mixed-type results:* `/search/?q=attention` → 13 results grouped under projects, references, phases, decisions.
- *Background sync with visible state:* created "Sync Gate" project via API with DOIs 10.1038/nature14539 + 10.1162/neco.1997.9.8.1735 → POST graph/sync → huey consumer processed → "Last synced 2026-06-10 22:10 — 2/2 references matched on OpenAlex; 1 new edge(s)"; graph shows the real LeCun→LSTM citation edge.
- Tests: sync (mocked transport, 4 cases), graph builder, wiki-link service (5), notes views (4), search (4). Suite: 118 passed; ruff clean.

**Decisions:** Graph JSON has both a session-auth UI endpoint and the keyed API endpoint, both rendering from one builder. Tests run huey in immediate MemoryHuey mode.

**Known gaps → Backlog:** none new.

### Phase 2 — API v1 + Literature core (2026-06-10)

**Built:** `api` app — DRF + drf-spectacular, sole auth via `X-API-Key` against `ATLAS_API_KEY` (session auth rejected on API; UI login untouched), all Phase 1 resources read/write incl. multipart document upload, pagination 50, schema + Swagger at `/api/docs/`; `literature` app — global Reference library, add by DOI (Crossref → OpenAlex fallback, httpx, 10s timeouts, `MetadataError` with human-readable reasons), add by arXiv ID (arXiv export API — see DECISIONS.md), manual entry with generated `lastnameYEARfirstword` keys, BibTeX paste-import with DOI dedup, PDF attach; ProjectReference linking with reading status + priority, per-project literature page with filters, reading queue (priority-sorted, HTMX inline status change), per-project `.bib` export with stable keys; bib checkers v1 in `literature/services.py` (duplicates DOI-exact + fuzzy-title, missing required fields per entry type, DOI resolution via doi.org, retraction flag via Crossref `updates:` filter) on a report page with offline mode; quick-capture inbox with triage + API; `POST /api/v1/references/by-doi`, `/api/v1/project-references/`, `/api/v1/quick-capture/`.

**Evidence per acceptance criterion** (clean run: fresh volume → compose → migrate → seed_demo → runserver):
- *curl with API key creates a project and adds a reference by DOI end-to-end:* `POST /api/v1/projects/ {"name": "Gate Two Project"}` → 201 slug `gate-two-project`; `POST /api/v1/references/by-doi/ {"doi": "10.1038/nature12373", "project": "gate-two-project"}` → 201, real Crossref metadata, key `kucsko2013nanometre`, linked to project. Also live arXiv: `{"doi": "1706.03762"}` → 201 `vaswani2017attention`.
- *Bib report renders with the four checker categories:* `/projects/attention-and-memory/literature/report/` shows Duplicates, Missing fields, DOI resolution, Retractions; seeded fake DOIs correctly flagged "does not resolve (HTTP 404)"; incomplete seeded entry flagged for missing fields. Tests: `literature/tests/test_services.py::TestCheckers` (8 tests).
- *Reading queue works:* `/literature/queue/` lists only to-read/skimmed, HighPaper before LowPaper (priority order, tested); HTMX status change → 200 and persists.
- Auth: missing/wrong key → 401; session cookie on API → 401; UI still login-only (tested).
- Suite: 98 passed; ruff check + format clean.

**Decisions:** arXiv via arXiv export API (OpenAlex 404s DataCite DOIs — verified live); `doi` is the unique nullable identity, `arxiv_id`/`openalex_id` plain strings.

**Known gaps → Backlog:** none new (OpenAlex enrichment of arXiv records arrives naturally with Phase 3 citation sync).

### Phase 1 — Projects, Documents, Plans (2026-06-10)

**Built:** Project CRUD with status/color/position, archive action, slug auto-generation; DecisionRecord log (list/create/edit/delete, markdown rendering via markdown+nh3); Project Overview (current phase, progress bar, next 5 milestones with overdue flags, recent documents, recent decisions, at-a-glance counts); plans app (Phase/Milestone/Task/ResearchQuestion, plan page with HTMX check-off for milestones and tasks, progress roll-up milestone→phase→project with OOB progress-bar update, overdue highlighting); documents app (nested folders with cycle-safe moves, per-parent unique names, upload into any folder, tags with per-project uniqueness, filter by folder/tag/all, forced-attachment download); `seed_demo` builds a realistic 4-phase psychology study exercising everything.

**Evidence per acceptance criterion** (clean run: fresh volume → `docker compose up -d --wait` → `migrate` → `seed_demo` → `runserver`; all via curl with a logged-in session, no admin):
- Create a project: `POST /projects/new/` → 302 to `/projects/gate-test-project/`.
- Build a 4-phase plan: 4× `POST .../plan/phases/new/` → 302 each; plan page renders all four.
- Upload documents into nested folders: created `Outer` → `Inner` (parent=Outer), uploaded into Inner → 302; doc listed under `?folder=Inner`; download → 200 with `Content-Disposition: attachment`.
- Check off milestones, progress propagates: `POST .../milestones/{pk}/toggle/` → response contains "1/2 milestones" in the phase card **and** OOB `#project-progress`; overview then shows "1/2 milestones".
- Record a decision: `POST .../decisions/new/` → 302; decision list shows it.
- Tests cover roll-up and nesting: `plans/tests/test_plans.py::TestProgressRollup` (4 tests), `documents/tests/test_documents.py::TestFolderNesting` (5 tests). Full suite: 44 passed; ruff check + format clean.
- Seeded project: overview shows "Current phase: Experimental design & pilot"; plan page highlights the overdue pilot milestone.

**Decisions:** Folder delete uses `on_delete=SET_NULL` for documents (files survive at project root) but cascades subfolders — logged in DECISIONS.md. HTMX toggle returns the phase card partial plus an out-of-band project progress bar, keeping one source of truth for markup.

**Known gaps → Backlog:** none new.

### Phase 0 — Scaffold (2026-06-10)

**Built:** settings split (base/dev/prod) on django-environ; docker-compose Postgres 16; Django 5.2 `LoginRequiredMiddleware` (all pages require login, exactly one superuser); base template with fixed left sidebar + breadcrumb block; Tailwind v4 standalone CLI via `make css`; pytest + ruff configured; `seed_demo` stub; README quick start.

**Evidence per acceptance criterion:**
- `docker compose up -d --wait` → healthy; `manage.py migrate` applied cleanly on a fresh volume.
- Styled login page: `curl http://127.0.0.1:8000/login/` → 200, contains tagline; `static/css/app.css` built by Tailwind CLI.
- Login required: `curl /` anonymous → 302 to `/login/?next=/`; POST credentials → 302 to `/` and dashboard renders ("Dashboard" present).
- Empty dashboard shell: `core:dashboard` at `/`, smoke-tested in `core/tests/test_smoke.py::test_dashboard_renders_when_logged_in`.
- Tests & lint: `pytest -q` → 5 passed; `ruff check .` and `ruff format --check .` clean.

**Decisions:** Python 3.12 via uv (see DECISIONS.md). Used Django 5.1+ built-in `LoginRequiredMiddleware` instead of a custom middleware/mixin — less code, contrib login view is auto-exempt.

**Known gaps → Backlog:** none.
